[project]
name = "eigen_wrapper"
root = "."
uuid = "eigen-wrapper"

[compile]
source_files = ["eigen_wrapper.cpp"]
flags = ["-std=c++17", "-fPIC", "-O2", "-g"]

[binary]
type = "shared"

[paths]
output = "julia"
