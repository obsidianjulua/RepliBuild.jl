[project]
name = "AdvancedTypes"
root = "."

[paths]
source = "."
output = "build_advanced/julia"

[compile]
source_files = ["test_advanced_types.cpp"]
flags = ["-std=c++17", "-O0", "-g", "-fPIC"]

[binary]
type = "shared"
name = "libadvanced_types"

[binding]
tier = "introspective"
