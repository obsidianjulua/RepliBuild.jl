var documenterSearchIndex = {"docs":
[{"location":"inheritance-abi/#The-inheritance-ABI","page":"The Inheritance ABI","title":"The inheritance ABI","text":"A reasoning guide to how RepliBuild makes C++ inheritance — multiple inheritance (MI) and virtual inheritance (VMI) — callable from Julia. This page teaches the mental model so you can read a generated wrapper and know what it's doing. For the dated, blow-by-blow record of how it was built, see the update notes: MI + vcall and virtual inheritance. For the dialect these decisions are expressed in — jlcs.vcall, jlcs.type_info and their lowering — see ABI Marshalling as Compiler IR.","category":"section"},{"location":"inheritance-abi/#The-one-invariant","page":"The Inheritance ABI","title":"The one invariant","text":"Everything below follows from a single rule:\n\nnote: The invariant\nEvery generated Class_method(this, …) wrapper expects this to already point at the Class subobject — not at the start of whatever derived object happens to contain it.\n\nBecause the wrapper knows the static type at each call site, it can choose the right offset to turn a derived pointer into the base pointer the method wants. That conversion is the upcast helper. Dispatch itself stays dumb and class-local; all the difficulty of inheritance is pushed into \"given a Derived, produce the correct base-subobject pointer.\"\n\nSo there is really only one question to answer, in three increasingly hard forms:\n\nCase Where is the base? Upcast\nSingle / primary base offset 0 none — a Derived* already is a Base*\nMultiple inheritance a fixed offset add a compile-time constant\nVirtual inheritance depends on the most-derived type read the offset from the object's vtable at runtime","category":"section"},{"location":"inheritance-abi/#1.-Single-inheritance-—-nothing-to-do","page":"The Inheritance ABI","title":"1. Single inheritance — nothing to do","text":"The base sits at offset 0, so the derived pointer is bit-for-bit a valid base pointer. No helper is emitted; you pass the handle straight through.","category":"section"},{"location":"inheritance-abi/#2.-Multiple-inheritance-—-add-a-constant","page":"The Inheritance ABI","title":"2. Multiple inheritance — add a constant","text":"For Derived : Base1, Base2, the object is three consecutive regions:\n\nOffset Region Upcast from a Derived* p\n0 Base1 subobject (vptr1, data) none — p already is a Base1*\n16 Base2 subobject (vptr2, data) Derived_as_Base2(p) = p + 16\n32 Derived's own data —\n\nThe +16 is not guessed — it is DW_AT_data_member_location on the DW_TAG_inheritance edge in DWARF, captured at extraction time. The generated helper is exactly:\n\nfunction Derived_as_Base2(obj)::Ptr{Cvoid}\n    p = obj isa Ptr ? Ptr{Cvoid}(obj) : Ptr{Cvoid}(obj.handle)\n    return p + 16                       # static offset, known at compile time\nend\n\nBase1 gets no helper (offset 0). Base members are handled the same way: flatten_struct_members rebases each inherited member by its base's subobject offset, so derived.some_base2_field reads from the right place.","category":"section"},{"location":"inheritance-abi/#3.-Virtual-inheritance-—-read-the-offset-from-the-vtable","page":"The Inheritance ABI","title":"3. Virtual inheritance — read the offset from the vtable","text":"A diamond shares one copy of the virtual base. The catch: the shared base's offset is a property of the most-derived object, not of the class. A standalone Left puts VBase at +16; a Left embedded in a Diamond puts it at +32. A Left* cannot know statically which world it is in — only the object's vtable does.\n\nFor Diamond : Left, Right where both Left and Right derive virtually from VBase, there is exactly one shared copy of VBase, and a Diamond* p lays out as:\n\nOffset Region\n0 Left subobject (vptr_L) — p points here\n16 Right subobject (vptr_R)\n28 Diamond's own data (d)\n32 shared VBase (vptr_V) — one copy, reached by both Left and Right\n\nThe offset to that shared VBase isn't a class constant — it lives in the vtable. Left's vtable (what vptr_L points into) carries the vbase-offset entry just below its address point:\n\nVtable position Contents\nvptr_L − 24 vbase-offset = 32 — the entry the upcast reads\nvptr_L (address point) RTTI, &tag(), the virtual method slots\n\nso Left_as_VBase(p) = p + *(vptr_L − 24) = p + 32.\n\nThe DWARF location on a virtual edge is an expression, not a constant (this + *(vptr − 24)); the parser distills it into vbase_vtable_offset = −24. The generated helper does the two-hop read — load the vptr, read the vbase-offset entry below the address point, add it:\n\nfunction Left_as_VBase(obj)::Ptr{Cvoid}\n    p    = obj isa Ptr ? Ptr{Cvoid}(obj) : Ptr{Cvoid}(obj.handle)\n    vptr = unsafe_load(Ptr{Ptr{Int64}}(p))   # load the vtable pointer\n    return p + unsafe_load(vptr + (-24))      # read the dynamic vbase offset\nend\n\ntip: The canary that proves it's dynamic\nLeft_as_VBase is one generated function, yet it resolves +16 on a standalone Left and +32 on a Left* that is really a Diamond — because it reads the answer out of each object's own vtable. Any static implementation gets one of the two wrong. This is the pin in test/vi_test/verify.jl.\n\nWhen a virtual base is reached through a static chain (e.g. Diamond_as_VBase = static +0 to Left, then dynamic), the helper composes: apply the static prefix to p first, then do the vtable read. One helper text is correct for every dynamic type.","category":"section"},{"location":"inheritance-abi/#Virtual-dispatch-and-overrides-—-why-you-never-do-slot-math","page":"The Inheritance ABI","title":"Virtual dispatch and overrides — why you never do slot math","text":"You might expect dispatching a virtual method on a secondary or virtual base to require walking vtables and adjusting this. It doesn't, because the C++ compiler already did the hard part:\n\nIt re-homes an override into the derived class's own primary vtable — an override of Base2::get_b in Derived reports its slot in Derived's vtable, not Base2's. Diamond::tag re-homes the same way.\nIt plants this-adjusting thunks in the secondary vtables that fix the pointer back before entering the override.\n\nSo RepliBuild dispatches class-local: index the method's own slot, with a class-relative this — vtable_offset = 0, this_offset = 0. The caller-side upcast plus the compiler's thunks do the pointer adjustment; the jlcs.vcall lowering never has to. This is why virtual inheritance needed zero changes to the dialect or the thunk generator — the only new machinery was the dynamic read in the Julia upcast helper.\n\nTwo deliberate exceptions:\n\nDestructors stay direct calls. Managed finalizers and scope-RAII need exact-class destruction, not dynamic dispatch. Polymorphic deletion through a base pointer goes through a small C++-side helper.\nvcall gates on scalar/pointer signatures. Virtual methods that return a struct by value or take a packed struct by value keep the direct-call path (the vcall lowering does no sret/packed coercion yet).","category":"section"},{"location":"inheritance-abi/#Where-each-piece-lives","page":"The Inheritance ABI","title":"Where each piece lives","text":"Three seams turn \"what the C++ ABI says\" into \"something Julia can call\":\n\nExtraction — DWARFParser.jl + Compiler.jl. Captures the static base offset (DW_AT_data_member_location) and parses the virtual edge's location expression into vbase_vtable_offset. ClassInfo carries parallel base_offsets / virtual_bases / vbase_vtable_offsets vectors; bases are sorted by subobject offset for determinism.\nMetadata — jlcs.type_info carries two tables: baseNames/baseOffsets (static) and vbaseNames/vbaseVtableOffsets (dynamic). Virtual bases never appear in the static table.\nWrapper — GeneratorCpp.jl. _collect_upcasts! (src/Wrapper/Cpp/GeneratorCpp.jl:1539) walks the base DAG accumulating static offsets, then emits either the static helper (:1591) or the dynamic helper (:1613).","category":"section"},{"location":"inheritance-abi/#Reading-and-using-a-generated-wrapper","page":"The Inheritance ABI","title":"Reading and using a generated wrapper","text":"The whole integration contract, at a glance:\n\nX_as_Y(p) = p + N → Y is a normal (MI) base at fixed offset N.\nX_as_Y(p) = p + unsafe_load(vptr + N) → Y is a virtual base; the offset is read from the vtable.\nTo call a Y method on an X object, pass X_as_Y(obj) as the handle. That is the only rule you have to remember — the compiler pre-arranged everything else.\nMembers: MI base members are flattened onto the derived accessor (rebased by offset). Virtual base members are deliberately not flattened — their offset is dynamic, so you reach them by upcasting (X_as_VBase) and using the base's own accessors.","category":"section"},{"location":"inheritance-abi/#What-is-deliberately-not-built","page":"The Inheritance ABI","title":"What is deliberately not built","text":"So you don't chase these as bugs — they're unbuilt pieces, tracked alongside the rest in Boundaries:\n\nvcall dispatch for struct-shaped virtual signatures (see the gate above).\nA dialect-level virtual-base upcast op, for IR that must do the adjustment inside a thunk. Nothing needs one today — the caller-side dynamic upcast above composes with class-local vcall coordinates, which is why virtual inheritance needed zero dialect changes.\nThe struct ABI classifier is x86-64 SysV only; Win64 / AAPCS are not modeled.\n\n(MEMORY-class by-value struct arguments were on this list and are now built: they pass as an alloca'd caller-owned copy with llvm.byval(T) and explicit alignment, matching what clang emits. See the lowering.)","category":"section"},{"location":"inheritance-abi/#Where-the-executable-truth-lives","page":"The Inheritance ABI","title":"Where the executable truth lives","text":"test/mi_test/ — compiler-laid-out two-base fixture (38/38): override through a secondary vtable, C++-handed base pointer, a pinned wrong-this canary.\ntest/vi_test/ — the diamond (33/33): the same-helper 16-vs-32 canary, single shared copy across three paths, override dispatch through the vbase vtable.\nDialect level: MLIR templates §8b (MI vcall) and §8d (the diamond).","category":"section"},{"location":"guide/#User-Guide","page":"Workflow","title":"User Guide","text":"This guide covers the common workflows for using RepliBuild.jl.","category":"section"},{"location":"guide/#Basic-Workflow","page":"Workflow","title":"Basic Workflow","text":"The standard workflow involves three steps: discovery, building, and wrapping.","category":"section"},{"location":"guide/#1.-Discovery","page":"Workflow","title":"1. Discovery","text":"The discover function scans your directory for C/C++ files and generates a replibuild.toml configuration file.\n\nRepliBuild.discover()               # current directory\nRepliBuild.discover(\"path/to/project\")\n\nRe-running discovery with force=true regenerates the config but preserves hand-curated keys that cannot be derived from source: [types].templates/template_headers, [wrap].varargs/macros/shim_headers/cstring_owned/tier1, and [link].promote_statics. A preserved: … line in the output reports what carried over. Everything else — including [compile] flags, include_dirs, and [dependencies] — is regenerated from the scan, so keep a hand-written config under version control rather than relying on force=true to round-trip it.\n\nDiscovery sees the shape of a source tree and nothing else. The information it cannot derive — template instantiations, macros, vararg signatures, char* ownership, the flags upstream's build system would have supplied — is enumerated in What discovery cannot know, which is the section to read before wrapping an unfamiliar library.","category":"section"},{"location":"guide/#2.-Building","page":"Workflow","title":"2. Building","text":"Once configured, the build function compiles your code into a shared library.\n\nRepliBuild.build()\n\nThis step performs:\n\nCompilation to LLVM IR (.c files use the bundled JLL clang; .cpp files use system clang++).\nLinking and optimization — for C projects this runs in-process on Julia's own libLLVM (version-matched by construction); C++ uses the external LLVM pipeline.\nGeneration of the shared library.\nExtraction of DWARF metadata for wrapping.","category":"section"},{"location":"guide/#3.-Wrapping","page":"Workflow","title":"3. Wrapping","text":"Finally, generate the Julia wrapper module.\n\nRepliBuild.wrap()\n\nThis creates a Julia module in the julia/ directory. Load it with the standard pattern:\n\ninclude(\"julia/MyProject.jl\")\nusing .MyProject","category":"section"},{"location":"guide/#Automated-Workflow","page":"Workflow","title":"Automated Workflow","text":"You can chain these steps together using the flags in discover:\n\n# Discover, Build, and Wrap in one go\nRepliBuild.discover(build=true, wrap=true)","category":"section"},{"location":"guide/#Package-Registry-and-the-Hub","page":"Workflow","title":"Package Registry and the Hub","text":"RepliBuild includes a local package registry (~/.replibuild/registry/) that caches build artifacts so repeated loads are instant, and a community registry — RepliBuild-Hub — holding ready-made replibuild.toml configs for popular C/C++ libraries.\n\n# Register a project locally (discover() does this automatically)\nRepliBuild.register(\"replibuild.toml\")\n\n# Load a registered package — builds on first call, cached thereafter.\n# On a local-registry miss, the config is fetched from the Hub.\nLua = RepliBuild.use(\"lua\")\nLua.luaL_newstate()\n\n# Search the Hub by name, description, tags, or language\nRepliBuild.search()          # list all Hub packages\nRepliBuild.search(\"xml\")\n\n# List / remove local registrations\nRepliBuild.list_registry()\nRepliBuild.unregister(\"lua\")\n\n# Scaffold a distributable Julia package from a registered project\nRepliBuild.scaffold_package(\"LuaWrapper\")\n\nThe use() function handles the full lifecycle: resolve dependencies, build (or load from cache), wrap, and return a loaded Julia module. Cached builds live in ~/.replibuild/builds/<hash>/; the cache key includes RepliBuild's own version and git revision, so upgrading RepliBuild rebuilds each package once with current codegen instead of serving stale wrappers. The REPLIBUILD_HOME environment variable overrides the registry location; REPLIBUILD_HUB_URL points search()/use() at a private Hub mirror.","category":"section"},{"location":"guide/#C-vs-C-Projects","page":"Workflow","title":"C vs C++ Projects","text":"RepliBuild uses wrap.language as an extensible dispatch key to select the generator, compiler toolchain, and build defaults for a project:\n\n[wrap]\nlanguage = \"c\"   # pure-C project: JLL clang, in-process libLLVM link/opt\nlanguage = \"cpp\" # C++ project: system clang++, external LLVM pipeline\n\ndiscover() sets this automatically from the scanned source file extensions.\n\nThe toolchain requirements differ by bucket: C projects need no external LLVM at all, while C++ projects need a system LLVM/MLIR 21+ install for the JLCS dialect and Tier 2 thunks. An experimental Rust generator (language = \"rust\") exists for extern \"C\" + #[repr(C)] surfaces.\n\nFor C projects enable_lto defaults to true, which emits the LTO bitcode artifact alongside the library — see Zero-cost LTO dispatch below for why production configurations disable it anyway, and use [wrap.tier1] instead.","category":"section"},{"location":"guide/#Ingest-Mode-(pre-built-binaries)-—-experimental,-C-only","page":"Workflow","title":"Ingest Mode (pre-built binaries) — experimental, C only","text":"For C libraries with elaborate build systems (autotools, CMake with code generators) that RepliBuild's source pipeline cannot reproduce, build upstream yourself with -g and ingest the resulting binary. RepliBuild skips compilation — only DWARF extraction and wrapper generation run.\n\n# Scaffold an ingest config\ntoml = RepliBuild.ingest(\"/path/to/libfoo.so\",\n                         headers=[\"/path/to/include\"],\n                         name=\"foo\",\n                         language=:c)\n\n# Or run the whole pipeline immediately\ntoml = RepliBuild.ingest(\"/path/to/libfoo.so\",\n                         headers=[\"/path/to/include\"],\n                         build=true, wrap=true)\n\nThe generated replibuild.toml carries an [ingest] section whose presence flips RepliBuild into ingest mode:\n\n[ingest]\nlibrary = \"/path/to/libfoo.so\"\nheaders = [\"/path/to/include\"]\nextra_link_libs = [\"m\", \"pthread\"]   # optional — additional -l libs at load time\n\nConstraints to understand before reaching for ingest:\n\nIngest is a fallback, not the flagship path. Extraction quality depends on upstream's compiler and debug-info settings, which RepliBuild does not control.\nIngested libraries dispatch through Tier 3 (ccall) only — no LTO bitcode, no Tier 2 thunks (both require the source build).\nC++ API surfaces are unsupported. Classes, methods, templates, and virtual dispatch need the MLIR dialect thunks only the source build generates; at best the extern \"C\" surface of a C++ binary works. Both entry points warn accordingly.","category":"section"},{"location":"guide/#Idiomatic-Julia-Class-Wrappers","page":"Workflow","title":"Idiomatic Julia Class Wrappers","text":"When your C++ library exposes a class through factory/destructor pairs, RepliBuild automatically generates an idiomatic mutable struct wrapper on top of the raw FFI bindings.\n\nDetection: the wrapper generator scans for:\n\nFactory functions whose name matches create_X, new_X, make_X, alloc_X, init_X, or whose return type is X*.\nDestructor/deleter functions whose name matches delete_X, destroy_X, free_X, dealloc_X, or X_destroy.\nInstance methods associated with the same class via the DWARF class field.\n\nGenerated output for a Circle class:\n\n# Raw bindings (always generated)\nfunction create_circle(radius::Cdouble)::Ptr{Cvoid} ... end\nfunction get_area(this::Ptr{Cvoid})::Cdouble ... end\nfunction delete_shape(this::Ptr{Cvoid}) ... end\n\n# Idiomatic wrapper (generated automatically on top)\nmutable struct Circle\n    handle::Ptr{Cvoid}\n\n    function Circle(radius::Cdouble)\n        handle = create_circle(radius)\n        obj = new(handle)\n        finalizer(obj) do o\n            delete_shape(o.handle)\n        end\n        return obj\n    end\nend\n\n# Method proxies via multiple dispatch\nget_area(c::Circle) = get_area(c.handle)\n\nFor C++ classes without factory functions, the generator emits in-place constructor/destructor thunks and Managed handle types whose GC finalizers call the DWARF-resolved destructor directly. Classes with multiple inheritance additionally get <Derived>_as_<Base> upcast helpers (static offset adjustment), and virtual bases get dynamic <Derived>_as_<VBase> helpers that read the correct offset through the object's vtable at runtime — the same helper is correct for every dynamic type.","category":"section"},{"location":"guide/#char*-Returns:-Ownership-Policy","page":"Workflow","title":"char* Returns: Ownership Policy","text":"C APIs use char* returns three different ways, and the wrapper handles each explicitly:\n\nDefault: the wrapper returns Union{String,Nothing} — NULL becomes nothing (a NULL char* is a value in C APIs, not an exception), anything else is copied into a Julia String.\nOwned returns: for functions returning malloc'd buffers, declare the deallocator in the TOML and the wrapper frees the C buffer after copying:\n[wrap.cstring_owned]\ncJSON_Print = \"cJSON_free\"\nRaw access: every Cstring-returning function also gets an exported <name>_ptr variant that returns the pointer unchanged — no copy, no NULL check, never freed — for lifetime-sensitive callers.\n\nOwnership of a returned char* is not recoverable from DWARF, so it is declared per-library in the TOML rather than guessed from function names.","category":"section"},{"location":"guide/#Replacing-Manual-Shims","page":"Workflow","title":"Replacing Manual Shims","text":"When wrapping C/C++ libraries, developers often have to write manual C wrappers (\"shims\") for things that aren't native functions: templates, varargs, and preprocessor macros. RepliBuild handles all of these declaratively via replibuild.toml.","category":"section"},{"location":"guide/#Template-Instantiation","page":"Workflow","title":"Template Instantiation","text":"C++ templates are only emitted into DWARF if the compiler actually instantiates them. To force instantiation for types you want to wrap, add them to [types]:\n\n[types]\ntemplates        = [\"std::vector<int>\", \"std::vector<double>\", \"std::pair<int,float>\"]\ntemplate_headers = [\"<vector>\", \"<utility>\"]\n\nRepliBuild auto-generates a stub .cpp file that explicitly instantiates each requested type, ensuring it appears in the DWARF metadata and the generated Julia module.","category":"section"},{"location":"guide/#Varargs","page":"Workflow","title":"Varargs","text":"Vararg wrappers lower as true variadic calls (the @ccall semicolon form), so the callee is declared variadic in LLVM IR and the x86-64 SysV register-count protocol (the AL setup that gates the callee's va_start) is emitted correctly — including for float varargs. Declare typed overloads in [wrap.varargs]:\n\n[wrap.varargs]\nlua_pushfstring = [\n    [\"Cstring\"],\n    [\"Cint\"],\n    [\"Cstring\", \"Cint\"],\n]\n\nRepliBuild generates a concrete Julia binding for each signature. Two rules that are easy to get backwards, both enforced with a hard error:\n\nList only the variadic arguments. The fixed parameters (here lua_State* and the const char* fmt) come from DWARF and are prepended for you.\nThe names are Julia types, not C types — Cstring, Cint, Cdouble, Ptr{X}, … (the full allowed set is in the configuration reference). This is the opposite of [wrap.macros], whose ret/args are C type strings pasted into generated C.","category":"section"},{"location":"guide/#Macro-Expansion","page":"Workflow","title":"Macro Expansion","text":"C/C++ preprocessor macros don't exist in compiled binaries or DWARF metadata. To expose them to Julia, configure [wrap.macros]:\n\n[wrap]\nshim_headers = [\"<stdio.h>\"]\n\n[wrap.macros.MY_MATH_MACRO]     # function-like: `args` present → MY_MATH_MACRO(a, b)\nret = \"int\"\nargs = [\"int\", \"float\"]\n\n[wrap.macros.MY_CONSTANT]       # value macro: `args` ABSENT → the bare expression\nret = \"int\"\n\nRepliBuild generates a C/C++ source file that wraps MY_MATH_MACRO inside a typed function and compiles it alongside your project. The presence of the args key is the switch, not its contents — args = [] means a zero-argument function-like macro (MACRO()), omitting args means a value macro (MACRO). Shims are emitted with default symbol visibility (they survive -fvisibility=hidden builds), and a header-collision guard verifies each shim #include resolves inside your project/dependency tree rather than to a system-installed copy of the same header at a different version.","category":"section"},{"location":"guide/#Zero-Cost-LTO-Dispatch","page":"Workflow","title":"Zero-Cost LTO Dispatch","text":"Tier 1 hands the C function's LLVM IR to Base.llvmcall, so Julia's JIT merges the C body directly into the calling Julia function — full cross-language inlining, no call instruction. There are two payloads that can carry that IR, and they are configured independently.","category":"section"},{"location":"guide/#Per-function-slices-([wrap.tier1])-—-experimental,-off-by-default","page":"Workflow","title":"Per-function slices ([wrap.tier1]) — experimental, off by default","text":"warning: Tier 1 is a side project, not a supported tier\nIt ships, it works on the libraries it has been driven over, and it is not something to reach for in production. It is off by default (enable = false), no RepliBuild-Hub package turns it on, and its test suite is deliberately unwired from devtests.jl — run test/test_static_promotion.jl, test/test_slicer.jl and test/test_tier1_dispatch.jl by hand if you work on it.Everything below is accurate; treat it as documentation of an experiment. The design doctrine is that llvmcall is a passenger tier and never the driver — any doubt resolves to ccall, which is why the machinery is mostly refusals, demotions and guards. Improvements are welcome by PR.\n\nA slice is a declarations-only module: one function's body, and every callee and global it reaches left as a bare declare, resolved at JIT time against the .so the wrapper already dlopen'd RTLD_GLOBAL. Size stops tracking library size and starts tracking function size — in Lua, lua_gettop is a 2.8 KB slice cut from a 15.8 MB module, and even luaL_openlibs comes out at 6 KB.\n\n[link]\npromote_statics = true    # default\n\n[wrap.tier1]\nenable = true\n\nEach accepted function gets a @generated kernel that decides ccall vs llvmcall once, at generation time, plus a public wrapper that routes through it:\n\nconst _SLICE_lua_gettop = joinpath(@__DIR__, \"slices\", \"lua_gettop.ll\")\nisfile(_SLICE_lua_gettop) && include_dependency(_SLICE_lua_gettop)\n\n@generated function _TIER1_lua_gettop(__ptr_L::Ptr{lua_State})\n    # llvmcall is opportunistic: any doubt resolves to the ccall body.\n    if ccall(:jl_generating_output, Cint, ()) == 1 || !isfile(_SLICE_lua_gettop)\n        return :(ccall((:lua_gettop, LIBRARY_PATH), Cint, (Ptr{lua_State},), __ptr_L))\n    end\n    ir = read(_SLICE_lua_gettop, String)\n    return :(Base.llvmcall(($ir, \"lua_gettop\"), Cint, Tuple{Ptr{lua_State}}, __ptr_L))\nend\n\nfunction lua_gettop(L::Any)::Cint\n    __cc_L = Base.cconvert(Ptr{lua_State}, L)\n    __ptr_L = Base.unsafe_convert(Ptr{lua_State}, __cc_L)\n    GC.@preserve __cc_L begin\n        return _TIER1_lua_gettop(__ptr_L)\n    end\nend\n\nThe slice is read at generation time — the first call — and spliced into the returned expression as a literal, which satisfies Base.llvmcall's statically-evaluable IR requirement while keeping module load free of slice I/O. The generation-time jl_generating_output check exists because emitting a sliced llvmcall inside a precompile worker deadlocks the JIT engine lock whenever a declare binds a dlopened library's symbol (and an untaken top-level branch reaches emission through inference alone): inside a precompile worker the kernel splices the plain ccall body instead, and a runtime first call regenerates to the slice. The isfile guard means a wrapper shipped or relocated without its slices/ directory demotes to a plain ccall wrapper instead of failing.\n\nThere is no if !isempty(...) branch: a function is decided at generation time, not call time. Anything not accepted emits the same ccall it always did, and the module exports TIER1_FUNCTIONS::Set{String} naming the functions that dispatch through a slice. Slicing a function successfully is necessary but not sufficient — the ABI shape gate still refuses Cstring and struct crossings — so that set can be smaller than the number of slices the wrap accepted.\n\nThree guarantees make this safe where whole-module embedding was not:\n\nOne copy of internal state. Static promotion ([link] promote_statics) renames anything a slice might bind by declare but that cannot reach the .so's dynamic symbol table — file-local statics, and external-linkage symbols marked hidden — to an exported __rb_<lib>_<name>, on the exact module that becomes both the .so and the slice source. Tier-1 and Tier-3 calls provably see the same state. Promoted names are filtered out of the wrappable API.\nSlices are refused, never guessed. Variadic targets, blockaddress, alias/ifunc, and an unpromoted module come back as refusals; :weak, :inline_asm and :module_asm demote through the hazard gate. Every slice is verified before it is written.\nSymbol pre-flight. An unresolved declare does not raise — ORC prints Symbols not found: [...] and then blocks forever on the first call. So before any slice reaches disk, the generator dlopens the .so and dlsym-checks every name the slice declares, which is the exact lookup ORC will perform. A miss demotes that one function to ccall with a warning naming the symbol; a .so that will not dlopen disables Tier 1 for the whole wrap.\n\nEach slice const is paired with an include_dependency on the same path, so the .ll files are real precompilation dependencies: a wrapper vendored into a package recompiles when its slices change. On Julia 1.11+ that tracking is by content, so restoring a slice with a different mtime but identical bytes correctly does not force a rebuild.","category":"section"},{"location":"guide/#Whole-module-bitcode-([link]-enable_lto)-—-scale-limited","page":"Workflow","title":"Whole-module bitcode ([link] enable_lto) — scale-limited","text":"The older payload emits <name>_lto.bc and embeds the whole linked module at each call site, behind a runtime branch:\n\nif !isempty(LTO_IR)\n    return Base.llvmcall((LTO_IR, \"vector_dot\"), Cdouble, Tuple{Ptr{Cvoid}, Ptr{Cvoid}, Cint}, a, b, n)\nelse\n    return ccall((:vector_dot, LIBRARY_PATH), Cdouble, (Ptr{Cvoid}, Ptr{Cvoid}, Cint), a, b, n)\nend\n\nwarning: Why production configs set `enable_lto = false`\nWhole-module embedding has two verified consequences at real-library scale:JIT scale limit — embedding a whole library's IR (hundreds of functions) per call can crash Julia's JIT. Small benchmark modules work; whole libraries do not reliably.\nDuplicated internal state — file-local static definitions stay private to the embedded bitcode, so Tier-1 and Tier-3 calls can observe different copies of the library's internal state (observed live on a JSON parser's error-reporting path).Both are properties of the whole-module payload, not of Tier 1. Slices fix the first by construction and the second through static promotion. Treat enable_lto as an experimentation feature for small stateless kernels, and use [wrap.tier1] for real libraries. The two are independent knobs.","category":"section"},{"location":"guide/#AOT-Thunks","page":"Workflow","title":"AOT Thunks","text":"Tier 2 functions normally compile their MLIR thunks through the in-process JIT at module load. Setting aot_thunks = true pre-compiles them at build time instead:\n\n[compile]\naot_thunks = true\n\nDuring RepliBuild.build(), the JLCS dialect thunks are generated and compiled into a companion <name>_thunks.so placed alongside the main library. The generated wrapper emits static ccall bindings against THUNKS_LIBRARY_PATH — no JIT startup, no MLIR runtime dependency after build. Requires src/mlir/build/libJLCS.so (cd src/mlir && ./build.sh).","category":"section"},{"location":"guide/#Debugging-a-Tier-2-Call","page":"Workflow","title":"Debugging a Tier-2 Call","text":"Generated MLIR thunks carry real DWARF pointing at the dialect source RepliBuild wrote to <project>/.debug/mlir/, so a thunk is debuggable at source level — gdb stops inside the emitted MLIR by file and line, and disassemble /s interleaves dialect ops with the machine code they became. There is also a static path that needs no live process:\n\nD = RepliBuild.Debug\nD.thunks(\"test/vi_test\")                            # what you can ask about\nD.walk(\"test/vi_test\", \"_ZNK5VBase3tagEv_thunk\")    # MLIR + emitted asm, one call\n\nBoth are covered in ABI Marshalling as Compiler IR, including the two gdb flags that are mandatory and fail silently without explanation.","category":"section"},{"location":"guide/#Running-Tests","page":"Workflow","title":"Running Tests","text":"The CI suite (no C++ toolchain required):\n\njulia --project=. test/runtests.jl\n\nThe full developer integration suite (requires the C++ bucket: system LLVM/MLIR + Clang + CMake):\n\njulia --project=. test/devtests.jl\n\nSections cover the real-world fixtures (Lua, SQLite, cJSON, Duktape, pugixml), the dialect suites (test_mlir_templates.jl, test_jlcs_invariants.jl, test_jlcs_producers.jl, test_struct_abi.jl), inheritance verification (test/mi_test/, test/vi_test/), and the ABI trace fixtures. External sources are downloaded on first run.","category":"section"},{"location":"api/#API-Reference","page":"API Reference","title":"API Reference","text":"The public API is small: a build lifecycle (discover → build → wrap), a local registry with a community Hub (register / use / search), an experimental ingest path for pre-built binaries, and an environment check. Everything else — the compiler, wrapper generators, DWARF parser, MLIR/JLCS dialect bindings — is internal and documented under Internals.\n\nEvery entry point takes a path to a replibuild.toml (or a project directory that contains one) and is idempotent: unchanged inputs hit the content-hash cache and return immediately.","category":"section"},{"location":"api/#Build-lifecycle","page":"API Reference","title":"Build lifecycle","text":"The four-stage pipeline. discover writes a replibuild.toml, build compiles source to a .so + DWARF metadata, wrap emits the Julia module, clean removes generated artifacts. info reports status without touching anything.\n\ntoml = RepliBuild.discover(\"path/to/project\")   # scan → replibuild.toml\nRepliBuild.build(toml)                           # clang → LLVM IR → .so + compilation_metadata.json\nRepliBuild.wrap(toml)                            # DWARF + symbols → julia/<Module>.jl\n\ndiscover accepts build=true, wrap=true to run the whole pipeline in one call. Re-running discover(force=true) regenerates the config but preserves the hand-curated keys that cannot be derived from source — [types].templates / template_headers, [wrap].varargs / macros / shim_headers / cstring_owned / tier1, and [link].promote_statics — so a forced re-scan never eats your intent. Everything else is regenerated from the scan. See what discovery cannot know for the information you are expected to supply by hand. build(clean=true) forces a full rebuild past the cache.","category":"section"},{"location":"api/#Package-registry-and-the-Hub","page":"API Reference","title":"Package registry and the Hub","text":"register records a project in the local registry (~/.replibuild/registry/). use is the one-call \"give me a loaded module\": it resolves dependencies, builds (or serves the cached build from ~/.replibuild/builds/<hash>/), wraps, and returns the loaded Julia module. On a local-registry miss, use/search fall through to the RepliBuild-Hub community registry.\n\nLua = RepliBuild.use(\"lua\")          # build + wrap + load (cached), Hub on miss\nLua.luaL_newstate()\n\nRepliBuild.search(\"xml\")             # query the Hub by name/description/tags/language\nRepliBuild.list_registry()           # what's registered locally\n\nThe build-cache key includes RepliBuild's own version and git revision, so upgrading the generator rebuilds each package once with current codegen instead of serving a stale wrapper. scaffold_package turns a registered project into a distributable, Pkg.add-able Julia package. Two environment overrides: REPLIBUILD_HOME relocates the registry, REPLIBUILD_HUB_URL points Hub operations at a private mirror.","category":"section"},{"location":"api/#Ingest-(experimental,-C-only)","page":"API Reference","title":"Ingest (experimental, C only)","text":"For C libraries whose build systems RepliBuild's source pipeline can't reproduce (autotools, CMake code generators, configure scripts), build the .so yourself with -g and ingest it — RepliBuild skips compilation and runs only DWARF extraction + wrapper generation. Ingested libraries dispatch through Tier 3 (ccall) exclusively; the C++ API surface of an ingested binary is not supported (classes/methods/templates/virtual dispatch need the thunks only the source build produces — at best the extern \"C\" surface works). Prefer the source build; reach for ingest only when you must.\n\ntoml = RepliBuild.ingest(\"/path/to/libfoo.so\",\n                         headers=[\"/path/to/include\"],\n                         name=\"foo\", language=:c,\n                         build=true, wrap=true)","category":"section"},{"location":"api/#Environment","page":"API Reference","title":"Environment","text":"check_environment validates both toolchain buckets — the C bucket (JLL clang + Julia's resident libLLVM, no external install) and the C++/Tier-2 bucket (system LLVM/MLIR 21+, Clang, mlir-tblgen, CMake, and libJLCS.so) — and reports which dispatch tiers are available, with OS-specific install hints for anything missing.","category":"section"},{"location":"api/#What-a-generated-wrapper-exposes-about-itself","page":"API Reference","title":"What a generated wrapper exposes about itself","text":"Beyond the wrapped API, every generated module carries facts about its own generation. None of these are exported — reach for them qualified:\n\nName What it answers\ndispatch_tier(f) Which tier f actually dispatches through now: :tier1 / :tier2 / :tier3, :unknown (not wrapped), :mixed (methods on several tiers), :deferred (asked during precompilation — ask at runtime)\nDISPATCH_TIER Symbol => Symbol — the tier the generator emitted for each function\nstruct_size(name) Byte size of a wrapped struct, from DWARF\nmember_offset(name, member) Byte offset of a member within it\nSTRUCT_SIZES / STRUCT_OFFSETS The tables behind those two\nTIER1_FUNCTIONS / TIER1_DECLARES C only: functions on a bitcode slice, and the symbols each slice declares\nBUILD_ID / BUILD_TARGET / BUILD_GENERATOR Identity of the library and generator the wrapper came from\n\ndispatch_tier and DISPATCH_TIER disagree exactly when a Tier-1 kernel demotes (missing slices/, unresolvable declare); the function reports what will actually run, the table what was intended. dispatch_tier forces the kernel to generate, so it is not read-only and refuses to answer during precompilation.","category":"section"},{"location":"api/#Debugging-generated-thunks","page":"API Reference","title":"Debugging generated thunks","text":"RepliBuild.Debug inspects what the Tier-2 pipeline emitted for a package — including one this process never built — without running anything:\n\nD = RepliBuild.Debug\nD.thunks(pkg)                       # thunk symbols available to ask about\nD.mlir_body(pkg, symbol)            # the generated dialect for one thunk\nD.disassemble(pkg; symbol = s)      # objdump -dS: ops interleaved with machine code\nD.dwarf(pkg; section = \"line\")      # the address → MLIR-line table\nD.walk(pkg, symbol)                 # the common combination, one call\n\nThe disassembly path needs ENV[\"REPLIBUILD_JIT_OBJDUMP\"] = \"1\" set before the wrapper loads. The live-process counterpart — breaking inside the emitted MLIR under gdb — is documented in Debugging a thunk.","category":"section"},{"location":"api/#RepliBuild.discover","page":"API Reference","title":"RepliBuild.discover","text":"discover(target_dir=\".\"; force=false, build=false, wrap=false) -> String\n\nScan C++ project and generate replibuild.toml configuration file.\n\nThis is the entry point for new projects. Run this first to set up RepliBuild.\n\nArguments\n\ntarget_dir: Project directory to scan (default: current directory)\nforce: Force rediscovery even if replibuild.toml exists (default: false)\nbuild: Automatically run build() after discovery (default: false)\nwrap: Automatically run wrap() after build (requires build=true, default: false)\n\nReturns\n\nPath to generated replibuild.toml file\n\nWorkflow\n\nBasic workflow (step-by-step):\n\n# 1. Discover and create config\ntoml_path = RepliBuild.discover()\n\n# 2. Build the library\nRepliBuild.build(toml_path)\n\n# 3. Generate Julia wrappers\nRepliBuild.wrap(toml_path)\n\nChained workflow (automated):\n\n# Discover → Build → Wrap (all at once)\ntoml_path = RepliBuild.discover(build=true, wrap=true)\n\n# Or just discover and build\ntoml_path = RepliBuild.discover(build=true)\n\nExamples\n\n# Discover current directory\nRepliBuild.discover()\n\n# Discover another directory\nRepliBuild.discover(\"path/to/cpp/project\")\n\n# Force regenerate config\nRepliBuild.discover(force=true)\n\n# Full automated pipeline\nRepliBuild.discover(build=true, wrap=true)\n\n\n\n\n\n","category":"function"},{"location":"api/#RepliBuild.build","page":"API Reference","title":"RepliBuild.build","text":"build(toml_path=\"replibuild.toml\"; clean=false)\n\nCompile C++ project → library (.so/.dylib/.dll)\n\nWhat it does:\n\nCompiles your C++ code to LLVM IR\nLinks and optimizes IR\nGenerates library file\nExtracts metadata (DWARF + symbols) for wrapping\n\nWhat it does NOT do:\n\nDoes NOT generate Julia wrappers (use wrap() for that)\n\nArguments\n\ntoml_path: Path to replibuild.toml configuration file (default: \"replibuild.toml\")\nclean: Clean before building (default: false)\n\nReturns\n\nLibrary path (String)\n\nExamples\n\n# Build using replibuild.toml in current directory\nRepliBuild.build()\n\n# Build with specific config file\nRepliBuild.build(\"path/to/replibuild.toml\")\n\n# Clean build\nRepliBuild.build(clean=true)\n\n# Then generate Julia wrappers:\nRepliBuild.wrap(\"replibuild.toml\")\n\n\n\n\n\n","category":"function"},{"location":"api/#RepliBuild.wrap","page":"API Reference","title":"RepliBuild.wrap","text":"wrap(toml_path=\"replibuild.toml\"; headers=String[])\n\nGenerate Julia wrapper from compiled library\n\nWhat it does:\n\nLoads metadata from build (DWARF + symbols)\nGenerates Julia module with ccall wrappers\nCreates type definitions from C++ structs\nSaves to julia/ directory\n\nRequirements:\n\nMust run build() first\nMetadata must exist in julia/compilation_metadata.json\n\nArguments\n\ntoml_path: Path to replibuild.toml configuration file (default: \"replibuild.toml\")\nheaders: C++ headers for advanced wrapping (optional)\n\nReturns\n\nPath to generated Julia wrapper file\n\nExamples\n\n# Generate wrapper using replibuild.toml in current directory\nRepliBuild.wrap()\n\n# Generate wrapper with specific config file\nRepliBuild.wrap(\"path/to/replibuild.toml\")\n\n# With headers for better type info\nRepliBuild.wrap(\"replibuild.toml\", headers=[\"mylib.h\"])\n\n\n\n\n\n","category":"function"},{"location":"api/#RepliBuild.clean","page":"API Reference","title":"RepliBuild.clean","text":"clean(toml_path=\"replibuild.toml\")\n\nRemove build artifacts (build/, julia/, caches)\n\nArguments\n\ntoml_path: Path to replibuild.toml configuration file (default: \"replibuild.toml\")\n\nExamples\n\n# Clean using replibuild.toml in current directory\nRepliBuild.clean()\n\n# Clean specific project\nRepliBuild.clean(\"path/to/replibuild.toml\")\n\n\n\n\n\n","category":"function"},{"location":"api/#RepliBuild.info","page":"API Reference","title":"RepliBuild.info","text":"info(toml_path=\"replibuild.toml\")\n\nShow project status (config, library, wrapper)\n\nArguments\n\ntoml_path: Path to replibuild.toml configuration file (default: \"replibuild.toml\")\n\nExamples\n\n# Show info for current directory\nRepliBuild.info()\n\n# Show info for specific project\nRepliBuild.info(\"path/to/replibuild.toml\")\n\n\n\n\n\n","category":"function"},{"location":"api/#RepliBuild.register","page":"API Reference","title":"RepliBuild.register","text":"register(toml_path::String; name=\"\", verified=false) -> RegistryEntry\n\nHash and store a replibuild.toml in the global registry (~/.replibuild/registry/). Name is inferred from [project].name if not provided. Called automatically by discover().\n\n\n\n\n\n","category":"function"},{"location":"api/#RepliBuild.use","page":"API Reference","title":"RepliBuild.use","text":"use(name::String; force_rebuild=false, verbose=true) -> Module\n\nLoad a wrapper by registry name. Resolves dependencies, checks environment, builds if needed, and returns the loaded Julia module.\n\nExample\n\nLua = RepliBuild.use(\"lua\")\nLua.luaL_newstate()\n\n\n\n\n\n","category":"function"},{"location":"api/#RepliBuild.search","page":"API Reference","title":"RepliBuild.search","text":"search(query::String=\"\")\n\nSearch the RepliBuild Hub for available packages. Matches against names, descriptions, tags, and language. Call with no arguments to list everything.\n\nRepliBuild.search()           # list all hub packages\nRepliBuild.search(\"json\")     # filter by keyword\n\n\n\n\n\n","category":"function"},{"location":"api/#RepliBuild.list_registry","page":"API Reference","title":"RepliBuild.list_registry","text":"list_registry()\n\nPrint all registered packages in the global RepliBuild registry.\n\n\n\n\n\n","category":"function"},{"location":"api/#RepliBuild.unregister","page":"API Reference","title":"RepliBuild.unregister","text":"unregister(name::String)\n\nRemove a package from the global registry.\n\n\n\n\n\n","category":"function"},{"location":"api/#RepliBuild.scaffold_package","page":"API Reference","title":"RepliBuild.scaffold_package","text":"scaffold_package(name::String; path::String=\".\") -> String\n\nGenerate a standardized Julia package for distributing RepliBuild wrappers.\n\nCreates a complete package with Project.toml, replibuild.toml, source stub, deps/build.jl hook, and test skeleton. Edit the replibuild.toml to point at your C/C++ source, then Pkg.build() compiles and wraps automatically.\n\nExample\n\nRepliBuild.scaffold_package(\"MyEigenWrapper\")\n\n\n\n\n\n","category":"function"},{"location":"api/#RepliBuild.ingest","page":"API Reference","title":"RepliBuild.ingest","text":"ingest(library_path; headers=String[], extra_link_libs=String[],\n                     name=\"\", project_dir=\".\", language=:c, build=false, wrap=false) -> String\n\nEXPERIMENTAL. Scaffold a replibuild.toml for ingest mode: wrap a pre-built .so (built by upstream's own build system) without recompiling. RepliBuild only runs DWARF metadata extraction + wrapper generation; the library must be built with -g.\n\nSupport matrix — the maintained, flagship path is the source-build pipeline (discover/build/wrap), where RepliBuild's own version-matched compilation guarantees the DWARF it consumes:\n\nlanguage = :c — works for plain-C ABIs (Tier-3 ccall only, no bitcode/thunks), but is best-effort: upstream's compiler and debug-info settings are outside RepliBuild's control, so extraction quality varies. Prefer the source build when the sources compile under one flag set.\nlanguage = :cpp — NOT supported. The C++ ABI surface (classes, methods, templates, virtual dispatch) requires the MLIR dialect to marshal calls and generate thunks, which only the source-build pipeline produces. Ingesting a C++ library can at best expose its extern \"C\" surface; the generated wrapper for the C++ API proper is unusable. If the library ships a C API variant, ingest that with language = :c instead.\n\nThis is the fallback for C libraries with elaborate build systems (autotools, CMake with code generators, configure scripts) that RepliBuild's source-build pipeline can't reproduce.\n\nArguments\n\nlibrary_path: path to the pre-built .so / .dylib / .dll\nheaders: header search dirs for type extraction (recommended)\nextra_link_libs: additional -l libraries the wrapper needs at load time\nname: project name (default: derived from library basename)\nproject_dir: where to write replibuild.toml (default: cwd)\nlanguage: :c or :cpp — drives wrapper generator selection\nbuild: also run build() after scaffolding\nwrap: also run wrap() (requires build=true)\n\nReturns\n\nPath to the generated replibuild.toml.\n\nExample\n\ntoml = RepliBuild.ingest(\"/usr/lib/libsqlite3.so\",\n                         headers=[\"/usr/include\"],\n                         name=\"sqlite_ingest\",\n                         build=true, wrap=true)\n\n\n\n\n\n","category":"function"},{"location":"api/#RepliBuild.check_environment","page":"API Reference","title":"RepliBuild.check_environment","text":"check_environment(; verbose=true, throw_on_error=false) -> ToolchainStatus\n\nRun environment diagnostics to verify LLVM 21+, MLIR, CMake, and other toolchain requirements.\n\nPrints a colorful report showing which tools are found, their versions, and installation instructions for anything missing. Use throw_on_error=true to abort on missing requirements.\n\nExample\n\nstatus = RepliBuild.check_environment()\nstatus.ready          # true if Tier 1 (ccall) builds will work\nstatus.tier2_ready    # true if MLIR JIT tier is also available\n\n\n\n\n\n","category":"function"},{"location":"mlir/#ABI-Marshalling-as-Compiler-IR-—-the-JLCS-Dialect","page":"ABI Marshalling as Compiler IR (MLIR/JLCS)","title":"ABI Marshalling as Compiler IR — the JLCS Dialect","text":"This is the architecture page. Every other page in this manual describes what RepliBuild does; this one describes the mechanism that makes the hard half of it possible, and argues for why it is built the way it is.\n\nThe short version: a foreign call is a compilation problem, so RepliBuild compiles it. Where a wrapper generator would emit a C shim or interpret a signature at runtime, RepliBuild emits a small program in a purpose-built MLIR dialect (JLCS), lowers it to LLVM IR, and executes the result. The marshalling code for a function is derived from the same DWARF that describes its types, type-checked by MLIR, lowered by a pass that models the platform ABI explicitly, and — because it is real code with real debug info — steppable in gdb.\n\n","category":"section"},{"location":"mlir/#1.-The-problem","page":"ABI Marshalling as Compiler IR (MLIR/JLCS)","title":"1. The problem","text":"A foreign function call is not one operation. It is a small pile of decisions the compiler normally makes for you, invisibly, at the call site:\n\nWhich arguments go in registers, which on the stack, and in what order.\nWhether a struct is passed as bytes in registers, as a hidden caller-owned stack copy, or by reference.\nWhether the return value comes back in RAX/XMM0, or through a hidden pointer the caller supplies (sret).\nWhether the C++ object's this pointer needs adjusting before the callee sees it, and whether the callee is even known statically (virtual dispatch).\nWhat happens to the Julia stack frame if the callee throws.\n\nccall makes exactly one of those decisions available to you — the argument type tuple — and derives the rest from Julia's own view of the types. That view is correct whenever Julia's idea of a type matches the C compiler's. The whole category of cases RepliBuild exists for is the category where it does not:\n\nCase Why ccall cannot express it\nPacked / attribute-aligned structs Julia lays the struct out its own way; the offsets diverge silently\nMEMORY-class (>16 B) by-value struct arguments SysV wants a caller-owned stack copy; Julia has no byval spelling\nLarge by-value struct returns Needs an sret pointer with ABI-correct alignment for a layout Julia may not model\nUnions crossing by value Register class depends on all overlapping fields, not on the arm you picked\nC++ virtual methods The callee address is in the object's vtable, not in the symbol table\nC++ exceptions An escaping exception unwinds through a Julia frame that has no landing pad\nNon-trivial by-value class parameters Itanium requires a copy-constructed temporary, destructed after the call\n\nTwo conventional answers exist, and both were rejected on purpose.\n\nWrite a C shim. Generate extern \"C\" glue, compile it, ccall that. This works, and it is what most binding generators fall back to. What it costs is the thing RepliBuild is built to avoid: the shim is source text in a second language, so its correctness is only checkable by compiling it, its knowledge of layout comes from headers rather than from the binary, and every library-specific quirk grows a new hand-written shim that someone has to maintain. The failure mode is a shim that compiles cleanly and marshals wrongly.\n\nInterpret the signature at runtime. libffi builds the call frame from a description, per call. That is a genuinely general answer, but it is an interpreter in the hot path, it models the ABI in a closed C library you cannot extend for C++ specifics (vtables, RAII scopes, landing pads), and there is nothing to inspect afterwards — a wrong call is a wrong call, with no artifact between the description and the crash.","category":"section"},{"location":"mlir/#2.-The-claim","page":"ABI Marshalling as Compiler IR (MLIR/JLCS)","title":"2. The claim","text":"Marshalling should be compiled code generated from the compiler's own record of the library, expressed in an IR whose types can be checked and whose lowering can be read.\n\nMLIR's dialect ecosystem is almost entirely about compute lowering — tensors, loops, hardware targets. JLCS uses the same machinery for the interop boundary, which as far as this project is aware nobody else does. Four properties fall out of that choice, and they are the argument for it:\n\nOne source of truth. The struct offsets in !jlcs.c_struct and the offsets the wrapper generator uses for Julia field access are the same DWARF numbers, read once. A shim-based design has two derivations that can drift.\nType-checked marshalling. Ops carry verifiers, the type converter refuses what it cannot translate, and a malformed thunk fails at parse — before anything executes. Several ops gained verifiers specifically because a malformed hand-written body used to reach the lowering and segfault it.\nThe ABI is a pass, not a convention. classifySysVStruct is ~40 lines that state the x86-64 SysV rules explicitly. When the rule was wrong (see §10) the fix was one function, applied to every call site at once, with a fixture that pins it against a real clang++-compiled callee.\nIt is inspectable. The generated dialect is written to disk, the JIT registers DWARF pointing at it, and gdb stops inside the emitted MLIR by file and line. Every ABI claim on this page is checkable with a breakpoint rather than by reasoning (§9).\n\nThe cost is honest and worth stating: Tier 2 needs a system LLVM/MLIR install and a compiled dialect (libJLCS.so), which is the single largest dependency in the project. C-only projects never touch it.","category":"section"},{"location":"mlir/#3.-Where-thunks-sit","page":"ABI Marshalling as Compiler IR (MLIR/JLCS)","title":"3. Where thunks sit","text":"compilation_metadata.json  +  DWARF vtables          (Builder/)\n                    │\n                    ▼\n        JLCSIRGenerator.generate_jlcs_ir            Julia emits MLIR *text*\n        ├─ StructGen      → type aliases, DWARF-offset struct bodies\n        ├─ FunctionGen    → one `func.func @<mangled>_thunk` per function\n        ├─ ArrayViewGen   → strided accessors for fixed-size array members\n        └─ STLContainerGen→ container accessor thunks\n                    │  MLIR source text\n                    ▼\n        jlcsModuleCreateParse(ctx, text, \"<pkg>/.debug/mlir/jlcs_<hash>.mlir\")\n                    │  every op stamped with a FileLineColLoc naming that buffer\n                    ▼\n        jlcs_lower_to_llvm  ── jlcs-lower-to-llvm\n                            ── convert-func-to-llvm\n                            ── convert-arith-to-llvm\n                            ── reconcile-unrealized-casts\n                            ── DIScopeForLLVMFuncOp   (line-table DWARF)\n                    │\n        ┌───────────┴────────────┐\n        ▼                        ▼\n  ORC JIT engine           emit_object → clang -shared\n  (one per library)        → lib<name>_thunks.so        [aot_thunks = true]\n        │                        │\n        ▼                        ▼\n  JITManager.invoke(       direct ccall into the\n   \"_mlir_ciface_…\")       companion library\n\nThe generator is Julia code that produces MLIR as text, which is then parsed. That is a deliberate choice with two consequences worth knowing: it is why no producer has ever needed the dialect's C++ build() methods (which is how ten of them shipped undefined for years — §10), and it is why the parse buffer can be a real file on disk, which is what makes the debugger work.","category":"section"},{"location":"mlir/#4.-The-thunk-contract","page":"ABI Marshalling as Compiler IR (MLIR/JLCS)","title":"4. The thunk contract","text":"Every Tier-2 function is reached through one C signature, regardless of what it actually takes:\n\nReturn Emitted _mlir_ciface_* signature\nScalar / pointer T ciface(void** args)\nAny aggregate void ciface(T* sret, void** args)\nVoid void ciface(void** args)\n\nThe two sides agree on which is which without negotiating: emit_c_interface routes every struct-typed result through a result pointer, and JITManager._invoke_call picks the sret shape for every non-isprimitivetype Julia return type. (Note this is the ciface boundary, not the SysV classification of §7 — the thunk's own call into the library is classified separately.)\n\nThat uniformity is forced by the Julia side. ccall needs its signature at macro expansion time, so a runtime-general dispatcher cannot build one; a single void** entry point can be called for any arity, and JITManager.invoke is @generated, so each concrete argument tuple still specializes into allocation-free code. llvm.emit_c_interface on the func.func is what makes MLIR emit the _mlir_ciface_ wrapper implementing exactly this.","category":"section"},{"location":"mlir/#The-arg-slot-convention","page":"ABI Marshalling as Compiler IR (MLIR/JLCS)","title":"The arg-slot convention","text":"A slot holds a pointer to the argument's storage, not the argument. The thunk therefore double-loads:\n\n%arg_ptr_1 = llvm.getelementptr %args_ptr[%idx_1] : (!llvm.ptr, i64) -> !llvm.ptr, !llvm.ptr\n%val_ptr_1 = llvm.load %arg_ptr_1 : !llvm.ptr -> !llvm.ptr   // slot → storage address\n%val_1     = llvm.load %val_ptr_1 : !llvm.ptr -> !llvm.ptr   // storage → value\n\nRef(x) on the Julia side produces exactly that shape for any isbits x. Two argument kinds are already an indirection and must be flattened to a raw pointer first, or the double-load spends one level too many and the callee receives the payload's own bytes as an address:\n\nan AbstractString — Ref(::String) points at the String object, not at its bytes;\na Base.Ref that is not already a Ptr — the spelling a caller reaches for when a parameter is annotated ::Ref{T}, which is what a C++ T const& generates. Ptr{T} <: Ref{T}, so the annotation accepts a RefValue silently.\n\nBoth are handled by _arg_marshal_plan (one plan shared by both invoke methods, because two copies is how a fix to one silently misses the other), and the original is GC-preserved across the call since only a raw pointer into it reaches the slot.\n\nCallers packing a by-value struct argument put &struct in the slot directly — not &Ref — because the struct's storage is what the callee wants.","category":"section"},{"location":"mlir/#Worked-example","page":"ABI Marshalling as Compiler IR (MLIR/JLCS)","title":"Worked example","text":"A virtual method on a secondary base, from test/mi_test/. Julia side, as emitted by the wrapper generator:\n\nfunction Base2_get_b(this::Any)\n    # [Tier 2] Dispatch to MLIR JIT (Complex ABI / Packed / Union)\n    return RepliBuild.JITManager.invoke(\"_mlir_ciface__ZNK5Base25get_bEv_thunk\", Int32, this)\nend\n\nThe thunk that name resolves to, as generated and as written to test/mi_test/.debug/mlir/jlcs_<hash>.mlir:\n\nfunc.func private @_ZNK5Base25get_bEv(!llvm.ptr) -> i32\nfunc.func @_ZNK5Base25get_bEv_thunk(%args_ptr: !llvm.ptr) -> i32\n    attributes { llvm.emit_c_interface } {\n  %idx_1     = arith.constant 0 : i64\n  %arg_ptr_1 = llvm.getelementptr %args_ptr[%idx_1] : (!llvm.ptr, i64) -> !llvm.ptr, !llvm.ptr\n  %val_ptr_1 = llvm.load %arg_ptr_1 : !llvm.ptr -> !llvm.ptr\n  %val_1     = llvm.load %val_ptr_1 : !llvm.ptr -> !llvm.ptr\n  %ret_val   = \"jlcs.vcall\"(%val_1)\n      { class_name = @Base2, vtable_offset = 0 : i64, slot = 2 : i64,\n        this_offset = 0 : i64, may_throw } : (!llvm.ptr) -> i32\n  return %ret_val : i32\n}\n\nand what that lowers to, read out of gdb's disassemble /s at the breakpoint:\n\n%val_ptr_1 = llvm.load %arg_ptr_1     →  mov (%rdi),%rax     slot → storage address\n%val_1     = llvm.load %val_ptr_1     →  mov (%rax),%rdi     storage → `this`\njlcs.vcall … { slot = 2 }             →  mov 0x10(%rax),%rax 2 × 8 = 0x10\n\nThe slot arithmetic is checkable by eye. That is the point of §9.","category":"section"},{"location":"mlir/#5.-Types","page":"ABI Marshalling as Compiler IR (MLIR/JLCS)","title":"5. Types","text":"The dialect defines two types. Both exist because LLVM's type system cannot say the thing the ABI needs said.","category":"section"},{"location":"mlir/#!jlcs.c_struct","page":"ABI Marshalling as Compiler IR (MLIR/JLCS)","title":"!jlcs.c_struct","text":"!jlcs.c_struct<\"Vec3\", [f32, f32, f32], [[0 : i64, 4 : i64, 8 : i64]], packed = false>\n\nA C-ABI struct carrying its Julia type name, its flattened field types, the explicit byte offset of every field, and a packed flag. An !llvm.struct implies offsets from its element list; !jlcs.c_struct states them, which is what lets the marshalling ops move a field from where Julia put it to where C expects it without either side guessing. It converts to LLVM::LLVMStructType::getLiteral(fields, isPacked) during lowering.\n\nwarning: A `c_struct` must never appear inside an `!llvm.struct` body\nThe type converter treats !llvm.struct as legal and never rewrites its body — signature positions get rebuilt, body-op types do not. A nested c_struct alias therefore survives lowering and segfaults translateModuleToLLVMIR inside PtrLikeTypeInterface::getMemorySpace. StructGen inlines the byte-identical !llvm.struct<packed (…)> literal for c_struct-classified members instead, and jlcs_create_jit pre-flights every type in the module with mlir::LLVM::isCompatibleType, returning null (catchable in Julia → \"Tier 2 disabled for this library\") rather than crashing the process on anything foreign that still slips through.","category":"section"},{"location":"mlir/#!jlcs.array_view","page":"ABI Marshalling as Compiler IR (MLIR/JLCS)","title":"!jlcs.array_view","text":"!jlcs.array_view<f64, 3>\n\nA strided array descriptor — element type and rank — whose runtime layout is\n\nstruct ArrayView { T* data; int64_t* dims; int64_t* strides; int64_t rank; };\n//                 @0       @8             @16               @24\n\nchosen to be simultaneously expressible as a Julia Array{T,N} view, a NumPy ndarray, a C++ strided span, and a Rust slice. The producer (ir_gen/ArrayViewGen.jl) materializes one in place over a fixed-size array member — the data pointer aims into the caller's struct, so element access is zero-copy. Rank 1 today; the descriptor already carries dims and rank for the generalization, and the lowering reads data and strides only.","category":"section"},{"location":"mlir/#6.-The-ops","page":"ABI Marshalling as Compiler IR (MLIR/JLCS)","title":"6. The ops","text":"Fourteen ops, all fourteen with a producer as of 2026-08-13. Liveness is tracked deliberately, because \"defined in TableGen\" and \"something emits it\" are different claims — and it is checked mechanically rather than asserted here, by test_jlcs_invariants.jl §E, which reads the mnemonics out of JLCSOps.td, greps src/ for emission, and fails naming any op whose tier moved. That guard exists because this table was wrong for months (see below).\n\nOp Producer Verifier Role\njlcs.type_info JLCSIRGenerator ✅ class metadata: layout, primary base, destructor, MI/VI base tables\njlcs.ffe_call FunctionGen — direct call to a named symbol, SysV-coerced\njlcs.try_call FunctionGen — as ffe_call, plus invoke + landing pad\njlcs.vcall FunctionGen ✅ vtable dispatch: read vptr, index slot, adjust this, indirect call\njlcs.marshal_arg FunctionGen ✅ Julia-aligned struct pointer → C-packed struct value\njlcs.marshal_ret FunctionGen — C-packed struct value → Julia-aligned struct value\njlcs.scope FunctionGen ✅ RAII region; destructors fire in reverse at exit\njlcs.ctor_call FunctionGen — constructor on an object pointer\njlcs.dtor_call FunctionGen ✅ destructor on an object pointer (exactly one operand)\njlcs.yield FunctionGen — jlcs.scope terminator\njlcs.load_array_element ArrayViewGen — strided read through an !jlcs.array_view\njlcs.store_array_element ArrayViewGen — strided write\njlcs.get_field FunctionGen, ArrayViewGen ✅ field read by byte offset\njlcs.set_field ArrayViewGen ✅ field write by byte offset","category":"section"},{"location":"mlir/#How-the-last-three-got-producers","page":"ABI Marshalling as Compiler IR (MLIR/JLCS)","title":"How the last three got producers","text":"Until 2026-08-13 the bottom three rows read none, and one of them read none without saying so. jlcs.dtor_call was credited to FunctionGen here and in the internal devlog for months while nothing emitted it: the scope-RAII producer builds jlcs.scope(...) dtors([@sym, ...]), and it is the scope's lowering that emits those calls (as LLVM::CallOp, directly), so DestructorCallOp had a registered and unreachable conversion pattern. The pairing with ctor_call — which does have a producer — is what made the gap read as symmetry.\n\ndtor_call — a destructor thunk is exactly this op's shape: one object pointer in, void out. It used to go out as a generic ffe_call/try_call with the full SysV coercion machinery attached to a signature that needs none. The op gained a may_throw unit attribute (mirroring vcall's) with an invoke + landing-pad lowering, so a thunk moving onto it keeps the exact EH semantics it had — DWARF marks no destructor noexcept, so in practice C++ destructor thunks take the EH path.\nThe arity gate is load-bearing: under Itanium a base-object destructor (D2) of a class with virtual bases takes a second VTT argument, and this op has no operand to carry it. vi_test has three (_ZN4LeftD2Ev(this, vtt)). Those keep the ffe_call/try_call path; converting them would drop the VTT silently. The gate reads the final argument list, after this synthesis, not the DWARF parameter list.\nget_field / set_field — the thunk argument array is a record with a fixed layout: the ciface convention hands the thunk a void**, so reading argument slot i is a field read at byte offset 8i. That is what both producers now say, in one op, instead of a constant plus a pointer-scaled GEP plus a load. ArrayViewGen additionally builds the !jlcs.array_view descriptor with one set_field per field (data@0, dims@8, strides@16, rank@24) — the same terms the array-op lowering reads it back in, which previously was a GEP chain on the producer side and byte offsets on the consumer side with nothing tying them together.\n\nThis changes what the IR says, not what it does, and that is checked rather than asserted: every function of mi_test, vi_test and stl_test (188 symbols) compiles to byte-identical machine code across the switch, and test_jlcs_producers.jl §G pins the per-shape equivalence directly. The motivation is that the .mlir is the debugger's source file (see Debugging below) — jlcs.get_field ... {fieldOffset = 8} is what gdb shows at the breakpoint, and the argument-slot convention it encodes is the one this project's notes record as having cost a debugging session.","category":"section"},{"location":"mlir/#Metadata:-type_info","page":"ABI Marshalling as Compiler IR (MLIR/JLCS)","title":"Metadata: type_info","text":"jlcs.type_info \"Derived\",\n  !jlcs.c_struct<\"Derived\", [i32], [[28 : i64]], packed = false>,\n  \"Base1\", \"_ZN7DerivedD0Ev\"\n  {baseNames = [\"Base1\", \"Base2\"], baseOffsets = [0 : i64, 16 : i64]}\n\nErased at lowering — it exists so the layout facts travel with the IR that uses them. superType is the primary base (single-inheritance consumers). baseNames/baseOffsets pair 1:1 and carry each non-virtual base subobject's static offset. vbaseNames/vbaseVtableOffsets carry virtual bases, keyed on the negative byte position of the vbase-offset entry relative to the vtable address point — a virtual base has no static offset, so it can only be named by where to read the offset. destructorName is the DWARF-resolved complete-object destructor, which is what Managed finalizers and the RAII scope call. The verifier enforces the 1:1 pairing, because consumers index one array by the other's position.","category":"section"},{"location":"mlir/#Calls:-ffe_call,-try_call,-vcall","page":"ABI Marshalling as Compiler IR (MLIR/JLCS)","title":"Calls: ffe_call, try_call, vcall","text":"ffe_call is the direct call. try_call is the same call marshalled identically and terminated differently — llvm.invoke with a landing pad, where a caught C++ exception is recorded through jlcs_catch_current_exception() and the call yields a zero sentinel rather than unwinding. The Julia side polls jlcs_has_pending_exception() after every Tier-2 call and raises CxxException with the original what() string. Sentinel-continue rather than unwind-through-Julia is the whole reason a C++ exception is survivable here.\n\nWhich one a function gets is per-function: the module-level may_throw (set for C++ builds) is overridden by the function's own DWARF noexcept flag.\n\nvcall is dispatch through the object's vtable, so overrides are honored — a direct symbol call is p->Class::method() static semantics, which is the wrong answer for a base-class wrapper invoked on a derived object. The op takes class-local coordinates (vtable_offset, slot, this_offset) and its producer always emits vtable_offset = this_offset = 0; see The inheritance ABI for why class-local coordinates are universally correct under Itanium. Two deliberate exclusions: destructors stay direct calls (exact-class semantics for finalizers and RAII), and struct-shaped signatures stay direct because the vcall lowering does no sret/packed coercion.","category":"section"},{"location":"mlir/#RAII:-scope,-ctor_call,-dtor_call,-yield","page":"ABI Marshalling as Compiler IR (MLIR/JLCS)","title":"RAII: scope, ctor_call, dtor_call, yield","text":"jlcs.scope(%tmp : !llvm.ptr) dtors([@_ZN4BaseD1Ev]) {\n  jlcs.ctor_call @_ZN4BaseC1ERKS_(%tmp, %src) : (!llvm.ptr, !llvm.ptr) -> ()\n  %r = jlcs.ffe_call %tmp { callee = @_Z5takesB } : (!llvm.ptr) -> i32\n  llvm.store %r, %retslot : i32, !llvm.ptr\n  jlcs.yield\n}\n\nUnder Itanium, a by-value parameter of a class with a non-trivial destructor is passed as a pointer to a caller-owned temporary, destructed by the caller after the call — not as raw bits in registers. Passing the bits miscompiles those calls. The producer detects the case by symbol presence (a truly trivial destructor is never emitted as a symbol, so a class with one in the metadata is non-trivial for the purposes of calls), copy-constructs a temporary when a copy constructor is resolvable and bit-copies when the copy is trivial, and brackets the call in a jlcs.scope. Destructors fire in reverse order at scope exit.\n\njlcs.scope has no results, so a return value leaves through a stack slot — that is why the RAII path allocates %raii_retslot. Because try_call converts an exception into sentinel-and-continue, the normal path is the only path, so destructor coverage is total.\n\nThe verifier exists because an arity mismatch between managed_ptrs and destructors used to SIGSEGV inside the lowering, which indexes one by the other's position.","category":"section"},{"location":"mlir/#Marshalling:-marshal_arg,-marshal_ret","page":"ABI Marshalling as Compiler IR (MLIR/JLCS)","title":"Marshalling: marshal_arg, marshal_ret","text":"%packed = jlcs.marshal_arg %ptr\n  { memberTypes = [i32, f64], juliaOffsets = [0 : i64, 8 : i64] }\n  : (!llvm.ptr) -> !llvm.struct<packed (i32, f64)>\n\nThe layout-mismatch pair. marshal_arg reads each member from the Julia offset and assembles the C-packed value; marshal_ret does the inverse for returns. memberTypes and juliaOffsets pair 1:1 and the verifier says so — same reason as type_info.","category":"section"},{"location":"mlir/#7.-The-lowering","page":"ABI Marshalling as Compiler IR (MLIR/JLCS)","title":"7. The lowering","text":"One pass, jlcs-lower-to-llvm, run as a partial conversion with all fourteen ops marked illegal and the LLVM + arith dialects legal. func.func is dynamically legal — a signature containing !jlcs.c_struct cannot be handled by the stock ConvertFuncToLLVM type converter, so those functions are claimed by this pass with a converter that knows the type. The rest of the pipeline is stock (convert-func-to-llvm, convert-arith-to-llvm, reconcile-unrealized-casts), plus DIScopeForLLVMFuncOp last — see §9.\n\nInter-pass verification is deliberately off for the pipeline: the JLCS pass emits llvm.call ops referencing func.func symbols that do not become llvm.func until the next pass. The final module is fully verified.","category":"section"},{"location":"mlir/#The-SysV-classifier","page":"ABI Marshalling as Compiler IR (MLIR/JLCS)","title":"The SysV classifier","text":"The heart of the whole thing is classifySysVStruct, and it is worth reading as prose because every by-value struct crossing in RepliBuild runs through it:\n\nCompute abiSize. 0 or > 16 bytes → MEMORY class, full stop.\nOtherwise flatten the struct to leaves with absolute offsets (collectAbiLeaves, recursing through nested structs and arrays).\nIf any leaf sits at an offset that is not a multiple of its natural size, the struct is genuinely misaligned (attribute-packed) → MEMORY class.\nOtherwise mark, per eightbyte, whether any overlapping leaf is integer and whether any is floating point. An eightbyte is SSE only if every overlapping leaf is FP; otherwise INTEGER.\nEmit one coercion scalar per eightbyte: f64 for SSE, i64 for INTEGER.\n\nFrom that classification the shared buildSysVCallShape derives the entire call:\n\nShape Lowering\nMEMORY-class return alloca (memorySlotAlign), pass as first argument, call returns void, load the result back\nRegister-class return call returns the coerced scalar(s); store to a slot, reload as the original struct type\nRegister-class argument store to a slot, load one scalar per eightbyte, pass them as separate arguments — exactly clang's coercion\nMEMORY-class argument alloca + store + pass the pointer with llvm.byval(T) and llvm.align\nEverything else passed through unchanged\n\nTwo details are load-bearing and were each a bug first:\n\nbyval must be stamped on the call site and the declaration. The call site is what the backend lowers; the declaration is what the verifier checks it against. retargetCallee rewrites the external declaration to the coerced signature and copies the attributes onto it.\nSlot alignment is explicit (memorySlotAlign = max(8, abiAlign)). Clang never emits an sret/byval slot below 8 on x86-64, and a struct RepliBuild failed to model field-by-field degrades to !llvm.array<N x i8>, whose natural alignment is 1 — leaving alignment implicit would hand native code an underaligned buffer for exactly the types least understood.\n\nffe_call and try_call share one derivation of all of this. They used to carry independent copies, which is precisely how a fix to one silently missed the other; that shape recurs throughout §10.","category":"section"},{"location":"mlir/#Exception-lowering","page":"ABI Marshalling as Compiler IR (MLIR/JLCS)","title":"Exception lowering","text":"try_call (and vcall with may_throw) emit llvm.invoke with a landing pad that calls __cxa_begin_catch / jlcs_catch_current_exception / __cxa_end_catch, records the message in a thread-local buffer, and continues with a zero-valued result. A post-pipeline walk stamps __gxx_personality_v0 onto any llvm.func containing an llvm.invoke — it must run after ConvertFuncToLLVM, since only LLVM::LLVMFuncOp carries a personality attribute. The C++ runtime symbols themselves are registered into the JIT by JITManager at engine setup.","category":"section"},{"location":"mlir/#8.-Struct-bodies-come-from-DWARF-offsets","page":"ABI Marshalling as Compiler IR (MLIR/JLCS)","title":"8. Struct bodies come from DWARF offsets","text":"A struct body emitted for a thunk must have the size and layout of the C type, not of anything Julia or LLVM would pick. StructGen lays members out at their DWARF offsets with explicit !llvm.array<N x i8> padding between them, and verifies the result against a Julia mirror of LLVM's own abiSize/abiAlign (_mlir_layout). One shared derivation (_apply_dwarf_layout) feeds all three body builders.\n\nA struct that cannot be laid out consistently — overlapping members, a member whose emitted type will not sit at its offset, an unmeasurable member, or members summing past byte_size — degrades to a byte_size-byte opaque region and warns. Opaque, but never the wrong size.\n\nThat rule replaced the original \"close the struct with one trailing filler of byte_size - Σ member sizes\", which double-counted: LLVM inserts interior alignment padding itself, and DWARF reports enum members with size = 0. Every non-packed struct with interior padding came out larger than the C type, and since llvm.emit_c_interface stores a MEMORY-class result straight into the caller's buffer while JITManager sizes that buffer from the Julia struct, every such call wrote past a live Julia object. Measured on llama.cpp: llama_context_params emitted 200 bytes against a native 160, overrunning a 160-byte Ref by 34. Every member offset was correct, which is why it presented as intermittent corruption rather than as a marshalling bug. Pinned by test/test_struct_layout.jl, which needs no toolchain and is negative-checked against the old rule.","category":"section"},{"location":"mlir/#9.-Debugging-a-thunk","page":"ABI Marshalling as Compiler IR (MLIR/JLCS)","title":"9. Debugging a thunk","text":"This is the default way to debug Tier 2. Reach for it before reading IR and reasoning about it.\n\nThree pieces, each added for its own reason, compose into a source-level debugger for generated marshalling code:\n\noptions.enableGDBNotificationListener = true — ORC registers every JIT'd object with gdb through __jit_debug_register_code.\nMLIR's parser stamps every op with a FileLineColLoc naming its buffer, and DIScopeForLLVMFuncOpPass turns that into the emitted DWARF's DIFile.\nThe .debug/mlir/jlcs_<hash>.mlir dump is the file those locations point at — so it is not diagnostic output, it is the debugger's source file.\n\nNothing needs to be enabled and nothing links gdb: RepliBuild is the publisher of LLVM's GDB JIT interface, and gdb reads the descriptor out of the inferior. clean() removes .debug, and it regenerates on the next JIT init.\n\ntimeout -s KILL 175 gdb -batch -nx \\\n  -ex 'set pagination off' -ex 'set confirm off' -ex 'set listsize 4' \\\n  -ex 'handle SIGSEGV nostop noprint pass' \\\n  -ex 'set breakpoint pending on' \\\n  -ex 'break _ZNK5Base15get_aEv_thunk' -ex 'run' \\\n  -ex 'bt 1' -ex 'info source' -ex 'disassemble /s' \\\n  --args julia --project=. test/mi_test/verify.jl\n\nThread 1 \"julia\" hit Breakpoint 1, _ZNK5Base15get_aEv_thunk () at jlcs_83a242c27fb37885.mlir:126\n126\t  %val_ptr_1 = llvm.load %arg_ptr_1 : !llvm.ptr -> !llvm.ptr\nProducer is MLIR.   Compiled with DWARF 4 debugging format.\n\nTwo flags are mandatory and fail silently without explanation: set breakpoint pending on (the thunk does not exist until the JIT emits it, so the breakpoint cannot resolve at load) and handle SIGSEGV nostop noprint pass (Julia's GC uses SIGSEGV for write barriers). disassemble /s is the payoff — dialect ops interleaved with the machine code they became.","category":"section"},{"location":"mlir/#The-static-path-—-RepliBuild.Debug","page":"ABI Marshalling as Compiler IR (MLIR/JLCS)","title":"The static path — RepliBuild.Debug","text":"gdb needs a live process stopped at the right moment. RepliBuild.Debug needs a file, carries the same information, cannot wedge, and works on a package this process never built. Prefer it.\n\nENV[\"REPLIBUILD_JIT_OBJDUMP\"] = \"1\"   # BEFORE the wrapper loads — see below\nD = RepliBuild.Debug\n\nD.thunks(\"test/vi_test\")                            # what you can ask about\nD.walk(\"test/vi_test\", \"_ZNK5VBase3tagEv_thunk\")    # MLIR + emitted asm, one call\nD.disassemble(pkg; symbol = s)                      # objdump -dS, ops ↔ machine code\nD.mlir_body(pkg, symbol)                            # just the dialect\nD.dwarf(pkg; section = \"line\")                      # the address → MLIR-line table\n\nThe env var is read, not passed, and that is forced: MLIR must have the object cache when the engine is created, and engines are cached per library per process, so there is no later moment at which an argument could arrive. Setting it after the first Tier-2 call is too late, and says so.\n\n.debug_info holds only a compile unit — no DW_TAG_subprogram, no variable DIEs. That is what LineTablesOnly means here, and it is why file and line are perfect in gdb while info locals is empty. It is pinned negatively in test_debug_inspection.jl, so raising the emission kind shows up as a failing test rather than as a surprise.\n\nOptional profiling: REPLIBUILD_JIT_PROFILE turns on LLVM's perf listener and routes the per-process jitdump to a session directory. Off by default — the listener writes to $HOME/.debug/jit with nothing rotating or expiring it.","category":"section"},{"location":"mlir/#10.-Failure-classes-this-architecture-has-hit","page":"ABI Marshalling as Compiler IR (MLIR/JLCS)","title":"10. Failure classes this architecture has hit","text":"Recorded because the shapes recur, and because a reader deciding how much to trust the marshalling layer deserves the real list rather than a claim of correctness.\n\nTwo generators, one guard. The Julia wrapper generator and the MLIR thunk generator derive the same decisions independently, so a gate added to one silently misses the other. Dear ImGui exposed it: is_method is a string heuristic (:: in the demangled prefix) and Itanium mangles a namespace-scoped free function identically to a member function, so the thunk generator synthesized a this for ImGui::GetVersion(). The thunk loaded args_ptr[0] and dereferenced it while Julia passed an empty array — 174 zero-arg functions segfaulting on first call, 614 more with every argument shifted one slot. Both generators now consult the aggregate table (a namespace is never a key in it), matching on every :: suffix, angle-bracket-depth aware. The same shape produced the independent ffe_call/try_call SysV copies (§7).\n\nEmitted size ≠ DWARF byte_size. §8. A live heap smasher on every MEMORY-class struct return, invisible because every member offset was right.\n\nMEMORY-class by-value arguments had two wrong shapes. Packed structs went as a bare pointer (by reference — the callee reads an address where it expects bytes); non-packed ones went as an LLVM first-class aggregate, which the backend splits per element, shifting every later argument. One llvm.byval path now. The discriminating fixture is gap_probe in test/struct_abi/, driven against a real clang++ callee — a self-JIT'd callee cannot catch a convention mismatch, because both sides share it.\n\nEnum returns must be bare integers. A struct result — even !llvm.struct<(i32)> — makes emit_c_interface use the sret convention, while the Julia side calls an @enum back as a scalar: the args pointer lands in the sret slot and the call dereferences garbage. Found on tinyxml2's XMLDocument::Parse → XMLError.\n\nA tier decided a presentation policy. The C++ generator's Tier-2 branch continues past the ccall emission path, and the char* return policy (Union{String,Nothing}, the _ptr sibling, the [wrap.cstring_owned] free) lived only on that path — so 75 functions across five Hub packages handed back a raw Cstring and silently discarded any declared deallocator. Not a marshalling bug: the pointer was correct, it just arrived undressed. The rule the fix encodes is the general one — a dispatch tier decides how a function is called, never how its result is presented — and it is enforced by _assert_cstring_policy on the wrapper write path rather than by convention.\n\nTen undefined symbols shipped in libJLCS.so for its entire life. dlopen(…, RTLD_NOW) refused the library. Eight op build() bodies and two ArrayViewType accessors were declared by TableGen (skipDefaultBuilders, genStorageClass = 0) and never defined in C++. Nothing caught it because ccall((:sym, path), …) binds lazily and every producer builds IR as text, so no call site ever existed: the library loaded, the dialect worked, and the whole Tier-2 suite was green on a binary the loader would reject if asked to resolve it eagerly. The guard is a fresh-subprocess RTLD_NOW dlopen plus an nm -D --undefined-only sweep (test_jlcs_invariants.jl §D).\n\nNested c_struct in an llvm.struct body. §5 — a whole-process load crash on pugixml, now prevented at emission and caught by the create_jit pre-flight.\n\nUnbounded arity and malformed operand segments. llvm.call's AttrSizedOperandSegments has two groups, not three; a hand-rolled OperationState for the indirect vcall wrote {1, nArgs, 0} where {1 + nArgs, 0} was required, and translation walked off the end. Use the dedicated indirect-call builder.\n\nThe through-line: every one of these was silent. That is why the invariants suite, the layout mirror, the RTLD_NOW probe, the op verifiers, and the create_jit type pre-flight all exist — the architecture's real requirement is not that marshalling be right, it is that wrong marshalling be loud.","category":"section"},{"location":"mlir/#11.-Boundaries","page":"ABI Marshalling as Compiler IR (MLIR/JLCS)","title":"11. Boundaries","text":"Deliberately unbuilt, so they are not chased as bugs:\n\nArray views are rank 1. The producer's regex skips T[N][M]; the descriptor's dims/rank fields are populated but unread by the lowering, and the user-facing Julia accessors that would call these thunks are not emitted yet.\nvcall gates on scalar/pointer signatures. Virtual methods returning a struct by value or taking a packed struct by value keep the direct-call path; closing this means giving the vcall lowering try_call's coercion.\nSeven ops have no verifier (ffe_call, try_call, load/store_array_element, ctor_call, yield, marshal_ret). No known crash paths, but hand-written IR gets no schema help. get_field, set_field and dtor_call gained theirs on 2026-08-13 with their producers — all three lowerings take a pointer operand on faith, which ODS's AnyType accepts and LLVM translation then rejects far from the mistake.\nThe classifier is x86-64 SysV only. The 16-byte MEMORY threshold, 64-bit pointers, and i64/f64 eightbytes are hardcoded. Win64 and AAPCS are not modeled.\nNo in-IR virtual-base upcast op. Virtual inheritance works through a dynamic upcast on the Julia side (The inheritance ABI), which needed zero dialect changes. An op would only be required for IR that must do the adjustment inside a thunk, and no producer needs that today.","category":"section"},{"location":"mlir/#12.-AOT-thunks","page":"ABI Marshalling as Compiler IR (MLIR/JLCS)","title":"12. AOT thunks","text":"[compile] aot_thunks = true runs the identical generator and lowering at build time, emits an object, and links it against the library into a companion lib<name>_thunks.so. The wrapper then ccalls that library instead of calling JITManager.invoke: no JIT at load, no MLIR runtime dependency at all after the build. libJLCS.so is still required to build.","category":"section"},{"location":"mlir/#13.-Building-the-dialect","page":"ABI Marshalling as Compiler IR (MLIR/JLCS)","title":"13. Building the dialect","text":"cd src/mlir && ./build.sh\n\nNeeds system LLVM/MLIR 21+ (22.1.x here), CMake 3.20+, and mlir-tblgen. The artifact is src/mlir/build/libJLCS.so, gitignored, and pinned to the installed MLIR's minor SONAME (libMLIR.so.22.1).\n\nA patch bump inside a minor needs nothing — SONAMEs are unchanged and the existing .so keeps resolving. Rebuilding anyway is cheap and was verified clean.\nA minor bump requires rm -rf build && ./build.sh and a Tier-2 re-run. Check the release notes for MLIR entries first; that decides whether the bump is a formality or real work.\nA worktree or fresh clone has no libJLCS.so (it is a build artifact), so Tier 2 fails there in a way that reads exactly like a code regression. Build it, or symlink src/mlir/build from a tree that has one.\n\n\n\nWhere the executable truth lives: test_mlir_templates.jl (dialect semantics: CStructs, RAII, vcall, sret, MI and the VI diamond), test_jlcs_invariants.jl (op arity/liveness probes, the RTLD_NOW symbol guard), test_jlcs_producers.jl (scope-RAII and array-view producers executing through the real JIT), test_struct_abi.jl (SysV small-struct and byval ABI against a real clang++ callee), test_struct_layout.jl (DWARF-offset layout, no toolchain), test_multilib_jit.jl (two wrappers, one session), test_debug_inspection.jl (the debug surfaces above), and the fixtures test/mi_test/, test/vi_test/, test/stl_test/.","category":"section"},{"location":"release-notes/#Release-Notes","page":"Release Notes","title":"Release Notes","text":"Condensed highlights of recent releases. The authoritative, fully detailed history lives in the repository CHANGELOG.","category":"section"},{"location":"release-notes/#v3.0.1-(2026-07-18)-—-the-inheritance-ABI-and-Tier-2-correctness-release","page":"Release Notes","title":"v3.0.1 (2026-07-18) — the inheritance-ABI and Tier-2-correctness release","text":"C++ inheritance, end to end.\n\nMultiple inheritance — base-class subobject offsets are extracted from DW_TAG_inheritance, derived layouts flatten base members at their true offsets, and <Derived>_as_<Base> upcast helpers apply the static Itanium adjustment so secondary-base methods receive a correct base-relative this.\nVirtual dispatch honors overrides — virtual instance methods now dispatch through jlcs.vcall: the thunk reads the object's vptr, indexes the slot, and calls indirectly, so a base-class wrapper invoked on a derived object reaches the override. Dispatch coordinates are class-local (an empirically pinned Itanium/DWARF fact); destructors deliberately remain direct calls for exact-class RAII semantics.\nVirtual inheritance — a virtual base's offset is vtable-resident and dynamic, not static. Both DWARF parsers now decode the vbase location expression, jlcs.type_info carries a virtual-base table, and the wrapper emits dynamic <Derived>_as_<VBase> upcasts that read the offset through the object's vtable at runtime — the same helper is correct for every dynamic type. Diamond-proven: one shared base, override dispatch through the vbase vtable, polymorphic deletion.\n\nTier-2 ABI correctness (driven by the pugixml and tinyxml2 Hub packages).\n\nSysV small-struct classification — the thunk lowering previously forced sret for every packed struct return; native x86-64 returns ≤16-byte aligned structs in registers, so calls shifted every argument by one slot. try_call/ffe_call now classify MEMORY vs register class and coerce register-class structs one scalar per eightbyte, clang-style, for returns and by-value arguments. Verified against a real clang-compiled callee.\nNested packed structs — a padding-free struct nested by value inside a padded struct leaked a JLCS dialect type into LLVM struct bodies, crashing LLVM-IR translation at module load. Such members now inline as byte-identical LLVM literals.\nJIT pre-flight guard — create_jit refuses modules containing untranslatable types with a catchable error naming the type and op; a bad type degrades the module to \"Tier 2 disabled\" instead of killing the process.\nScope-RAII by-value parameters — under Itanium, a by-value parameter of a class with a non-trivial destructor is passed as a pointer to a caller-owned temporary. Thunks now copy-construct the temporary in a jlcs.scope and destruct it at scope exit; the old raw-bits pass was a silent miscompile.\nThunk routing — virtual instance methods were previously routed to a legacy thunk pass whose symbols the wrapper never looked up; they now flow through the standard ciface thunk pass and are callable through invoke().\nDialect verifiers — jlcs.scope, jlcs.marshal_arg, jlcs.vcall, and jlcs.type_info verify their attribute arity and element kinds at parse time; the previously known lowering crashes are now parse diagnostics.\n\nRobustness and honesty.\n\ndiscover(force) preserves user intent — forced re-discovery no longer destroys hand-curated TOML sections ([types].templates, [wrap].varargs/macros/shim_headers/cstring_owned); this had silently broken template-dependent fixtures for six weeks.\nDepth-aware DWARF attribution — members declared after a nested type definition no longer vanish from metadata (clang interleaves nested-type DIEs mid-member-list; the parser now attributes by DIE tree depth).\nIngest honesty pass — ingest is documented and guarded as an experimental, C-only fallback; both entry points warn that C++ API surfaces of ingested binaries are unsupported.\nMacro-shim header-collision guard — shim includes are verified to resolve inside the project/dependency tree, not to a system-installed header at a different version.","category":"section"},{"location":"release-notes/#v3.0.0-(2026-07-10)-—-the-C-generator-audit-release","page":"Release Notes","title":"v3.0.0 (2026-07-10) — the C-generator audit release","text":"Ownership and ABI edges of the ergonomic layer closed. Breaking changes to the generated-wrapper API (wrappers regenerate automatically via the fingerprinted cache; calling code may need updates):\n\nStruct-by-value convenience overloads removed — every such call was UB-adjacent (the callee saw a pointer to a temporary copy; frees and stores corrupted memory). Pass a pointer or Ref instead.\nchar* returns are Union{String,Nothing} — NULL returns nothing instead of throwing. Ownership of malloc'd returns is declared in [wrap.cstring_owned] (the wrapper frees through the declared symbol after copying); every Cstring-returning function gains a raw <name>_ptr variant.\nTrue variadic calls — vararg wrappers emit the @ccall semicolon form, so the callee is declared variadic in LLVM IR and the SysV AL protocol is correct; float varargs no longer depend on leftover register state.\nNested-member structs resolve to named fields (v2.5.8) instead of _data::NTuple blobs, under an exact-layout proof.\nBitfield accessors assemble exact byte spans — no out-of-bounds reads/writes at struct tails; setters accept negative integers with wrapping semantics.\nRegistry cache fingerprints the generator — use() can no longer serve wrappers generated by outdated codegen; each package rebuilds once after upgrading RepliBuild.\nWrappers resolve their library sibling-first via @__DIR__, making cached builds self-contained.\nMacro shims pinned to default visibility — [wrap.macros] survives -fvisibility=hidden builds.\nMisaligned small blob parameters by value now trap loudly instead of corrupting silently; unresolvable-type globals get a _ptr accessor only.","category":"section"},{"location":"release-notes/#v2.5.8-/-v2.5.9-(2026-06,-internal)","page":"Release Notes","title":"v2.5.8 / v2.5.9 (2026-06, internal)","text":"Never separately registered; shipped as part of v3.0.0.\n\nNested-struct member resolution (C) — structs whose members are themselves structs resolve to named Julia fields under an exact-layout proof (explicit padding fields, every DWARF offset and total size reproduced) instead of opaque byte blobs. This closed a silent by-value miscompile where a small float-bearing struct's blob image classified INTEGER while the real struct traveled in XMM registers.\nABI safety trap for residual register-class float blobs: fail closed rather than corrupt.\nPer-file IR cache correctness — the cache is keyed on a compile fingerprint in addition to mtime, so flag/define/include changes rebuild without manual cache clearing.\njlcs.vcall emit fix — the indirect-call lowering used a malformed operand-segment encoding that crashed LLVM-IR emission; rebuilt on the dedicated indirect-call builder.\nVerified compatibility with system LLVM/MLIR 22.x for the dialect build.","category":"section"},{"location":"using-wrappers/#Using-a-Wrapper-in-Your-Package","page":"Using a Wrapper","title":"Using a Wrapper in Your Package","text":"The generated wrapper is a complete Julia module, and the README's include + using .Module pattern is enough for scripts. This page covers the next step: building a real Julia package on top of a generated wrapper — one that precompiles, drives the Tier-2 MLIR JIT, manages C++ object lifetimes, and composes with other wrapped libraries.\n\nEverything here is demonstrated by a working reference package: examples/BoxWorld in the RepliBuild-Hub repository — a physics sandbox driving the Box2D (C++) wrapper through Tier-2 thunks from a precompiled package, with a full test suite.","category":"section"},{"location":"using-wrappers/#The-two-layer-discipline","page":"Using a Wrapper","title":"The two-layer discipline","text":"A package built on a wrapper has two layers with a hard boundary:\n\nThe ABI layer — the RepliBuild output (<Name>.jl, the .so, compilation_metadata.json, thunk_manifest.json), vendored into your package unmodified. Regenerate it with wrap(); never edit it.\nThe ergonomic layer — your package's own code: Julia-idiomatic types, lifecycles, defaults, and naming. Every C++-ism the wrapper cannot hide (ctor-only classes, header-inline defaults, abstract-class vtables) is encapsulated here once, behind ordinary Julia functions.\n\nKeeping library-specific ergonomics in your package — not in hand-edits to the generated file — is what keeps the wrapper regenerable.","category":"section"},{"location":"using-wrappers/#Package-layout:-vendor-the-wrapper","page":"Using a Wrapper","title":"Package layout: vendor the wrapper","text":"MyPkg/\n├── Project.toml            # [deps] must include RepliBuild\n├── lib/                    # vendored wrapper output, verbatim from julia/\n│   ├── Box2d.jl\n│   ├── libbox2d.so\n│   ├── compilation_metadata.json\n│   └── thunk_manifest.json\n├── src/MyPkg.jl\n└── test/runtests.jl\n\nmodule MyPkg\n\nusing RepliBuild        # the wrapper dispatches Tier 2 through RepliBuild.JITManager\n\ninclude(joinpath(@__DIR__, \"..\", \"lib\", \"Box2d.jl\"))\nusing .Box2d\n\n# ... your ergonomic layer ...\nend\n\nThree facts make the vendored copy self-contained:\n\nSibling-first library resolution. The wrapper resolves its .so next to its own file first, with the build-time absolute path as fallback — so the copy in lib/ binds to lib/libbox2d.so wherever the package lives.\nMetadata rides along. compilation_metadata.json and thunk_manifest.json are read from the wrapper's own directory at load time (JIT initialization) and are available to your code for layout facts (see below).\nThe dependency is RepliBuild, nothing else. Tier-2 dispatch calls RepliBuild.JITManager.invoke; Tier-3 wrappers are plain ccall. There is no MLIR/LLVM Julia dependency in the consuming package — libJLCS.so is found through the installed RepliBuild.\n\nAlternatives to vendoring: RepliBuild.use(\"box2d\") loads a registry/Hub package at runtime (good for scripts and exploration, not for precompiled packages), and RepliBuild.scaffold_package generates a package skeleton that builds the library at the consumer's machine instead of shipping a binary.","category":"section"},{"location":"using-wrappers/#Precompilation:-what-the-wrapper-guarantees,-what-you-must-uphold","page":"Using a Wrapper","title":"Precompilation: what the wrapper guarantees, what you must uphold","text":"A package that includes a generated wrapper precompiles normally. The generated module is precompilation-safe by construction:\n\nAll process-level state — dlopen, JIT engine creation, C stdout unbuffering — lives in the wrapper's __init__, which Julia runs at load time, never during precompilation.\nEvery emitted method has a unique dispatch signature. Distinct C++ symbols that collapse to the same Julia signature (destructor D1/D2 pairs, overloads whose parameter types all map to ::Any) are deduplicated at generation time, keeping the last definition — method overwriting is a hard error during package precompilation, not the warning you see from a script include.\n\nwarning: Wrappers generated before v3.0.2 do not precompile\nOlder generated wrappers contain those duplicate definitions and will fail package precompilation with Method overwriting is not permitted. Regenerate with a current RepliBuild (RepliBuild.wrap(toml)) — the registry cache fingerprints the generator, so use()-managed packages rebuild automatically.\n\nYour ergonomic layer must follow the same two rules:\n\nNever bake process addresses at top level. A const computed from dlsym, cglobal, or a JIT lookup is evaluated during precompilation and baked stale into the image. Use a Ref filled in your package's __init__ (BoxWorld does this for the shape vtable address points).\nBaking pure data is good. Parsing compilation_metadata.json into const offset tables at top level is safe and fast — the metadata ships inside your package and cannot drift from the vendored .so.","category":"section"},{"location":"using-wrappers/#The-JIT-lifecycle-from-a-package's-point-of-view","page":"Using a Wrapper","title":"The JIT lifecycle from a package's point of view","text":"When your package loads, the wrapper's __init__ calls RepliBuild.JITManager.initialize_global_jit(LIBRARY_PATH), which:\n\nregisters one JIT engine for that binary (parsing its DWARF vtables, reading its thunk manifest, generating + lowering + compiling exactly the thunks the wrapper dispatches to);\nleaves per-symbol pointers to be resolved lazily — the first call to each thunk takes a locked slow path, every later call is a lock-free dictionary read.\n\nMultiple wrapped libraries compose. Engines are per-binary: loading a second wrapper registers a second engine, and thunk lookups search all of them (thunk names derive from mangled symbols, so they are unique across libraries). One library's initialization failure disables Tier 2 for that library only — its ccall wrappers and every other library keep working.\n\nDegradation is catchable, not fatal. If JIT initialization fails (missing libJLCS.so, an untranslatable type refused by the pre-flight guard), the wrapper module still loads; Tier-2 calls raise a descriptive error naming the root cause. Design your package so Tier-3-only operation degrades features rather than crashing imports.\n\nLoad-time cost is real. Engine initialization parses DWARF and compiles thunks — around a second for a mid-sized C++ library. It happens once per process, in __init__, not per call.\n\nAsk the wrapper which tier a function actually uses. Every generated module carries its own dispatch table and an introspection function — deliberately not exported, so reach for them qualified:\n\nLua.dispatch_tier(Lua.lua_gettop)        # :tier1 | :tier2 | :tier3 | :unknown\nLua.DISPATCH_TIER[:lua_gettop]           # what the generator emitted\n\nThe two differ exactly when a Tier-1 kernel demotes — a wrapper relocated without its slices/ directory, or an unresolvable declare. DISPATCH_TIER is the emitted intent; dispatch_tier reads the code that will actually run and is the one to assert on in a test.\n\nTwo answers are not tiers and both mean \"don't build on this\":\n\n:mixed — the name carries methods on more than one tier (typically a Tier-1 primary plus a Tier-3 convenience overload), so no single answer is true.\n:deferred — you asked during precompilation. dispatch_tier is not a read-only call: probing forces the @generated kernel to generate, and a kernel generating inside a precompile worker deliberately splices its ccall body. An answer taken there would describe the worker, not the session that runs, so const T = M.dispatch_tier(:f) at module scope is refused rather than frozen wrong. Ask at runtime.\n\nLayout facts travel with the wrapper too. Sizes and member offsets come from the DWARF the module was generated from, which you need for the two cases the type system cannot cover — stack space for an in-place constructor, and reading a member through an opaque pointer the API hands back as Ptr{Cvoid}:\n\nbuf = zeros(UInt8, Box2d.struct_size(:b2BodyDef))     # in-place ctor space\noff = Imgui.member_offset(:ImGuiIO, :MousePos)        # poke through ImGui_GetIO()\n\nBoth are scoped to the types the module actually declares, and both throw a message naming the alternatives rather than returning a zero that would silently under-allocate.\n\nWhen a Tier-2 call misbehaves, you can look at it. The thunks your package dispatches to are written as MLIR to <wrapper>/../.debug/mlir/ and carry DWARF pointing there, so RepliBuild.Debug.walk(pkg, symbol) prints the dialect and the emitted machine code for one thunk, and gdb will stop inside the generated MLIR by file and line. See Debugging a thunk.","category":"section"},{"location":"using-wrappers/#Calling-conventions-your-layer-will-use","page":"Using a Wrapper","title":"Calling conventions your layer will use","text":"The generated docstrings state each function's argument types. The patterns that come up when driving a C++ library:\n\nBy-value handle objects as this. Small C++ handle structs (an 8-byte xml_node, a b2Vec2) are returned by value. To call a method on such a value, pass a pointer to it:\n\nwithptr(f, h) = (r = Ref(h); GC.@preserve r f(Base.unsafe_convert(Ptr{typeof(h)}, r)))\nname = withptr(node -> pugi_xml_node_name(node), root)\n\nManaged* types own heap pointers. For pointer-returning factory/creator functions, the wrapper emits Managed<Class> types whose finalizer calls the C++ destructor. Use the _safe variants (make_thing_safe() -> ManagedThing) when the returned object is yours to free; use raw pointers when the library retains ownership (a b2Body* is owned by its world — never wrap it in a finalizer).\n\nC++ exceptions arrive as CxxException. May-throw functions route through landing-pad thunks; a C++ exception escaping the callee surfaces as RepliBuild.JITManager.CxxException carrying the original what() string. Catch it like any Julia exception.\n\nFinalizers and the JIT. A finalizer that calls a Tier-2 destructor thunk should be warmed at __init__ (one _lookup_cached call), so the finalizer always takes the lock-free path — first-call compilation inside a GC finalizer means taking a lock at GC time. And make destruction idempotent + explicit-first: offer destroy!(x) for deterministic teardown with the finalizer as the safety net (see BoxWorld.World).","category":"section"},{"location":"using-wrappers/#The-C-isms-your-layer-will-encapsulate","page":"Using a Wrapper","title":"The C++-isms your layer will encapsulate","text":"Real C++ libraries have constructs no binding generator can fully hide. The wrapper gives you the raw material; your layer wraps each once:\n\nCtor-only classes (no factory function, e.g. b2World): allocate byte_size bytes (from the metadata), call the constructor symbol on that storage, run the Tier-2 destructor thunk at teardown:\nmem = zeros(UInt8, struct_size(\"b2World\"))\nccall((:_ZN7b2WorldC2ERK6b2Vec2, Box2d.LIBRARY_PATH), Cvoid,\n      (Ptr{Cvoid}, Ptr{b2Vec2}), pointer(mem), gravity_ref)\nHeader-inline default constructors (b2BodyDef()): inline functions never exist as symbols, so their defaults are unreachable through any binding. Replicate them by writing fields at their DWARF offsets — read the offsets from compilation_metadata.json, never hardcode them, so layout drift fails loudly.\nHeader-inline accessors (GetPosition()): same story — read the member directly at its metadata offset.\nAbstract-class instances that C++ code virtual-calls (a b2CircleShape the library Clone()s): plant the real compiler vtable — dlsym the _ZTV… symbol, +16 past offset-to-top and RTTI to the address point, store it at offset 0. Resolve the vtable address in __init__ (it is a process address).\n\nEach of these is a few lines, written once, tested by your package's suite. The metadata-offset habit is the load-bearing one: every layout fact your layer uses should come from the wrapper's own compilation_metadata.json.","category":"section"},{"location":"using-wrappers/#Testing-your-package","page":"Using a Wrapper","title":"Testing your package","text":"Treat the wrapper boundary as tested (RepliBuild's suites own it) and test your claims: lifecycles (create/use/destroy/idempotent-destroy), behavior of the underlying library through your API (BoxWorld asserts free-fall against the semi-implicit Euler closed form), the finalizer path (drop a live object, GC.gc(), assert no crash), and — worth pinning in any consumer — that the JIT engine registered:\n\n@test any(e -> occursin(\"libbox2d\", e.binary_path) && e.init_error === nothing,\n          RepliBuild.JITManager.GLOBAL_JIT.engines)","category":"section"},{"location":"using-wrappers/#Deployment-notes","page":"Using a Wrapper","title":"Deployment notes","text":"The vendored .so is platform-specific. For multi-platform distribution, build per-platform artifacts or scaffold with deps/-driven builds (scaffold_package); the wrapper file itself is platform-independent apart from the binary it binds.\naot_thunks = true at wrap time replaces the load-time JIT with a pre-compiled _thunks.so — no MLIR at runtime, faster load, at the cost of shipping a second binary.\nStrip nothing: the DWARF in the .so is what the JIT initialization parses at load time.","category":"section"},{"location":"#RepliBuild.jl","page":"Home","title":"RepliBuild.jl","text":"ABI-aware C/C++ compiler bridge for Julia. Point it at source code, get type-safe Julia bindings — correct struct layouts, enum definitions, calling conventions, inheritance, and virtual dispatch — without writing a single ccall by hand.","category":"section"},{"location":"#Overview","page":"Home","title":"Overview","text":"RepliBuild compiles your C/C++ source with Clang, then combines multiple information sources to generate bindings that are correct by construction:\n\nDWARF debug metadata — struct member offsets, sizes, function signatures, vtable layout, base-class subobject offsets, bitfield positions. This is the compiler's own record of what it produced — always accurate for the target platform.\nSymbol tables (nm) — mangled C++ names and function addresses. The authoritative linking identity.\nClang.jl AST — enums the compiler optimized away, function pointer typedefs, macro definitions. Fills gaps where DWARF is incomplete.\nCross-verification — DWARF struct size is checked against Julia's alignment calculation. If they disagree, the struct is packed and gets routed to an MLIR thunk instead of ccall (which would silently misalign fields).\n\nFunctions are automatically routed to one of three calling tiers — Base.llvmcall with LTO bitcode, MLIR thunks, or ccall — based on ABI complexity.","category":"section"},{"location":"#Three-tier-dispatch","page":"Home","title":"Three-tier dispatch","text":"Tier Mechanism When selected\n1 Base.llvmcall on a per-function bitcode slice POD args, scalar/pointer return, [wrap.tier1] enable = true\n2 MLIR thunks (libJLCS.so) Packed structs, unions, large struct returns, C++ classes, virtual dispatch, exceptions\n3 ccall Direct call into the .so; the unconditional fallback\n\nTier selection is automatic — the wrapper generator analyses each function signature against DWARF metadata and emits the appropriate calling convention.\n\nTier 2 is the architectural centre of the project: RepliBuild expresses ABI marshalling as first-class compiler IR in a purpose-built MLIR dialect (JLCS), derived from the same DWARF that describes the types, lowered by a pass that models the x86-64 SysV ABI explicitly, and debuggable at source level in gdb. See ABI Marshalling as Compiler IR for the full treatment.\n\nnote: Current Tier 1 status\nTier 1 runs on per-function bitcode slices — declarations-only modules holding one function body, with everything it reaches left as a declare bound to the .so at JIT time. Opt in per project with [wrap.tier1] enable = true (C only, default off) — experimental, a side project rather than a supported tier; it is live at library scale (Lua: 209 slices accepted, 190 emitted across 189 functions, the rest cleanly on ccall). The older [link] enable_lto payload embeds the whole linked module per call site and remains scale-limited — it can crash Julia's JIT on large libraries and duplicates file-local static state — so production configs keep enable_lto = false; C++ defaults to LTO off. The two knobs are independent. See Zero-cost LTO dispatch for details.","category":"section"},{"location":"#Quick-start","page":"Home","title":"Quick start","text":"using RepliBuild\n\n# Scan a C/C++ project, generate replibuild.toml, compile, and wrap\nRepliBuild.discover(\"path/to/project\", build=true, wrap=true)\n\n# Load the generated module\ninclude(\"path/to/project/julia/MyProject.jl\")\nusing .MyProject\n\nOr step by step:\n\ntoml = RepliBuild.discover(\"path/to/project\")  # generates replibuild.toml\nRepliBuild.build(toml)                          # Clang → LLVM IR → .so + DWARF metadata\nRepliBuild.wrap(toml)                           # DWARF → Julia module in julia/","category":"section"},{"location":"#Package-registry-and-the-Hub","page":"Home","title":"Package registry and the Hub","text":"RepliBuild.register(\"path/to/project/replibuild.toml\")  # one-time local registration\nLua = RepliBuild.use(\"lua\")                              # build + wrap + load, cached\nLua.luaL_newstate()\n\nRepliBuild.search(\"xml\")   # search the community Hub (RepliBuild-Hub) for ready-made configs\n\nuse() checks the local registry first, then fetches the package configuration from the RepliBuild-Hub community registry on a miss. Builds are cached at ~/.replibuild/builds/<hash>/; the cache key includes RepliBuild's own version, so upgrading the generator automatically rebuilds stale wrappers.","category":"section"},{"location":"#What-gets-wrapped","page":"Home","title":"What gets wrapped","text":"Structs with correct field order, alignment padding, and topological sort for circular references; struct-typed members resolve to named fields when the layout can be proven exact\nEnums via @enum with correct underlying types (Clang.jl AST walker)\nUnions as NTuple{N,UInt8} with typed getter/setter accessors\nBitfields with exact byte-span accessors (reads and writes never touch bytes outside the field's span)\nFunction pointers with DWARF signature parsing to @cfunction-compatible types\nVariadic functions as true variadic calls (@ccall semicolon form — formally correct on x86-64 SysV, including float varargs), with typed overloads via [wrap.varargs]\nMacros with auto-generated typed shims via [wrap.macros]\nchar* returns with an ownership-aware policy: Union{String,Nothing} (NULL → nothing), declared deallocators via [wrap.cstring_owned], and raw <name>_ptr variants for lifetime-sensitive callers\nMulti-level pointers / references — T** → Ptr{Ptr{T}}, T& → Ref{T}\nC++ classes — methods, in-place constructor/destructor thunks, Managed handle types with GC finalizers backed by DWARF-resolved destructors\nC++ virtual methods — vtable dispatch that honors overrides (a base-class wrapper invoked on a derived object reaches the override)\nC++ inheritance — non-virtual multiple inheritance with <Derived>_as_<Base> upcast helpers, and virtual inheritance with dynamic (vtable-resident) upcasts\nC++ exceptions — may-throw functions route through landing-pad thunks; escaped exceptions surface in Julia as CxxException with the original what() message\nIdiomatic wrappers — factory/destructor pairs → mutable struct with GC finalizers\nGlobal variables via cglobal accessors (unresolvable types degrade to a _ptr accessor rather than an unsafe getter)\nTemplates — declare in [types].templates; RepliBuild forces DWARF emission","category":"section"},{"location":"#Example:-wrapping-a-git-dependency","page":"Home","title":"Example: wrapping a git dependency","text":"# replibuild.toml\n[dependencies.cjson]\ntype    = \"git\"\nurl     = \"https://github.com/DaveGamble/cJSON\"\ntag     = \"v1.7.18\"\nexclude = [\"test\", \"fuzzing\"]\n\nRepliBuild.build(\"replibuild.toml\")\nRepliBuild.wrap(\"replibuild.toml\")\n\ninclude(\"julia/MyCjsonWrapper.jl\")\nusing .MyCjsonWrapper\n\nobj = cJSON_CreateObject()\ncJSON_AddStringToObject(obj, \"key\", \"value\")","category":"section"},{"location":"#Configuration","page":"Home","title":"Configuration","text":"The replibuild.toml file controls the entire build. Generated by discover(), hand-editable:\n\n[project]\nname = \"MyEngine\"\n\n[compile]\nflags      = [\"-O3\", \"-std=c++17\", \"-fPIC\"]\nparallel   = true\naot_thunks = false           # true → pre-compile MLIR thunks into <name>_thunks.so\n\n[link]\nenable_lto         = false   # true → emit _lto.bc and llvmcall the whole module (scale-limited; see note above)\noptimization_level = \"3\"\n\n[wrap]\nlanguage     = \"cpp\"         # \"c\" | \"cpp\" (auto-detected by discover())\nuse_clang_jl = true\n\n[wrap.tier1]                 # C only — per-function bitcode slices for Base.llvmcall\nenable = false\n\n[types]\nstrictness = \"warn\"\ntemplates  = [\"std::vector<int>\"]\ntemplate_headers = [\"<vector>\"]\n\n[dependencies.mylib]\ntype = \"git\"\nurl  = \"https://github.com/example/mylib\"\ntag  = \"v1.0.0\"\n\nHand-curated sections survive re-discovery: discover(force=true) preserves user-intent keys ([types].templates/template_headers, [wrap].varargs/macros/shim_headers/cstring_owned/tier1, [link].promote_statics) instead of regenerating them empty.\n\nDiscovery writes what it can see. What it cannot infer — template instantiations, macros, vararg signatures, char* ownership, the flags upstream's build system supplied — you declare, and the Configuration Reference documents every key, every default, and exactly what each class of library requires you to state.","category":"section"},{"location":"#System-requirements","page":"Home","title":"System requirements","text":"Julia 1.10+ (developed on 1.12).\nC projects need no external LLVM. Compilation uses the Clang JLL shipped with the Julia ecosystem, and the link/optimize/assemble steps run in-process on Julia's own libLLVM — version-matched by construction.\nC++ projects and Tier 2 need a system LLVM/MLIR toolchain (21+) for the JLCS dialect (libJLCS.so) and the external thunk pipeline, plus CMake 3.20+ and mlir-tblgen to build the dialect (cd src/mlir && ./build.sh).\nRun RepliBuild.check_environment() to see exactly which tiers are available on your system, with install instructions for anything missing.","category":"section"},{"location":"#Documentation","page":"Home","title":"Documentation","text":"Workflow — discover → build → wrap → use, dependencies, LTO, AOT thunks, templates, the registry and Hub, ingest\nConfiguration — The complete replibuild.toml reference: what discovery cannot know and you must declare, every section and key with its true default, what fails loudly vs. what is silently ignored, worked configs, and a symptom → key troubleshooting index\nABI Marshalling as Compiler IR — The JLCS MLIR dialect: why marshalling is compiled IR, the thunk contract, the op reference, the SysV lowering, source-level debugging, and the failure classes the design is built around\nUsing a Wrapper — Building a precompiled Julia package on a generated wrapper: vendoring, the JIT lifecycle, C++ object lifetimes, and the C++-isms your layer encapsulates\nAPI Reference — The public entry points\nThe Inheritance ABI — How MI and virtual inheritance become callable: upcast helpers, class-local vcall dispatch\nInternals & Dispatch — Pipeline, the three dispatch tiers, DWARF extraction, module-by-module reference, caching\nRelease Notes — Version history","category":"section"},{"location":"internals/#Internals-and-Dispatch","page":"Internals & Dispatch","title":"Internals & Dispatch","text":"RepliBuild compiles C/C++ with Clang, reads the DWARF debug metadata the compiler emits about its own output, and generates Julia bindings that are correct by construction — struct offsets, enum underlying types, vtable slots, base-subobject offsets, and calling conventions all come from the compiler's own record rather than being guessed. Where DWARF is incomplete (enums the optimizer folded away, macro definitions, function-pointer typedefs) the Clang.jl AST fills the gap; where Julia's computed struct alignment disagrees with the DWARF size, the struct is treated as packed and routed away from ccall.\n\nThis page is the technical reference for how that pipeline is assembled — the stages, the three dispatch tiers, and the modules behind each. It is aimed at contributors and advanced integrators, not everyday use.\n\nTier 2 — the MLIR/JLCS marshalling layer — has its own page: ABI Marshalling as Compiler IR covers the dialect's types and ops, the SysV lowering, the thunk calling contract, source-level debugging, and the failure classes the design exists to make loud. This page describes where it sits; that one describes what it is.","category":"section"},{"location":"internals/#The-pipeline","page":"Internals & Dispatch","title":"The pipeline","text":"Source becomes a loadable Julia module in seven stages, each owned by one module:\n\nDependencyResolver — resolve the [dependencies] table (git / local / system) and merge the external sources into the build graph.\nDiscovery — scan files and the #include graph; write or refresh replibuild.toml.\nCompiler — Clang compiles each translation unit to LLVM IR, cached on source mtime plus a compile fingerprint (flags, defines, include dirs, LLVM version, target triple).\nLinker — link, optimize, and assemble the IR into the .so (plus LTO bitcode when enable_lto is on).\nDWARFParser — llvm-dwarfdump + nm produce ClassInfo / VtableInfo and the compilation_metadata.json layout facts.\nWrapper generator — DWARF + symbols drive the C or C++ generator to emit the Julia module.\nJITManager — at module load, stand up a per-library MLIR thunk engine for any Tier-2 calls.\n\nFor C projects the link/optimize/assemble steps run in-process on Julia's resident libLLVM, version-matched to the JLL clang that emitted the IR (no external LLVM, no DWARF-dropping version skew); a failure is a hard error unless [link] fallback = true selects the external llvm-link/opt pipeline. C++ always uses the external pipeline plus the system MLIR dialect. Only the final .ll → .so codegen shells to clang in both buckets.","category":"section"},{"location":"internals/#Three-dispatch-tiers","page":"Internals & Dispatch","title":"Three dispatch tiers","text":"Each wrapped function is routed to exactly one calling tier, chosen from its DWARF signature:\n\nTier Mechanism Selected when\n1 Base.llvmcall against a per-function bitcode slice POD args, scalar/pointer return, [wrap.tier1] enable = true (C only)\n2 MLIR thunk via libJLCS.so (JIT at load, or AOT _thunks.so) packed structs, unions, large by-value struct returns, C++ classes, virtual dispatch, exceptions\n3 ccall into the .so the unconditional fallback\n\nThe routing decision lives in Wrapper/DispatchLogic.jl (is_ccall_safe, is_c_lto_safe); the Tier selection logic section below lists the exact checks.\n\nnote: Two Tier-1 payloads, one supported\nTier 1 can carry its IR two ways, and the knobs are independent.Per-function slices — [wrap.tier1] enable (C only, default off) is the supported path. _tier1_slice_prepass (Wrapper/C/GeneratorC.jl) runs IRGen/Slicer.jl over every is_c_lto_safe non-varargs candidate, applies the hazard/size policy, and dlsym-pre-flights each slice's declarations against the real .so. Acceptance only makes a function eligible: a call site additionally needs lto_shape_ok (no Cstring or struct crossing) and must survive the signature dedup, so _tier1_emit_slices! writes julia/slices/<mangled>.ll from the final wrapper text — only slices a call site actually reads reach disk, and TIER1_FUNCTIONS is derived in the same pass. Rests on static promotion (_promote_statics_libllvm) to guarantee every declared symbol reaches the dynamic symbol table. Decided at generation time — non-accepted functions emit plain ccall, and the wrapper exports TIER1_FUNCTIONS.Whole-module bitcode — [link] enable_lto embeds the entire linked module at each call site: scale-limited (can crash Julia's JIT on large libraries) and it duplicates file-local static state between the embedded bitcode and the .so. Production configurations set enable_lto = false; C++ defaults to LTO off. Treat it as an experimentation path for small stateless kernels.See Zero-cost LTO dispatch.","category":"section"},{"location":"internals/#Wrapper","page":"Internals & Dispatch","title":"Wrapper","text":"Source: src/Wrapper.jl, src/Wrapper/\n\nThe Wrapper package generates Julia FFI modules from DWARF metadata and binary symbol tables. It is structured as a two-track system: a C generator and a C++ generator, selected automatically via config.wrap.language.","category":"section"},{"location":"internals/#Module-layout","page":"Internals & Dispatch","title":"Module layout","text":"Module Source Role\nWrapper.Generator src/Wrapper/Generator.jl Top-level wrap_library() entry point; dispatches to C or C++ generator\nWrapper.DispatchLogic src/Wrapper/DispatchLogic.jl Per-function tier routing decisions (is_ccall_safe, is_c_lto_safe)\nWrapper.TypeRegistry src/Wrapper/TypeRegistry.jl TypeRegistry and TypeStrictness — shared type-resolution context\nWrapper.Symbols src/Wrapper/Symbols.jl ParamInfo / SymbolInfo structs for structured symbol data\nWrapper.FunctionPointers src/Wrapper/FunctionPointers.jl DWARF function_ptr(...) signature to Julia @cfunction type string\nWrapper.Utils src/Wrapper/Utils.jl Keyword escaping, identifier sanitization shared between generators\nWrapper.C.GeneratorC src/Wrapper/C/GeneratorC.jl Full C wrapper generator (structs, enums, functions, LTO, thunks)\nWrapper.C.TypesC src/Wrapper/C/TypesC.jl C type heuristics and base type map\nWrapper.C.UtilsC src/Wrapper/C/UtilsC.jl C-specific identifier/format helpers\nWrapper.C.IdentifiersC src/Wrapper/C/IdentifiersC.jl C name sanitization\nWrapper.Cpp.GeneratorCpp src/Wrapper/Cpp/GeneratorCpp.jl Full C++ wrapper generator (classes, inheritance flattening with subobject-offset rebasing, as_<Base>/as_<VBase> upcasts, Managed handles, virtual dispatch)\nWrapper.Cpp.TypesCpp src/Wrapper/Cpp/TypesCpp.jl C++ type map including STL, templates, references\nWrapper.Cpp.IdentifiersCpp src/Wrapper/Cpp/IdentifiersCpp.jl Namespace stripping, operator sanitization\nWrapper.Cpp.UtilsCpp src/Wrapper/Cpp/UtilsCpp.jl C++ formatting helpers\nWrapper.Cpp.STLWrappers src/Wrapper/Cpp/STLWrappers.jl STL container type detection and accessor generation\nWrapper.Rust.GeneratorRust src/Wrapper/Rust/GeneratorRust.jl Experimental Rust generator — requires extern \"C\" + #[repr(C)] surfaces","category":"section"},{"location":"internals/#Language-selection","page":"Internals & Dispatch","title":"Language selection","text":"wrap.language is an extensible dispatch key — \"c\" and \"cpp\" are the first two targets, with additional language generators planned:\n\n[wrap]\nlanguage = \"c\"   # selects C generator + clang toolchain\nlanguage = \"cpp\" # selects C++ generator + clang++ toolchain (default)\n\ndiscover() sets this automatically based on the scanned source files. Adding a new language means adding a generator under src/Wrapper/<Lang>/ and registering it in Wrapper/Generator.jl.","category":"section"},{"location":"internals/#Tier-selection-logic","page":"Internals & Dispatch","title":"Tier selection logic","text":"The function is_ccall_safe() in src/Wrapper/DispatchLogic.jl is the core dispatch decision. It inspects each function's DWARF metadata and returns false when the signature needs an MLIR thunk (Tier 2), true when it can be called directly (Tier 1 or Tier 3).\n\nChoosing between the two direct tiers is a second, C-only step. is_c_lto_safe() marks a function Tier-1-eligible unless its return type is a packed struct or a by-value union — C has no templates, vtables, STL, or inheritance, so those are the only ABI hazards, and by-value parameters are fine either way. Eligibility is necessary but not sufficient: with [wrap.tier1] enable = true the function must also survive slicing, the hazard/size policy, and the dlsym pre-flight. Anything that does not, and everything when Tier 1 is off, emits ccall.\n\nChecks performed:\n\nSTL container types — Any STL type in parameters or return forces Tier 2\nReturn type safety:\nTemplate returns (contains <) → Tier 2 (unpredictable ABI)\nStruct return by value > 16 bytes → Tier 2 (too large for ccall sret)\nNon-POD class return → Tier 2\nPacked struct return (DWARF size != Julia aligned size) → Tier 2\nParameter type safety:\nUnion parameters → Tier 2\nPacked struct parameters → Tier 2\nException safety — Per-function is_noexcept flag from DWARF. If absent (function may throw) and the module's may_throw setting is on, the function routes through jlcs.try_call rather than ccall.\n\nFor struct-graph cases where pairwise heuristics miss transitive layout mismatches (a non-packed struct that contains a packed struct, for example), src/IRGen/DAGDiff.jl performs a structural type-graph diff to surface bad cases and produces a topo-sorted lowering order for multi-type thunks.\n\nFunctions routed to Tier 2 are further divided between JIT dispatch (JITManager.invoke()) and AOT thunks (ccall to _thunks.so), controlled by the aot_thunks config flag.","category":"section"},{"location":"internals/#Idiomatic-wrapper-generation","page":"Internals & Dispatch","title":"Idiomatic wrapper generation","text":"Beyond raw ccall bindings, the wrapper generator clusters related C++ functions by class name to produce idiomatic Julia types:\n\nFactory detection: Functions matching create_X, new_X, make_X, alloc_X, init_X, or returning X* are identified as constructors.\nDestructor detection: Functions matching delete_X, destroy_X, free_X, dealloc_X, or X_destroy are identified as destructors.\nMethod clustering: Functions taking X* as their first parameter and associated with the same DWARF class are grouped as instance methods.\n\nThe result is a mutable struct ManagedX with a raw Ptr{Cvoid} handle, a registered finalizer calling the C++ destructor, and multiple-dispatch method proxies that pass the pointer via Base.unsafe_convert.","category":"section"},{"location":"internals/#Compiler","page":"Internals & Dispatch","title":"Compiler","text":"Source: src/Builder/Compiler.jl\n\nThe Compiler module handles the translation of C/C++ source code into LLVM IR and shared libraries. It oversees the entire build pipeline from dependency management down to IR optimization.","category":"section"},{"location":"internals/#Build-pipeline","page":"Internals & Dispatch","title":"Build pipeline","text":"Auto-discovery and dependency resolution: Scans the project directory, resolving file paths and external git/local dependencies to merge into the build graph.\nPre-processing (shims and templates): Dynamically generates C/C++ shim files for configured macros and explicitly instantiates templates based on replibuild.toml settings. This allows normally invisible constructs to manifest in the final binary and DWARF metadata. Macro shims are pinned to default symbol visibility, and a header-collision guard verifies each direct shim #include resolves inside the project/dependency tree — the shim TU lives under the build cache, so a bare include could otherwise fall through the -I path to a system-installed header at a different version and silently bake wrong macro values.\nCompilation to LLVM IR: Translates source code into .ll text format — .c via the JLL clang, .cpp via system clang++. The per-file cache is keyed on source mtime plus a compile fingerprint (flags, defines, include dirs, LLVM version, target triple); config changes can never silently reuse stale IR.\nIR transformation and sanitization: Strips attributes incompatible with Julia's internal LLVM JIT, removes va_start/va_end intrinsics from varargs function bodies (varargs are routed through true-variadic @ccall wrapper generation), and cleans mismatched debug metadata.\nLink / optimize / assemble: For C, these steps run in-process on Julia's resident libLLVM — LLVM.link! for linking, the new pass manager (default<O…>) for optimization, and in-process bitcode assembly — version-matched to the JLL clang that emitted the IR. A failure is a hard error; [link] fallback = true selects the external llvm-link/opt pipeline instead. C++ always uses the external pipeline.\nStatic promotion (_promote_statics_libllvm, C in-process bucket, [link] promote_statics): see below.\nCodegen: The final .ll → .so step shells to clang/clang++.","category":"section"},{"location":"internals/#Static-promotion","page":"Internals & Dispatch","title":"Static promotion","text":"Post-optimization and pre-codegen, every function or global that a Tier-1 bitcode slice may bind by declare but that cannot reach the .so's dynamic symbol table is renamed to an exported __rb_<lib>_<name> with external linkage and default visibility. The test is \"not exportable\", not \"internal linkage\": default<O…> runs no internalize pass, so it covers both internal/private linkage (file-local statics) and external-linkage-but-hidden/protected symbols — Lua's LUAI_FUNC functions and LUAI_DDEF tables are exactly the latter shape. Only internal constants stay internal: no symbol exists for a slice to bind, and read-only data has no divergence class.\n\nThe rename happens on the one module (<name>_abi.ll) that becomes both the .so and the slice source, so the two are bit-identical by construction — which is what removes the whole-module path's duplicated-static failure class rather than papering over it. The old→new map lands in compilation_metadata.json under promoted_symbols, and extract_symbols_from_binary filters __rb_* so promoted statics never surface as wrappable API.","category":"section"},{"location":"internals/#Metadata-extraction","page":"Internals & Dispatch","title":"Metadata extraction","text":"At build time the compiler also extracts DWARF metadata into compilation_metadata.json (functions, struct definitions, enums, globals). DIE parsing is depth-aware: readelf DIE headers carry the tree depth, and member/enumerator/inheritance/template DIEs at depth d attribute to the type last seen at depth d−1 — so nested type definitions interleaved between members (routine clang output) cannot steal subsequent members from the enclosing class. The recorded emitter version is the compiler that actually produced the IR, not whichever llvm-config happens to be on the PATH.","category":"section"},{"location":"internals/#Configuration-Manager","page":"Internals & Dispatch","title":"Configuration Manager","text":"Source: src/Builder/ConfigurationManager.jl\n\nThe single source of truth for all build settings. Handles TOML parsing, validation, and merging into a typed RepliBuildConfig struct.","category":"section"},{"location":"internals/#Discovery","page":"Internals & Dispatch","title":"Discovery","text":"Source: src/Builder/Discovery.jl\n\nScans the filesystem to identify C/C++ source files, headers, and dependencies. Auto-detects project language (:c vs :cpp) from the scanned source extensions and sets wrap.language accordingly in the generated replibuild.toml.","category":"section"},{"location":"internals/#DWARFParser","page":"Internals & Dispatch","title":"DWARFParser","text":"Source: src/Builder/DWARFParser.jl\n\nParses llvm-dwarfdump output to extract structured type information from compiled binaries. This is the bridge between C++ debug metadata and Julia wrapper generation.","category":"section"},{"location":"internals/#Data-structures","page":"Internals & Dispatch","title":"Data structures","text":"Type Fields Role\nClassInfo name, vtable_ptr_offset, base_classes, base_offsets, virtual_bases, virtual_methods, members, size Complete class/struct description with byte-level layout, inheritance chain (subobject offsets), and virtual-base flags\nVtableInfo classes, vtable_addresses, method_addresses Aggregate metadata for all classes in a binary\nVirtualMethod name, mangled_name, slot, return_type, parameters Single virtual method with the slot index in its declaring class's primary vtable\nMemberInfo name, type_name, offset Struct field with byte offset from struct base","category":"section"},{"location":"internals/#Extraction-targets","page":"Internals & Dispatch","title":"Extraction targets","text":"DWARF Tag Extracted Data\nDW_TAG_class_type / DW_TAG_structure_type Class/struct name, byte size, members, virtual methods, inheritance\nDW_TAG_member Field name, type, DW_AT_data_member_location (byte offset)\nDW_TAG_subprogram (with virtual flag) Virtual method name, mangled name, vtable slot (DW_AT_vtable_elem_location)\nDW_TAG_inheritance Base class with subobject offset; for virtual bases, the vtable-relative offset expression parsed into vbase_vtable_offset\nDW_TAG_enumeration_type Enum definitions\nDW_TAG_union_type Union layout\nDW_TAG_variable Global variables\nDW_TAG_typedef Type aliases","category":"section"},{"location":"internals/#JLCSIRGenerator","page":"Internals & Dispatch","title":"JLCSIRGenerator","text":"Source: src/IRGen/JLCSIRGenerator.jl, src/IRGen/ir_gen/\n\nTransforms parsed DWARF metadata (VtableInfo) into MLIR source text in the JLCS dialect. The generated IR is then parsed and either JIT-compiled by MLIRNative (Tier 2 JIT) or written to disk and AOT-compiled by ThunkBuilder (Tier 2 AOT). Both paths share this module — there is no separate AOT IR generator.","category":"section"},{"location":"internals/#Submodules","page":"Internals & Dispatch","title":"Submodules","text":"Module Source Input Output\nTypeUtils src/IRGen/ir_gen/TypeUtils.jl C++ type string MLIR type string (f64, i32, !llvm.ptr, etc.)\nStructGen src/IRGen/ir_gen/StructGen.jl struct metadata Struct type aliases + registration IR; aligned-vs-packed LLVM struct type strings; members laid out at their DWARF offsets with explicit padding and verified against a Julia mirror of LLVM's abiSize/abiAlign, degrading to a correctly-sized opaque region when they cannot be; packed structs nested by value in other struct bodies are inlined as byte-identical LLVM literals\nFunctionGen src/IRGen/ir_gen/FunctionGen.jl function or virtual method metadata external func.func private @mangled decl + public func.func @mangled_thunk wrapper with llvm.emit_c_interface; scope-RAII temporaries for non-trivial by-value class params\nArrayViewGen src/IRGen/ir_gen/ArrayViewGen.jl fixed-size primitive array members Zero-copy get/set thunks through jlcs.load/store_array_element\nSTLContainerGen src/IRGen/ir_gen/STLContainerGen.jl STL method metadata Accessor thunks for size(), data(), etc.","category":"section"},{"location":"internals/#Generation-flow","page":"Internals & Dispatch","title":"Generation flow","text":"generate_jlcs_ir(vtinfo, metadata; needed_symbols) produces a complete MLIR module:\n\nStruct aliases + registration: type aliases for all extracted structs (packed structs as !jlcs.c_struct, padded structs as !llvm.struct with packed members inlined as LLVM literals)\nType info operations: jlcs.type_info for each class with non-empty members, carrying the DWARF-resolved destructor and the base/virtual-base tables\nFunction thunks: func.func @mangled_thunk wrappers carrying llvm.emit_c_interface — filtered by needed_symbols (the wrapper's thunk manifest, i.e. dead-thunk elimination). Each body unpacks %args_ptr (ciface convention), emits jlcs.marshal_arg for packed-struct parameters, jlcs.scope copy-construct/destruct brackets for non-trivial by-value class parameters, jlcs.ffe_call / jlcs.try_call (per-function noexcept routing) or jlcs.vcall (virtual instance methods with scalar/pointer signatures), and jlcs.marshal_ret for packed-struct returns\nSTL container thunks: Accessor thunks for detected STL containers (size, data, push_back, etc.)\nArray-view thunks: rank-1 strided accessors for fixed-size primitive array members","category":"section"},{"location":"internals/#DAGDiff","page":"Internals & Dispatch","title":"DAGDiff","text":"Source: src/IRGen/DAGDiff.jl\n\nStructural type-graph diff used by tier selection and IR generation when a struct may contain other structs whose layouts disagree between Julia and C++. The pairwise check in is_ccall_safe() catches direct packed-vs-aligned mismatches; DAGDiff catches the transitive cases — a non-packed struct that contains a packed struct as a field, a struct chain through a typedef alias, etc. It outputs a topo-sorted lowering order so that the MLIR thunks for dependent types are emitted in the right sequence.","category":"section"},{"location":"internals/#Slicer","page":"Internals & Dispatch","title":"Slicer","text":"Source: src/IRGen/Slicer.jl\n\nPer-function bitcode slicing for Tier 1, on Julia's resident libLLVM. slice_library(abi_ll; targets, cache_dir) parses the promoted module once and clones it per target (LLVMCloneModule), then strips the clone to declarations: LLVMFunctionDeleteBody for every reached function, LLVMSetInitializer2(gv, NULL) for reached mutable and external constant globals, internalize + globaldce for everything unreached. Internal constants are embedded rather than declared. Every slice is verified before it is returned, and results are cached content-addressed under <cache>/slices/.\n\nThe closure is one level deep by construction — a declared function contributes no edges of its own — so slice size tracks the target function, not the library. lua_gettop cuts 15.8 MB down to 2.8 KB; luaL_openlibs lands at 6 KB. This is why max_slice_kb is a tripwire rather than a tuning knob.\n\nAnything the Slicer cannot slice correctly comes back as a refusal with a reason, never as silently-wrong IR: a variadic target, a blockaddress into a body being deleted, alias/ifunc, or an unpromoted module (the fail-loud guard against slicing _opt.ll by mistake). Softer shapes come back as hazard flags for the generator's gate — :setjmp_family, :varargs_callee, :noinline, :weak, :inline_asm, :module_asm.\n\nEach SliceResult also records the symbols the slice declares, post-DCE and excluding intrinsics. _tier1_preflight! in the C generator dlopens the .so RTLD_GLOBAL and dlsyms each one — the exact lookup ORC will perform at first call — because an unresolved declare does not raise: ORC prints Symbols not found: [...] and then blocks forever. A miss demotes that function to ccall; a .so that will not dlopen disables Tier 1 for the whole wrap.","category":"section"},{"location":"internals/#ThunkBuilder","page":"Internals & Dispatch","title":"ThunkBuilder","text":"Source: src/Builder/ThunkBuilder.jl\n\nAOT compilation path for Tier 2 thunks. When aot_thunks = true in replibuild.toml, this module drives the same JLCSIRGenerator.generate_jlcs_ir() used by the JIT path, lowers the result with MLIRNative.lower_to_llvm(), emits an object file through MLIRNative.emit_object(), and links it against the user's compiled library (clang/clang++ -shared, rpath'd to the library directory) into a companion shared library named <libname>_thunks.so. With [link] enable_lto on it additionally emits and assembles the thunks' own LTO bitcode. An AOT failure is a warning, not a build failure — the JIT path remains available.\n\nThe Julia wrapper then ccalls into the AOT thunks rather than calling JITManager.invoke. There is no MLIR JIT at runtime — libJLCS.so is only needed at build time for the lowering step. After AOT compilation, the user can ship the wrapped library + thunks .so without bundling LLVM/MLIR runtime libraries.","category":"section"},{"location":"internals/#MLIRNative","page":"Internals & Dispatch","title":"MLIRNative","text":"Source: src/IRGen/MLIRNative.jl\n\nLow-level ccall bindings to libJLCS.so, the compiled JLCS MLIR dialect shared library. Provides context management, module parsing, JIT engine creation, LLVM lowering, symbol lookup, and the object/IR emission used by the AOT path. Building the dialect (cd src/mlir && ./build.sh) is required only for the C++/Tier-2 bucket.\n\nTwo behaviours here are load-bearing beyond plain FFI plumbing. parse_module names the parse buffer after a content-hashed file it writes under the library's .debug/mlir/ — MLIR's parser stamps that name onto every op as a FileLineColLoc, and the lowering turns it into the emitted DWARF's DIFile, which is what makes a JIT'd thunk steppable in gdb. And jit_source_path falls back to a temp directory when .debug is unwritable (a read-only install), because losing co-location costs nothing while losing the source view costs the whole capability.\n\nThe dialect itself — its two types, fourteen ops, lowering pass, and calling contract — is documented in ABI Marshalling as Compiler IR.","category":"section"},{"location":"internals/#JITManager","page":"Internals & Dispatch","title":"JITManager","text":"Source: src/IRGen/JITManager.jl\n\nRuntime for Tier 2 dispatch: one MLIR execution engine per wrapped binary (LibraryEngine), held in a process-wide GLOBAL_JIT behind a shared, lock-free thunk cache.","category":"section"},{"location":"internals/#Key-design-points","page":"Internals & Dispatch","title":"Key design points","text":"Per-library engines. initialize_global_jit(binary_path) is called from each generated module's __init__ and creates (or reuses) the engine for its binary. Multiple wrappers coexist in one session — previously the first wrapper won and the second library's entire Tier 2 silently died, found while composing box2d with pugixml. A per-library initialization failure degrades only that library, and a missing-symbol error names every engine that was searched.\nManifest-driven initialization: initialize_global_jit() reads thunk_manifest.json — the thunks the wrapper actually dispatches to — so dead thunks are never generated. Any initialization failure (including the pre-flight rejection of untranslatable IR types in libJLCS) degrades the module to \"Tier 2 disabled\" with ccall wrappers intact, never a process crash.\nSymbol registration before lowering: the engine is given the library and libJLCS.so as shared libraries, and the C++ runtime EH symbols (__gxx_personality_v0, __cxa_begin_catch, __cxa_end_catch) plus the jlcs_* exception helpers are registered explicitly, since JIT'd landing pads reference them by name.\nLock-free hot path: _lookup_cached() reads from an @atomic snapshot of the symbol dictionary with no locking. The cache is published copy-on-write — a fresh dict is built with the new entry and atomically swapped in. Readers always see a stable, immutable snapshot.\nArity specialization: invoke is @generated, emitting arity-specialized code for any argument count — stack-allocated Refs and a fixed-size Ptr{Cvoid}[], allocation-free at every arity. A thunk slot holds a pointer to the argument's storage, so Ref(x) is the right shape for an isbits x; the two kinds that are already an indirection — an AbstractString, and a Base.Ref that is not a Ptr (what a caller passes for a C++ T const& parameter) — are flattened to a raw pointer first and GC-preserved across the call. Both invoke methods share one _arg_marshal_plan, because two copies is how a fix to one silently misses the other.\n@generated return dispatch: _invoke_call resolves at compile time whether the return type is a primitive (direct ccall return) or a struct (sret buffer allocation). An unresolved Any return fails loudly with the actual cause instead of corrupting memory.\nException propagation: After every Tier 2 call, _check_pending_exception() polls the thread-local exception buffer set by jlcs.try_call lowering. If a C++ exception was caught during the call, a CxxException is thrown with the original what() message.","category":"section"},{"location":"internals/#Calling-convention","page":"Internals & Dispatch","title":"Calling convention","text":"All Tier 2 functions use a unified ciface calling convention:\n\nReturn Signature\nScalar T ciface(void** args_ptr)\nStruct void ciface(T* sret, void** args_ptr)\nVoid void ciface(void** args_ptr)","category":"section"},{"location":"internals/#Debug","page":"Internals & Dispatch","title":"Debug","text":"Source: src/Debug/Debug.jl\n\nStatic inspection of what the Tier-2 pipeline actually emitted, for a package this process never built. thunks(pkg) lists the thunk symbols; mlir_body(pkg, symbol) prints the generated dialect; disassemble(pkg; symbol=…) shells to objdump -dS so dialect ops and machine code interleave; dwarf(pkg; section=…) shows the address → MLIR-line table; walk(pkg, symbol) does the common combination in one call.\n\nThe object file it disassembles only exists when the JIT's object cache was enabled, and MLIR requires that at engine-creation time — so it is read from REPLIBUILD_JIT_OBJDUMP before the wrapper loads, not passed as an argument. Nothing here links gdb or LLVM: it shells to objdump and llvm-dwarfdump. See Debugging a thunk for the live-process counterpart.","category":"section"},{"location":"internals/#BuildBridge","page":"Internals & Dispatch","title":"BuildBridge","text":"Source: src/Builder/BuildBridge.jl\n\nLow-level compiler driver that shells out to clang, clang++, llvm-link, opt, llvm-as, and nm. All subprocess invocations go through this module, providing a single point of control for toolchain interaction. It serves the C++ pipeline and the C bucket's [link] fallback = true escape hatch; the default C path links and optimizes in-process on Julia's libLLVM (see Compiler).","category":"section"},{"location":"internals/#LLVMEnvironment","page":"Internals & Dispatch","title":"LLVMEnvironment","text":"Source: src/Builder/LLVMEnvironment.jl\n\nDetects the system LLVM/Clang toolchain by searching standard paths and version-suffixed binaries. Falls back to LLVM_full_jll when no system toolchain is found. Caches results in ~/.replibuild/toolchain.toml with a 24-hour TTL.","category":"section"},{"location":"internals/#EnvironmentDoctor","page":"Internals & Dispatch","title":"EnvironmentDoctor","text":"Source: src/Builder/EnvironmentDoctor.jl\n\ncheck_environment() validates both toolchain buckets: the C bucket (JLL clang + Julia's resident libLLVM — no external install required) and the C++/Tier 2 bucket (system LLVM/MLIR 21+, Clang, mlir-tblgen, CMake 3.20+, and libJLCS.so). Returns a ToolchainStatus struct indicating which tiers are available, with OS-specific install instructions for missing components.","category":"section"},{"location":"internals/#DependencyResolver","page":"Internals & Dispatch","title":"DependencyResolver","text":"Source: src/Builder/DependencyResolver.jl\n\nProcesses the [dependencies] table from replibuild.toml. Supports three dependency types:\n\nType Mechanism\ngit Shallow clone (--depth 1) into .replibuild_cache/deps/<name>/; re-fetches on tag change\nlocal Scanned in-place; no copying\nsystem pkg-config --cflags to inject include paths\n\nThe exclude list is applied after scanning. Resolved source files merge into the compilation graph before the compile step.","category":"section"},{"location":"internals/#PackageRegistry","page":"Internals & Dispatch","title":"PackageRegistry","text":"Source: src/Builder/PackageRegistry.jl\n\nLocal package registry at ~/.replibuild/registry/. Provides:\n\nregister() — Store a project's build configuration\nuse() — Build + wrap + load, with artifact caching in ~/.replibuild/builds/<hash>/; on a local miss, fetches the package config from the RepliBuild-Hub community registry\nsearch() — Query the Hub index by name, description, tags, or language\nlist_registry() — Print all registered packages with hash, source, and build status\nunregister() — Remove a package and clean cached builds\n\nThe build-cache key (hash_config) covers the TOML, sources, headers, and project git HEAD plus the generator fingerprint — RepliBuild's own version and git revision — so upgrading RepliBuild invalidates wrappers produced by older codegen. Cached wrappers resolve their .so sibling-first via @__DIR__, with the baked absolute path as fallback.\n\nThe REPLIBUILD_HOME environment variable overrides the default registry location; REPLIBUILD_HUB_URL points Hub operations at a private mirror.","category":"section"},{"location":"internals/#STLWrappers","page":"Internals & Dispatch","title":"STLWrappers","text":"Source: src/Wrapper/Cpp/STLWrappers.jl\n\nDetects STL container types (std::vector, std::string, std::map, etc.) in DWARF metadata and generates accessor functions. These are used by the MLIR IR generator (src/IRGen/ir_gen/STLContainerGen.jl) to produce JIT thunks for STL container methods.","category":"section"},{"location":"internals/#ASTWalker","page":"Internals & Dispatch","title":"ASTWalker","text":"Source: src/Builder/ASTWalker.jl\n\nClang.jl-based AST walker for enum extraction. Handles enum class, hex values, namespaces, and other constructs that are difficult to extract reliably from DWARF alone. Replaces the earlier regex-based approach.","category":"section"},{"location":"internals/#ClangJLBridge","page":"Internals & Dispatch","title":"ClangJLBridge","text":"Source: src/Builder/ClangJLBridge.jl\n\nIntegration module for Clang.jl header parsing. Used by the wrapper generator when use_clang_jl = true to supplement DWARF metadata with AST-level information.","category":"section"},{"location":"internals/#Scaffold","page":"Internals & Dispatch","title":"Scaffold","text":"Source: src/Builder/PackageRegistry.jl (scaffold_package function)\n\nGenerates a distributable Julia package from a registered RepliBuild project. The scaffolded package includes the compiled shared library, generated wrapper module, and a standard Julia Project.toml — ready for Pkg.add().","category":"section"},{"location":"internals/#RepliBuild.Wrapper.extract_symbols-Tuple{String, TypeRegistry}","page":"Internals & Dispatch","title":"RepliBuild.Wrapper.extract_symbols","text":"extract_symbols(binary_path::String, registry::TypeRegistry; demangle::Bool=true, method::Symbol=:nm)\n\nExtract symbols from compiled binary using specified method.\n\nMethods\n\n:nm - Fast, basic symbol extraction\n:objdump - Detailed with debug info (TODO)\n:all - Try all methods and merge (TODO)\n\nReturns vector of SymbolInfo objects.\n\n\n\n\n\n","category":"method"},{"location":"internals/#RepliBuild.Wrapper.is_enum_like-Tuple{String}","page":"Internals & Dispatch","title":"RepliBuild.Wrapper.is_enum_like","text":"is_enum_like(cpp_type::String)::Bool\n\nCheck if a C++ type looks like an enum based on naming conventions. Returns false — enum detection is unreliable by name alone (uppercase names are more likely structs/classes). Real enum identification uses DWARF metadata via _is_enum_type() in DispatchLogic.jl which checks __enum__ prefixed keys.\n\n\n\n\n\n","category":"method"},{"location":"internals/#RepliBuild.Wrapper.is_function_pointer_like-Tuple{String}","page":"Internals & Dispatch","title":"RepliBuild.Wrapper.is_function_pointer_like","text":"is_function_pointer_like(cpp_type::String)::Bool\n\nCheck if a C++ type contains function pointer syntax. Looks for patterns: (name), ()(args), (^name) (blocks)\n\n\n\n\n\n","category":"method"},{"location":"internals/#RepliBuild.Wrapper.is_struct_like-Tuple{String}","page":"Internals & Dispatch","title":"RepliBuild.Wrapper.is_struct_like","text":"is_struct_like(cpp_type::String)::Bool\n\nCheck if a C++ type looks like a struct/class based on naming conventions. Structs typically: start with uppercase, contain only alphanumeric + underscore.\n\n\n\n\n\n","category":"method"},{"location":"internals/#RepliBuild.Wrapper.wrap_basic-Tuple{RepliBuildConfig, String}","page":"Internals & Dispatch","title":"RepliBuild.Wrapper.wrap_basic","text":"wrap_basic(config::RepliBuildConfig, library_path::String; generate_docs::Bool=true)\n\nGenerate basic Julia wrapper from binary symbols only (no headers required).\n\nQuality: ~40% - Conservative types, placeholder signatures, requires manual refinement. Use when: Headers not available, quick prototyping, binary-only distribution.\n\n\n\n\n\n","category":"method"},{"location":"internals/#RepliBuild.Wrapper.wrap_library-Tuple{RepliBuildConfig, String}","page":"Internals & Dispatch","title":"RepliBuild.Wrapper.wrap_library","text":"wrap_library(config::RepliBuildConfig, library_path::String;\n             headers::Vector{String}=String[],\n             generate_tests::Bool=false,\n             generate_docs::Bool=true)\n\nGenerate Julia wrapper for compiled library.\n\nAlways uses introspective (DWARF metadata) wrapping when metadata is available, otherwise falls back to basic symbol-only extraction with conservative types.\n\nArguments\n\nconfig: RepliBuildConfig with wrapper settings\nlibrary_path: Path to compiled library (.so, .dylib, .dll)\nheaders: Optional header files (currently unused, reserved for future)\ngenerate_tests: Generate test file (default: false, TODO)\ngenerate_docs: Include comprehensive documentation (default: true)\n\nReturns\n\nPath to generated Julia wrapper file\n\n\n\n\n\n","category":"method"},{"location":"internals/#RepliBuild.Wrapper.ParamInfo","page":"Internals & Dispatch","title":"RepliBuild.Wrapper.ParamInfo","text":"ParamInfo\n\nInformation about a function parameter.\n\n\n\n\n\n","category":"type"},{"location":"internals/#RepliBuild.Wrapper.SymbolInfo","page":"Internals & Dispatch","title":"RepliBuild.Wrapper.SymbolInfo","text":"SymbolInfo\n\nComprehensive information about a symbol extracted from binary/headers.\n\n\n\n\n\n","category":"type"},{"location":"internals/#RepliBuild.Wrapper.TypeRegistry","page":"Internals & Dispatch","title":"RepliBuild.Wrapper.TypeRegistry","text":"TypeRegistry\n\n\n\n\n\n","category":"type"},{"location":"internals/#RepliBuild.Wrapper.TypeStrictness","page":"Internals & Dispatch","title":"RepliBuild.Wrapper.TypeStrictness","text":"TypeStrictness\n\n\n\n\n\n","category":"type"},{"location":"internals/#RepliBuild.Compiler.assemble_bitcode-Tuple{String, String}","page":"Internals & Dispatch","title":"RepliBuild.Compiler.assemble_bitcode","text":"Assemble LLVM IR text (.ll) to bitcode (.bc). Uses Julia's own libLLVM via the C API (LLVMParseIRInContext + LLVMWriteBitcodeToFile) to guarantee bitcode version compatibility with Base.llvmcall. Falls back to Clangunifiedjll, then system llvm-as.\n\n\n\n\n\n","category":"method"},{"location":"internals/#RepliBuild.Compiler.build_type_registry-Tuple{Vector{Dict{String, Any}}}","page":"Internals & Dispatch","title":"RepliBuild.Compiler.build_type_registry","text":"Build type registry from functions. Collects all unique types used in the codebase.\n\n\n\n\n\n","category":"method"},{"location":"internals/#RepliBuild.Compiler.compile_project-Tuple{RepliBuildConfig}","page":"Internals & Dispatch","title":"RepliBuild.Compiler.compile_project","text":"Complete compilation workflow: discover sources, compile, link, create binary. This is the main entry point for building a project.\n\n\n\n\n\n","category":"method"},{"location":"internals/#RepliBuild.Compiler.compile_single_to_ir","page":"Internals & Dispatch","title":"RepliBuild.Compiler.compile_single_to_ir","text":"Compile a single C++ file to LLVM IR. Returns (irfilepath, success, exitcode).\n\ncompile_fingerprint is the compile-config hash gating the per-file cache; the batch path (compile_to_ir) computes it once and threads it in. When called standalone (empty default) it is computed here so the fingerprint check still applies — _detect_target_triple shells out, so the batch avoids paying that per file by passing it down.\n\n\n\n\n\n","category":"function"},{"location":"internals/#RepliBuild.Compiler.compile_to_ir-Tuple{RepliBuildConfig, Vector{String}}","page":"Internals & Dispatch","title":"RepliBuild.Compiler.compile_to_ir","text":"Compile multiple C++ files to LLVM IR (with parallel support). Returns vector of IR file paths.\n\n\n\n\n\n","category":"method"},{"location":"internals/#RepliBuild.Compiler.compute_project_hash-Tuple{RepliBuildConfig}","page":"Internals & Dispatch","title":"RepliBuild.Compiler.compute_project_hash","text":"Compute a content hash for the entire project: replibuild.toml + source files + git HEAD. Returns a hex string that changes when anything relevant changes.\n\n\n\n\n\n","category":"method"},{"location":"internals/#RepliBuild.Compiler.create_executable","page":"Internals & Dispatch","title":"RepliBuild.Compiler.create_executable","text":"Create executable from LLVM IR. Returns path to executable file.\n\n\n\n\n\n","category":"function"},{"location":"internals/#RepliBuild.Compiler.create_library","page":"Internals & Dispatch","title":"RepliBuild.Compiler.create_library","text":"Create shared library from LLVM IR. Returns path to library file.\n\n\n\n\n\n","category":"function"},{"location":"internals/#RepliBuild.Compiler.dwarf_type_to_julia-Tuple{AbstractString}","page":"Internals & Dispatch","title":"RepliBuild.Compiler.dwarf_type_to_julia","text":"Comprehensive C/C++ type to Julia type mapping. Handles all standard C/C++ types including sized integers, pointers, and qualifiers.\n\n\n\n\n\n","category":"method"},{"location":"internals/#RepliBuild.Compiler.extract_class_name-Tuple{String}","page":"Internals & Dispatch","title":"RepliBuild.Compiler.extract_class_name","text":"Extract class name from method signature (only considers '::' in the function-name prefix). Example: \"Calculator::compute(int, int)\" -> \"Calculator\" Example: \"sumvector(std::vector<int> const&)\"  -> \"\"  (free function) Example: \"bool ggufreader::read<int>(...)\"     -> \"ggufreader\"  (NOT \"bool ggufreader\")\n\n\n\n\n\n","category":"method"},{"location":"internals/#RepliBuild.Compiler.extract_compilation_metadata-Tuple{RepliBuildConfig, Vector{String}, String}","page":"Internals & Dispatch","title":"RepliBuild.Compiler.extract_compilation_metadata","text":"Extract compilation metadata from source files and binary. This is the core of automatic wrapper generation!\n\n\n\n\n\n","category":"method"},{"location":"internals/#RepliBuild.Compiler.extract_dwarf_return_types-Tuple{String}","page":"Internals & Dispatch","title":"RepliBuild.Compiler.extract_dwarf_return_types","text":"Extract return types and struct definitions from DWARF debug info. Returns: (returntypesdict, structdefsdict)\n\nreturntypes: Dict{mangledname => {ctype, juliatype, size}}\nstructdefs: Dict{structname => {members: [{name, type, offset}]}}\n\n\n\n\n\n","category":"method"},{"location":"internals/#RepliBuild.Compiler.extract_function_name-Tuple{String}","page":"Internals & Dispatch","title":"RepliBuild.Compiler.extract_function_name","text":"Extract function name from demangled signature. Example: \"Calculator::compute(int, int, char)\" -> \"compute\" Example: \"sumvector(std::vector<int, std::allocator<int> > const&)\" -> \"sumvector\" Example: \"bool gguf_reader::read<std::vector<int> >(...)\" -> \"read<std::vector<int> >\"\n\n\n\n\n\n","category":"method"},{"location":"internals/#RepliBuild.Compiler.extract_mangled_name-Tuple{String, String}","page":"Internals & Dispatch","title":"RepliBuild.Compiler.extract_mangled_name","text":"Extract mangled symbol name from nm output.\n\n\n\n\n\n","category":"method"},{"location":"internals/#RepliBuild.Compiler.extract_stl_method_symbols-Tuple{String, Vector{String}}","page":"Internals & Dispatch","title":"RepliBuild.Compiler.extract_stl_method_symbols","text":"Extract STL method symbols from the compiled binary. Uses nm to find mangled names of template-instantiated STL methods. Returns Dict mapping normalized container type -> vector of method info dicts.\n\n\n\n\n\n","category":"method"},{"location":"internals/#RepliBuild.Compiler.extract_symbols_from_binary-Tuple{String}","page":"Internals & Dispatch","title":"RepliBuild.Compiler.extract_symbols_from_binary","text":"Extract symbol information from compiled binary using nm. Returns vector of symbol dictionaries with mangled/demangled names.\n\n\n\n\n\n","category":"method"},{"location":"internals/#RepliBuild.Compiler.generate_macro_shims-Tuple{RepliBuildConfig, Vector{String}}","page":"Internals & Dispatch","title":"RepliBuild.Compiler.generate_macro_shims","text":"Generate a C/C++ shim file to instantiate requested macros as typed functions so they appear in the DWARF metadata and can be wrapped.\n\n\n\n\n\n","category":"method"},{"location":"internals/#RepliBuild.Compiler.generate_template_instantiations-Tuple{RepliBuildConfig, Vector{String}}","page":"Internals & Dispatch","title":"RepliBuild.Compiler.generate_template_instantiations","text":"Generate a dummy C++ file to explicitly instantiate requested templates so they appear in the DWARF metadata.\n\n\n\n\n\n","category":"method"},{"location":"internals/#RepliBuild.Compiler.get_type_size-Tuple{AbstractString}","page":"Internals & Dispatch","title":"RepliBuild.Compiler.get_type_size","text":"Get type size in bytes from C/C++ type name.\n\n\n\n\n\n","category":"method"},{"location":"internals/#RepliBuild.Compiler.infer_return_type-Tuple{String}","page":"Internals & Dispatch","title":"RepliBuild.Compiler.infer_return_type","text":"Infer return type from demangled function signature using pattern matching. This is a fallback when DWARF debug info is unavailable for a given function.\n\n\n\n\n\n","category":"method"},{"location":"internals/#RepliBuild.Compiler.ingest_library-Tuple{RepliBuildConfig}","page":"Internals & Dispatch","title":"RepliBuild.Compiler.ingest_library","text":"ingest_library(config) -> String\n\nBYOB (\"bring your own build\") path: skip the whole Clang/LLVM compile pipeline and ingest a pre-built .so produced by upstream's own build system. Runs DWARF metadata extraction on the binary and produces compilation_metadata.json next to a copy of the library inside the project's output dir, so wrap() finds it where it expects.\n\nIngested libraries dispatch through Tier 3 (ccall) only — there's no LTO bitcode to enable Tier 1 llvmcall, and no per-file IR for Tier 2 thunks.\n\nRequires config.ingest !== nothing (set by a [ingest] section in replibuild.toml).\n\n\n\n\n\n","category":"method"},{"location":"internals/#RepliBuild.Compiler.is_project_cache_valid-Tuple{RepliBuildConfig}","page":"Internals & Dispatch","title":"RepliBuild.Compiler.is_project_cache_valid","text":"Check if the project-level cache is valid. Returns true if all artifacts exist and the content hash matches, meaning the build can be skipped entirely.\n\n\n\n\n\n","category":"method"},{"location":"internals/#RepliBuild.Compiler.link_optimize_ir-Tuple{RepliBuildConfig, Vector{String}, String}","page":"Internals & Dispatch","title":"RepliBuild.Compiler.link_optimize_ir","text":"Link multiple LLVM IR files and optimize. Returns linked IR path (String) or original IR files (Vector{String}) on llvm-link failure.\n\n\n\n\n\n","category":"method"},{"location":"internals/#RepliBuild.Compiler.needs_recompile","page":"Internals & Dispatch","title":"RepliBuild.Compiler.needs_recompile","text":"Check if a source file needs recompilation.\n\nCache hit requires BOTH the IR being newer than the source (mtime) AND the compile fingerprint matching the one that produced it (<ir>.key sidecar). A missing key (cache from before fingerprinting) forces a recompile so the key is established. Pass compile_fingerprint=\"\" to skip the fingerprint check (legacy mtime-only behavior).\n\n\n\n\n\n","category":"function"},{"location":"internals/#RepliBuild.Compiler.parse_function_signatures","page":"Internals & Dispatch","title":"RepliBuild.Compiler.parse_function_signatures","text":"Parse function signatures from symbol information. Infers parameter types and return types from demangled names.\n\n\n\n\n\n","category":"function"},{"location":"internals/#RepliBuild.Compiler.parse_parameters","page":"Internals & Dispatch","title":"RepliBuild.Compiler.parse_parameters","text":"Parse parameter types from demangled signature. Example: \"add(int, int)\" -> [{\"type\": \"int\", \"julia_type\": \"Cint\"}, ...] Template-aware: angle brackets in nested types do not confuse the splitter.\n\n\n\n\n\n","category":"function"},{"location":"internals/#RepliBuild.Compiler.sanitize_ir_for_julia-Tuple{String}","page":"Internals & Dispatch","title":"RepliBuild.Compiler.sanitize_ir_for_julia","text":"sanitize_ir_for_julia(ir_text::String) -> String\n\nSanitize LLVM IR text for compatibility with Julia's internal LLVM (18). Strips LLVM 19–22 attributes/instructions, debug metadata, and converts varargs function bodies to extern declarations (vastart/vaend can't be JIT-compiled). Uses inlinehint (not alwaysinline) to avoid recursive inliner explosion on large modules.\n\nCoverage by LLVM version:   19: GEP nuw, #dbg* records, inrange(), captures(), deadonunwind,       initializes(), allocptr, icmp samesign, range(), trunc nuw/nsw,       zext/uitofp nneg   20: GEP nusw, or disjoint, fptrunc/fpext fast-math flags, ptrtoaddr→ptrtoint   21: (covered by 19 patterns — captures/deadon_unwind/allocptr landed here)   22: ptrtoaddr instruction\n\nThis is the single source of truth for IR compatibility — used by both the C source LTO pipeline and the MLIR AOT thunks pipeline.\n\n\n\n\n\n","category":"method"},{"location":"internals/#RepliBuild.Compiler.save_compilation_metadata-Tuple{RepliBuildConfig, Vector{String}, String}","page":"Internals & Dispatch","title":"RepliBuild.Compiler.save_compilation_metadata","text":"Save compilation metadata to JSON file next to binary. This enables automatic wrapper generation!\n\n\n\n\n\n","category":"method"},{"location":"internals/#RepliBuild.Compiler.save_project_hash-Tuple{RepliBuildConfig}","page":"Internals & Dispatch","title":"RepliBuild.Compiler.save_project_hash","text":"Save the project hash after a successful build.\n\n\n\n\n\n","category":"method"},{"location":"internals/#RepliBuild.ConfigurationManager.create_default_config","page":"Internals & Dispatch","title":"RepliBuild.ConfigurationManager.create_default_config","text":"Create a default configuration and save to file.\n\n\n\n\n\n","category":"function"},{"location":"internals/#RepliBuild.ConfigurationManager.get_build_path-Tuple{RepliBuildConfig}","page":"Internals & Dispatch","title":"RepliBuild.ConfigurationManager.get_build_path","text":"Get full build path (project_root + paths.build)\n\n\n\n\n\n","category":"method"},{"location":"internals/#RepliBuild.ConfigurationManager.get_cache_path-Tuple{RepliBuildConfig}","page":"Internals & Dispatch","title":"RepliBuild.ConfigurationManager.get_cache_path","text":"Get full cache path (project_root + cache.directory)\n\n\n\n\n\n","category":"method"},{"location":"internals/#RepliBuild.ConfigurationManager.get_compile_flags-Tuple{RepliBuildConfig}","page":"Internals & Dispatch","title":"RepliBuild.ConfigurationManager.get_compile_flags","text":"Get compiler flags\n\n\n\n\n\n","category":"method"},{"location":"internals/#RepliBuild.ConfigurationManager.get_include_dirs-Tuple{RepliBuildConfig}","page":"Internals & Dispatch","title":"RepliBuild.ConfigurationManager.get_include_dirs","text":"Get all include directories (from config)\n\n\n\n\n\n","category":"method"},{"location":"internals/#RepliBuild.ConfigurationManager.get_library_name-Tuple{RepliBuildConfig}","page":"Internals & Dispatch","title":"RepliBuild.ConfigurationManager.get_library_name","text":"Get library output name (uses config or auto-generates)\n\n\n\n\n\n","category":"method"},{"location":"internals/#RepliBuild.ConfigurationManager.get_module_name-Tuple{RepliBuildConfig}","page":"Internals & Dispatch","title":"RepliBuild.ConfigurationManager.get_module_name","text":"Get wrapper module name (uses config or auto-generates)\n\n\n\n\n\n","category":"method"},{"location":"internals/#RepliBuild.ConfigurationManager.get_output_path-Tuple{RepliBuildConfig}","page":"Internals & Dispatch","title":"RepliBuild.ConfigurationManager.get_output_path","text":"Get full output path (project_root + paths.output)\n\n\n\n\n\n","category":"method"},{"location":"internals/#RepliBuild.ConfigurationManager.get_source_files-Tuple{RepliBuildConfig}","page":"Internals & Dispatch","title":"RepliBuild.ConfigurationManager.get_source_files","text":"Get all C++ source files (from config)\n\n\n\n\n\n","category":"method"},{"location":"internals/#RepliBuild.ConfigurationManager.is_cache_enabled-Tuple{RepliBuildConfig}","page":"Internals & Dispatch","title":"RepliBuild.ConfigurationManager.is_cache_enabled","text":"Should use cache?\n\n\n\n\n\n","category":"method"},{"location":"internals/#RepliBuild.ConfigurationManager.is_parallel_enabled-Tuple{RepliBuildConfig}","page":"Internals & Dispatch","title":"RepliBuild.ConfigurationManager.is_parallel_enabled","text":"Should run parallel compilation?\n\n\n\n\n\n","category":"method"},{"location":"internals/#RepliBuild.ConfigurationManager.is_stage_enabled-Tuple{RepliBuildConfig, Symbol}","page":"Internals & Dispatch","title":"RepliBuild.ConfigurationManager.is_stage_enabled","text":"Check if a stage is in the workflow\n\n\n\n\n\n","category":"method"},{"location":"internals/#RepliBuild.ConfigurationManager.load_config","page":"Internals & Dispatch","title":"RepliBuild.ConfigurationManager.load_config","text":"Load RepliBuildConfig from TOML file. This is the ONLY function that parses TOML - all other modules use this.\n\n\n\n\n\n","category":"function"},{"location":"internals/#RepliBuild.ConfigurationManager.merge_compile_flags-Tuple{RepliBuildConfig, Vector{String}}","page":"Internals & Dispatch","title":"RepliBuild.ConfigurationManager.merge_compile_flags","text":"Merge compile flags into config (creates new config). Used for runtime overrides like: compile(sources, flags=[\"-O3\"])\n\n\n\n\n\n","category":"method"},{"location":"internals/#RepliBuild.ConfigurationManager.print_config-Tuple{RepliBuildConfig}","page":"Internals & Dispatch","title":"RepliBuild.ConfigurationManager.print_config","text":"Print configuration summary\n\n\n\n\n\n","category":"method"},{"location":"internals/#RepliBuild.ConfigurationManager.save_config-Tuple{RepliBuildConfig}","page":"Internals & Dispatch","title":"RepliBuild.ConfigurationManager.save_config","text":"Save configuration to TOML file. Only saves user-configurable settings (not runtime data).\n\n\n\n\n\n","category":"method"},{"location":"internals/#RepliBuild.ConfigurationManager.validate_config!-Tuple{RepliBuildConfig}","page":"Internals & Dispatch","title":"RepliBuild.ConfigurationManager.validate_config!","text":"Validate config and throw error if invalid.\n\n\n\n\n\n","category":"method"},{"location":"internals/#RepliBuild.ConfigurationManager.validate_config-Tuple{RepliBuildConfig}","page":"Internals & Dispatch","title":"RepliBuild.ConfigurationManager.validate_config","text":"Validate configuration and return list of errors (empty if valid).\n\n\n\n\n\n","category":"method"},{"location":"internals/#RepliBuild.ConfigurationManager.with_discovery_results-Tuple{RepliBuildConfig}","page":"Internals & Dispatch","title":"RepliBuild.ConfigurationManager.with_discovery_results","text":"Update both source files and include dirs (creates new config). Used after discovery completes.\n\n\n\n\n\n","category":"method"},{"location":"internals/#RepliBuild.ConfigurationManager.with_include_dirs-Tuple{RepliBuildConfig, Vector{String}}","page":"Internals & Dispatch","title":"RepliBuild.ConfigurationManager.with_include_dirs","text":"Update include directories in config (creates new config). Used after discovery finds include paths.\n\n\n\n\n\n","category":"method"},{"location":"internals/#RepliBuild.ConfigurationManager.with_source_files-Tuple{RepliBuildConfig, Vector{String}}","page":"Internals & Dispatch","title":"RepliBuild.ConfigurationManager.with_source_files","text":"Update source files in config (creates new config). Used after discovery finds C++ sources.\n\n\n\n\n\n","category":"method"},{"location":"internals/#RepliBuild.ConfigurationManager.BinaryConfig","page":"Internals & Dispatch","title":"RepliBuild.ConfigurationManager.BinaryConfig","text":"Nested struct for [binary] section\n\n\n\n\n\n","category":"type"},{"location":"internals/#RepliBuild.ConfigurationManager.CacheConfig","page":"Internals & Dispatch","title":"RepliBuild.ConfigurationManager.CacheConfig","text":"Nested struct for [cache] section\n\n\n\n\n\n","category":"type"},{"location":"internals/#RepliBuild.ConfigurationManager.CompileConfig","page":"Internals & Dispatch","title":"RepliBuild.ConfigurationManager.CompileConfig","text":"Nested struct for [compile] section\n\n\n\n\n\n","category":"type"},{"location":"internals/#RepliBuild.ConfigurationManager.DependenciesConfig","page":"Internals & Dispatch","title":"RepliBuild.ConfigurationManager.DependenciesConfig","text":"Nested struct for [dependencies] section\n\n\n\n\n\n","category":"type"},{"location":"internals/#RepliBuild.ConfigurationManager.DependencyItem","page":"Internals & Dispatch","title":"RepliBuild.ConfigurationManager.DependencyItem","text":"Nested struct for a single dependency.\n\ntag is a MUTABLE ref — upstream can force-push a tag to different content, and a plain git checkout <tag> would fetch it with no signal. commit is the optional expected 40-hex object name for that ref: when set, DependencyResolver hard-errors if the resolved HEAD differs, which is the only check that survives a cache wipe (the sidecar marker cannot, since clean() deletes it along with the clone). Leave commit empty to keep the old trust-the-tag behaviour.\n\n\n\n\n\n","category":"type"},{"location":"internals/#RepliBuild.ConfigurationManager.DiscoveryConfig","page":"Internals & Dispatch","title":"RepliBuild.ConfigurationManager.DiscoveryConfig","text":"Nested struct for [discovery] section\n\n\n\n\n\n","category":"type"},{"location":"internals/#RepliBuild.ConfigurationManager.IngestConfig","page":"Internals & Dispatch","title":"RepliBuild.ConfigurationManager.IngestConfig","text":"Nested struct for [ingest] section.\n\nPresence of this section flips RepliBuild from source-build mode (compile via Clang/LLVM) to ingest mode: the user supplies a pre-built .so (built by upstream's own build system), and RepliBuild only runs DWARF metadata extraction + wrapping. Ingested libraries dispatch through Tier 3 (ccall) only — no LTO bitcode, no Tier 1 llvmcall.\n\n\n\n\n\n","category":"type"},{"location":"internals/#RepliBuild.ConfigurationManager.LLVMConfig","page":"Internals & Dispatch","title":"RepliBuild.ConfigurationManager.LLVMConfig","text":"Nested struct for [llvm] section\n\n\n\n\n\n","category":"type"},{"location":"internals/#RepliBuild.ConfigurationManager.LinkConfig","page":"Internals & Dispatch","title":"RepliBuild.ConfigurationManager.LinkConfig","text":"Nested struct for [link] section\n\n\n\n\n\n","category":"type"},{"location":"internals/#RepliBuild.ConfigurationManager.PathsConfig","page":"Internals & Dispatch","title":"RepliBuild.ConfigurationManager.PathsConfig","text":"Nested struct for [paths] section\n\n\n\n\n\n","category":"type"},{"location":"internals/#RepliBuild.ConfigurationManager.ProjectConfig","page":"Internals & Dispatch","title":"RepliBuild.ConfigurationManager.ProjectConfig","text":"Nested struct for [project] section\n\n\n\n\n\n","category":"type"},{"location":"internals/#RepliBuild.ConfigurationManager.RepliBuildConfig","page":"Internals & Dispatch","title":"RepliBuild.ConfigurationManager.RepliBuildConfig","text":"Main immutable configuration structure. All modules receive this struct - it's the single source of truth.\n\n\n\n\n\n","category":"type"},{"location":"internals/#RepliBuild.ConfigurationManager.TypesConfig","page":"Internals & Dispatch","title":"RepliBuild.ConfigurationManager.TypesConfig","text":"Nested struct for [types] section - Type validation settings\n\n\n\n\n\n","category":"type"},{"location":"internals/#RepliBuild.ConfigurationManager.WorkflowConfig","page":"Internals & Dispatch","title":"RepliBuild.ConfigurationManager.WorkflowConfig","text":"Nested struct for [workflow] section\n\n\n\n\n\n","category":"type"},{"location":"internals/#RepliBuild.ConfigurationManager.WrapConfig","page":"Internals & Dispatch","title":"RepliBuild.ConfigurationManager.WrapConfig","text":"Nested struct for [wrap] section\n\n\n\n\n\n","category":"type"},{"location":"internals/#RepliBuild.Discovery.discover","page":"Internals & Dispatch","title":"RepliBuild.Discovery.discover","text":"discover(target_dir::String=pwd(); force::Bool=false, unsafe::Bool=false, build::Bool=false, wrap::Bool=false) -> String\n\nMain discovery pipeline - scans project and generates configuration.\n\nProcess:\n\nCheck for existing replibuild.toml (project identified by presence of replibuild.toml)\nScan all files and categorize\nDetect and analyze binaries\nWalk AST dependencies using clang\nGenerate or update replibuild.toml with discovered data\nOptionally run build and wrap pipeline\n\nArguments\n\ntarget_dir: Project directory (default: current directory)\nforce: Force rediscovery even if replibuild.toml exists\nunsafe: Bypass safety checks (use with extreme caution)\nbuild: Automatically run build() after discovery (default: false)\nwrap: Automatically run wrap() after build (requires build=true, default: false)\n\nSafety Features\n\nDiscovery is scoped ONLY to target_dir and subdirectories\nWill not scan outside the project root\nSkips .git, build, node_modules, .cache directories\n\nReturns\n\nPath to generated replibuild.toml file\n\nExamples\n\n# Discover only\ntoml_path = RepliBuild.Discovery.discover()\n\n# Discover and build\ntoml_path = RepliBuild.Discovery.discover(build=true)\n\n# Full pipeline: discover → build → wrap\ntoml_path = RepliBuild.Discovery.discover(build=true, wrap=true)\n\n# Then use the TOML path:\nRepliBuild.build(toml_path)\nRepliBuild.wrap(toml_path)\n\n\n\n\n\n","category":"function"},{"location":"internals/#RepliBuild.DWARFParser.export_vtable_json-Tuple{VtableInfo, String}","page":"Internals & Dispatch","title":"RepliBuild.DWARFParser.export_vtable_json","text":"export_vtable_json(vtinfo::VtableInfo, output_path::String)\n\nExport vtable information to JSON for inspection or use by other tools.\n\n\n\n\n\n","category":"method"},{"location":"internals/#RepliBuild.DWARFParser.parse_dwarf_output-Tuple{String}","page":"Internals & Dispatch","title":"RepliBuild.DWARFParser.parse_dwarf_output","text":"parse_dwarf_output(dwarf_text::String) -> Dict{String, ClassInfo}\n\nParse llvm-dwarfdump output to extract class and vtable information.\n\n\n\n\n\n","category":"method"},{"location":"internals/#RepliBuild.DWARFParser.parse_symbol_table-Tuple{String}","page":"Internals & Dispatch","title":"RepliBuild.DWARFParser.parse_symbol_table","text":"parse_symbol_table(nm_output::String) -> Tuple{Dict{String, UInt64}, Dict{String, UInt64}}\n\nParse nm output to extract vtable and method addresses. Returns (vtableaddresses, methodaddresses).\n\n\n\n\n\n","category":"method"},{"location":"internals/#RepliBuild.DWARFParser.parse_vtables-Tuple{String}","page":"Internals & Dispatch","title":"RepliBuild.DWARFParser.parse_vtables","text":"parse_vtables(binary_path::String) -> VtableInfo\n\nExtract complete vtable information from a binary using DWARF and symbol table.\n\nArguments\n\nbinary_path: Path to compiled binary with debug info\n\nReturns\n\nVtableInfo containing classes, vtable addresses, and method addresses\n\n\n\n\n\n","category":"method"},{"location":"internals/#RepliBuild.DWARFParser.read_vtable_data-Tuple{String, UInt64, Int64}","page":"Internals & Dispatch","title":"RepliBuild.DWARFParser.read_vtable_data","text":"read_vtable_data(binary_path::String, vtable_addr::UInt64, num_entries::Int) -> Vector{UInt64}\n\nRead actual vtable function pointers from binary at given address.\n\n\n\n\n\n","category":"method"},{"location":"internals/#RepliBuild.DWARFParser.ClassInfo","page":"Internals & Dispatch","title":"RepliBuild.DWARFParser.ClassInfo","text":"Information about a C++ class with virtual methods\n\n\n\n\n\n","category":"type"},{"location":"internals/#RepliBuild.DWARFParser.MemberInfo","page":"Internals & Dispatch","title":"RepliBuild.DWARFParser.MemberInfo","text":"Information about a data member (field)\n\n\n\n\n\n","category":"type"},{"location":"internals/#RepliBuild.DWARFParser.VirtualMethod","page":"Internals & Dispatch","title":"RepliBuild.DWARFParser.VirtualMethod","text":"Information about a virtual method\n\n\n\n\n\n","category":"type"},{"location":"internals/#RepliBuild.DWARFParser.VtableInfo","page":"Internals & Dispatch","title":"RepliBuild.DWARFParser.VtableInfo","text":"Complete vtable information from binary\n\n\n\n\n\n","category":"type"},{"location":"config/#The-replibuild.toml-Reference","page":"Configuration (replibuild.toml)","title":"The replibuild.toml Reference","text":"replibuild.toml is the interface. Every piece of library-specific knowledge lives in this file and nowhere else — not in a generator fork, not in a patch, not in a hand-written shim. discover() writes what it can see by scanning; the rest is what you have to tell it, and §2 is the list of exactly what that is.\n\nThe file is read in one place (ConfigurationManager.load_config) and frozen into an immutable RepliBuildConfig. Everything below is what that parser actually does — including the keys it validates loudly, the keys it warns about, and the handful it currently accepts and ignores.\n\n","category":"section"},{"location":"config/#1.-How-the-file-is-created-and-maintained","page":"Configuration (replibuild.toml)","title":"1. How the file is created and maintained","text":"toml = RepliBuild.discover(\"path/to/project\")      # writes replibuild.toml\nRepliBuild.discover(\"path/to/project\", force=true) # rescan, preserving intent keys\n\nDiscovery scans sources, walks the #include graph, picks wrap.language from the file extensions it found, and writes a config that compiles. It cannot infer anything that is not visible in the source tree's shape — see §2.\n\nForced re-discovery preserves hand-curated keys. discover(force=true) regenerates the file, and without carry-over it would emit these empty, silently destroying your intent. The preserved set (Discovery.PRESERVED_TOML_KEYS) is:\n\nSection Key\n[types] templates\n[types] template_headers\n[wrap] varargs\n[wrap] macros\n[wrap] shim_headers\n[wrap] cstring_owned\n[wrap] tier1\n[link] promote_statics\n\nA regenerated non-empty value wins; an empty or absent one gets the preserved value. If you add a new user-intent key to the schema, add it to that list too — otherwise the next forced re-discovery eats it. (This is not hypothetical: the stl_test fixture was red for six weeks because [types].templates vanished on every run.)\n\nAnything not in that table is regenerated from the scan. Notably [compile] flags, include_dirs, and [dependencies] are not preserved — keep a hand-written config under version control rather than relying on force=true to round-trip it.\n\nsave_config only writes keys with non-default or non-empty values, so a round-tripped file is smaller than the full schema. Absent keys take the parse-time defaults in §3, which are not always the same as what discover() writes — the divergences are flagged in the tables.\n\n","category":"section"},{"location":"config/#2.-What-discovery-cannot-know","page":"Configuration (replibuild.toml)","title":"2. What discovery cannot know","text":"This is the important section. Everything here is information that exists only in a human's head or in the upstream build system, and that RepliBuild will not guess. Omit it and you get a build that succeeds and a wrapper that is missing things or is wrong.\n\nIf your library… You must declare Symptom if you don't\nis C++ and you want STL/template types [types] templates + template_headers The type never appears in DWARF; members typed Any, methods missing\nexposes API through preprocessor macros [wrap.macros.*] + [wrap] shim_headers The macro simply does not exist in the wrapper — no error\nhas variadic functions worth calling with arguments [wrap.varargs] Only the zero-variadic base wrapper is generated\nreturns malloc'd char* [wrap.cstring_owned] Every call leaks the C buffer\ndefines its public \"def\" structs header-only -fstandalone-debug in [compile] flags Structs wrap to empty Julia structs\nships tests/examples/backends in the same repo exclude on the dependency Unrelated sources compile in and pollute the DWARF\nkeeps headers somewhere other than <root>, <root>/include, <root>/src [compile] include_dirs Compile failure, or a TU silently missing declarations\nneeds -lm, -lpthread, -ldl [link] link_libraries Loads fine; Tier-1 slices demote, or a symbol is missing at first use\nis configured by its build system through -D [compile] flags / [compile] defines One TU fails to compile, or a feature is silently off\nbuilds with -fvisibility=hidden Nothing extra for macros (handled); mind [compile] flags parity with upstream Exports missing from nm -g\nis C and you want cross-language inlining [wrap.tier1] enable = true Everything stays Tier 3 ccall (correct, just not inlined)\nmust be reproducible commit on the dependency A moved tag silently changes what you compiled","category":"section"},{"location":"config/#2.1-C-templates-and-the-STL-—-\"STL-needs-STL-in-the-TOML\"","page":"Configuration (replibuild.toml)","title":"2.1 C++ templates and the STL — \"STL needs STL in the TOML\"","text":"A template that is never instantiated does not exist in the binary. The compiler emits DWARF for code it generated; an uninstantiated std::vector<int> generated nothing, so there is nothing to read. No amount of header scanning changes this — the information is not missing, it was never created.\n\nSo you name the instantiations you want, and RepliBuild writes a stub .cpp that forces them:\n\n[types]\ntemplates        = [\"std::vector<int>\", \"std::string\", \"std::map<int, int>\"]\ntemplate_headers = [\"<vector>\", \"<string>\", \"<map>\"]\n\ntemplates entries are C++ type spellings, exactly as you would write them in template class X;.\nCommon STL headers are auto-detected from the type name (std::vector → <vector>, std::map → <map>, and the same for unordered_map, set, unordered_set, deque, list, basic_string/string). template_headers is for everything else — your own headers, or a library's:\ntemplates        = [\"fmt::basic_memory_buffer<char>\"]\ntemplate_headers = [\"<fmt/format.h>\"]\nTypedef spellings are expanded for you where an explicit instantiation would otherwise be invalid C++: std::string → std::basic_string<char>, and the same for wstring / u16string / u32string.\nEntries starting with < are emitted as #include <…>; anything else becomes #include \"…\".\n\nOnce instantiated, the STL method symbols are extracted (extract_stl_method_symbols) and become container accessor thunks. A C++ library whose public API is STL-free needs none of this — box2d 2.4 reports stl_methods = 0 and declares no templates.\n\nwarning: This section is preserved but not derived\n[types].templates is one of the preserved keys precisely because a rescan would otherwise wipe it. If your C++ types come back as Any or your struct collapses to an opaque blob, this is the first key to check.","category":"section"},{"location":"config/#2.2-Preprocessor-macros","page":"Configuration (replibuild.toml)","title":"2.2 Preprocessor macros","text":"Macros do not survive compilation, so they are absent from both the symbol table and DWARF. RepliBuild generates a typed C/C++ shim function per declared macro, compiles it into the library, and wraps the resulting real symbol.\n\n[wrap]\nshim_headers = [\"lua.h\", \"lauxlib.h\"]     # what the shim TU #includes\n\n# Function-like macro: `args` present → emits MACRO(arg0, arg1)\n[wrap.macros.lua_pop]\nret  = \"void\"\nargs = [\"lua_State*\", \"int\"]\n\n# Value macro: `args` ABSENT → emits the bare MACRO expression\n[wrap.macros.LUA_REGISTRYINDEX]\nret = \"int\"\n\nret and args are C type strings, pasted verbatim into generated C. They are not Julia types. (Contrast §2.3, which is Julia types — this asymmetry is the single easiest mistake to make in this file.)\nThe presence of the args key is the switch, not its contents: args = [] means a zero-argument function-like macro (MACRO()), while omitting args entirely means a value macro (MACRO).\nShims are emitted with __attribute__((used, visibility(\"default\"))) so they survive -fvisibility=hidden builds and LTO internalization — without that, projects like box2d3 would silently lose every macro.\nIn Julia the replibuild_shim_ prefix is stripped: you call lua_pop(L, 1).\n\nwarning: The shim's `#include` must resolve inside your tree\nThe shim TU is generated under the build cache, not inside the source tree, so a bare #include \"blake3.h\" can fall through the -I path to a system-installed copy of the header at a different version — and bake wrong values into every macro, with no other symptom. A guard checks each direct shim include and hard-errors if it resolved outside the project/dependency tree. The fix is either a more specific subpath (shim_headers = [\"c/blake3.h\"]) or an explicit -I<dir> in [compile] flags.","category":"section"},{"location":"config/#2.3-Variadic-functions","page":"Configuration (replibuild.toml)","title":"2.3 Variadic functions","text":"DWARF records that a function is variadic. It cannot record what you intend to pass. Declare one entry per concrete overload:\n\n[wrap.varargs]\nlua_pushfstring = [\n    [\"Cstring\"],\n    [\"Cint\"],\n    [\"Cstring\", \"Cint\"],\n]\n\nThree rules, each of which has bitten someone:\n\nList only the variadic arguments. The fixed parameters (lua_State*, the const char* fmt) come from DWARF and are prepended automatically.\nThe names are Julia types, not C types. Allowed: Any, Bool, Cstring, Cwstring, Cchar, Cuchar, Cshort, Cushort, Cint, Cuint, Clong, Culong, Clonglong, Culonglong, Cintmax_t, Cuintmax_t, Csize_t, Cssize_t, Cptrdiff_t, Cwchar_t, Cfloat, Cdouble, Cintptr_t, Cuintptr_t, the sized Int*/UInt*/Float* types, and Ptr{Name}. Anything else is a hard error naming the entry.\nIt is a list of lists. f = [\"Cint\"] is one overload taking… nothing coherent; the parser rejects it and tells you to write f = [[\"Cint\"]].\n\nVararg wrappers lower as true variadic calls (the @ccall semicolon form), so the x86-64 SysV variadic protocol — including the AL register-count setup that gates the callee's va_start — is formally correct, float varargs included. A Ptr{X} overload sanitizes to _PtrX in the generated function name.","category":"section"},{"location":"config/#2.4-char*-return-ownership","page":"Configuration (replibuild.toml)","title":"2.4 char* return ownership","text":"Whether a returned char* must be freed, and by what, is not in DWARF and not reliably in the name. Declare it:\n\n[wrap.cstring_owned]\ncJSON_Print     = \"cJSON_free\"\nsqlite3_mprintf = \"sqlite3_free\"\n\nThe wrapper copies the string into Julia and then frees the C buffer through the named symbol. Undeclared functions get the default policy — Union{String,Nothing}, where NULL is a value rather than an exception, and the buffer is left alone. Every Cstring-returning function additionally gets a raw <name>_ptr variant that returns the pointer unchanged for lifetime-sensitive callers. The value must be a non-empty string; anything else is warned about and ignored.\n\nnote: The policy does not depend on the dispatch tier\nA char* return is presented the same way whether the function is called through ccall, a Tier-1 slice, or a Tier-2 MLIR thunk — the tier decides how the call is made, never how the result is presented. That was not always true: until 2026-08-12 the C++ generator's Tier-2 path skipped the policy entirely, so 75 functions across five Hub packages returned a bare Cstring and any cstring_owned declaration on one of them was silently discarded. Both halves now come from one derivation, and a guard on the write path refuses any wrapper in which a char* return escapes as a bare Cstring without a _ptr sibling.","category":"section"},{"location":"config/#2.5-Header-only-aggregates-need-fstandalone-debug","page":"Configuration (replibuild.toml)","title":"2.5 Header-only aggregates need -fstandalone-debug","text":"Clang's default \"limited\" debug info emits a header-only aggregate that no TU instantiates as a DW_AT_declaration-only stub. Those are exactly the \"def\" structs a caller is supposed to fill in — b2BodyDef, b2FixtureDef, llama_model_params, llama_context_params — so they wrap to empty Julia structs and the API becomes uncallable.\n\n[compile]\nflags = [\"-O2\", \"-fPIC\", \"-std=c++17\", \"-fstandalone-debug\"]\n\nDWARF is RepliBuild's input format, so this flag is load-bearing, not bloat. Reach for it whenever a struct you can see in the header comes out with no fields.","category":"section"},{"location":"config/#2.6-Excluding-what-is-not-the-library","page":"Configuration (replibuild.toml)","title":"2.6 Excluding what is not the library","text":"A repository is usually more than the library: tests, examples, GUI testbeds, vendored third-party trees, alternative backends, other CPU architectures. The dependency resolver walks the whole tree, so anything compilable gets compiled and lands in the DWARF the wrapper is generated from.\n\n[dependencies.box2d]\ntype = \"git\"\nurl  = \"https://github.com/erincatto/box2d.git\"\ntag  = \"v2.4.1\"\nexclude = [\"testbed\", \"unit-test\", \"extern\", \"docs\"]\n\nexclude entries are matched against the path relative to the dependency root, and the match is deliberately generous — an entry hits if it equals the filename, or is a prefix, suffix, or any substring of the relative path. That is why \"ggml-cuda/\" and \"tools/\" work as directory filters and \"xmltest.cpp\" works as a file filter, and also why a very short entry can over-match: prefer \"tests\" over \"t\", and keep a trailing / on directory entries. Excluding aggressively is normal — llama.cpp's config excludes roughly thirty paths, each with a comment explaining why.\n\nThe walker additionally drops directories named test, tests, testes, example, examples, fuzzing, build, .git, doc, and docs on its own, so those need no entry (several Hub configs list them anyway, harmlessly).","category":"section"},{"location":"config/#2.7-Include-roots","page":"Configuration (replibuild.toml)","title":"2.7 Include roots","text":"The resolver adds <clone>, <clone>/include and <clone>/src automatically. Anything at a different depth must be named:\n\n[compile]\ninclude_dirs = [\n    \".replibuild_cache/deps/llamacpp/ggml/include\",\n    \".replibuild_cache/deps/llamacpp/ggml/src\",\n]\n\nEntries are passed through verbatim as -I<dir>. Relative paths resolve against the directory containing replibuild.toml, because build() runs there.","category":"section"},{"location":"config/#2.8-Link-libraries","page":"Configuration (replibuild.toml)","title":"2.8 Link libraries","text":"[link]\nlink_libraries = [\"m\", \"pthread\", \"dl\"]   # → -lm -lpthread -ldl\nlink_dirs      = [\"/opt/thing/lib\"]       # → -L/opt/thing/lib\n\nA shared library links happily with sin left undefined, so omitting -lm is not a build error — it is a .so whose DT_NEEDED chain is short. That matters beyond the obvious runtime failure: the Tier-1 slice pre-flight scopes symbol lookup to the library plus its DT_NEEDED chain, so on miniaudio a missing -lm demoted all 51 slices that declare sin/pow/log back to ccall with no other sign.\n\nwarning: `link_libraries` vs `extra_link_libs`\n[link] link_libraries is the source-build key. [ingest] extra_link_libs is the ingest-mode key. Putting extra_link_libs in a source-build config is silently ignored — it is not part of [link] and nothing reads it there.","category":"section"},{"location":"config/#2.9-Defines-the-build-system-would-have-supplied","page":"Configuration (replibuild.toml)","title":"2.9 Defines the build system would have supplied","text":"Upstream's CMake or Makefile often injects defines that no header declares. Without them a TU fails to compile, or a subsystem is silently absent:\n\n[compile]\nflags = [\n    \"-O2\", \"-fPIC\",\n    \"-DGGML_USE_CPU\",                 # else the backend registry is empty\n    '-DGGML_VERSION=\"0.18.1\"',        # else ggml.c is the one TU that won't build\n]\n\n[compile.defines]                     # equivalent, emitted as -Dkey=value\nSQLITE_THREADSAFE = \"1\"\n\n[compile.defines] always emits -Dkey=value; for a bare -DFOO with no value, use [compile] flags. Both feed the compile fingerprint, so changing either correctly invalidates the per-file IR cache.","category":"section"},{"location":"config/#2.10-Tier-1-(C-only)","page":"Configuration (replibuild.toml)","title":"2.10 Tier 1 (C only)","text":"Opt in per package. See Zero-cost LTO dispatch for what a slice is:\n\n[wrap.tier1]\nenable = true\n\nwarning: Experimental — off by default, and not a supported tier\nTier 1 is a side project inside RepliBuild. enable defaults to false, no Hub package sets it, and its tests are deliberately unwired from devtests.jl. Leaving it off costs you nothing but inlining: every function stays on ccall, which is correct. The guide has the mechanism and the caveats.","category":"section"},{"location":"config/#2.11-Pinning-content,-not-just-a-name","page":"Configuration (replibuild.toml)","title":"2.11 Pinning content, not just a name","text":"A git tag is a mutable ref. commit is the only check that survives a cache wipe:\n\n[dependencies.lua]\ntype   = \"git\"\nurl    = \"https://github.com/lua/lua.git\"\ntag    = \"v5.4.7\"\ncommit = \"1ab3208a1fceb12fca8f24ba57d6e13c5bff15e3\"\n\nIt must be a full 40-hex object name — abbreviated SHAs are rejected at config load, because they are ambiguous against future history. A mismatch after clone/checkout is a hard error, not a warning.\n\n","category":"section"},{"location":"config/#3.-Section-reference","page":"Configuration (replibuild.toml)","title":"3. Section reference","text":"Types are TOML types. \"Default\" is what the parser uses when the key is absent; where discover() writes something different, that is called out.","category":"section"},{"location":"config/#[project]","page":"Configuration (replibuild.toml)","title":"[project]","text":"Key Type Default Notes\nname String directory basename Drives lib<name>.so and the module name\nroot String directory containing the TOML Absolute or relative\nuuid String freshly generated An unparseable UUID warns and is regenerated. Keep the one discover() wrote — an absent uuid means a new one on every load\nversion String — Not part of the schema. Every Hub config carries one and it is useful documentation (it should match the pinned tag), but RepliBuild parses and drops it; the Hub's own index.toml is what publishes a version","category":"section"},{"location":"config/#[paths]","page":"Configuration (replibuild.toml)","title":"[paths]","text":"Key Type Default Notes\noutput String \"julia\" Wrapper + .so + compilation_metadata.json land here\nbuild String \"build\" Intermediate IR and objects\nsource String \"src\" Parsed, not consulted — sources come from discovery or [compile] source_files\ninclude String \"include\" Parsed, not consulted — include roots come from discovery or [compile] include_dirs\ncache String \".replibuild_cache\" Parsed, not consulted — the cache directory is [cache] directory","category":"section"},{"location":"config/#[discovery]","page":"Configuration (replibuild.toml)","title":"[discovery]","text":"Key Type Default\nenabled Bool true\nwalk_dependencies Bool true\nmax_depth Int 10\nignore_patterns Vector{String} [\"build\", \".git\", \".cache\"]\nparse_ast Bool true\n\nnote: Note\nThis whole section is currently parsed and stored but not consulted by the scanner. It round-trips faithfully; it does not change behaviour. Do not reach for ignore_patterns to trim a dependency — use exclude on the dependency (§2.6).","category":"section"},{"location":"config/#[compile]","page":"Configuration (replibuild.toml)","title":"[compile]","text":"Key Type Default Notes\nflags Vector{String} [\"-std=c++17\", \"-fPIC\"] discover() writes [\"-fPIC\"] for a C-only scan\ndefines Table{String→String} {} Emitted as -Dkey=value\nparallel Bool true Multi-threaded per-file compilation\nsource_files Vector{String} [] (auto-discovered) Explicit list; validated to exist at load\ninclude_dirs Vector{String} [] (auto-discovered) Passed as -I; relative to the TOML's directory\naot_thunks Bool false Pre-compile Tier-2 thunks into <name>_thunks.so; needs libJLCS.so\n\nChanging flags, defines, or include_dirs correctly invalidates the per-file IR cache — it is keyed on a compile fingerprint as well as source mtime.","category":"section"},{"location":"config/#[link]","page":"Configuration (replibuild.toml)","title":"[link]","text":"Key Type Default Notes\noptimization_level String \"0\" \"0\"–\"3\", \"s\", \"z\"; a leading O/o is stripped, so \"O2\" works. discover() writes \"0\"; Hub configs use \"2\"\nenable_lto Bool true for language = \"c\", else false Whole-module llvmcall payload — see the warning below\nlink_libraries Vector{String} [] -l entries\nlink_dirs Vector{String} [] -L entries\nfallback Bool false C only. false = link/optimize/assemble in-process on Julia's resident libLLVM, and a failure is a hard error. true = external llvm-link/opt. C++ always uses the external pipeline\npromote_statics Bool true C in-process bucket only. Rename anything a slice may bind by declare but that cannot reach the .so dynamic symbol table to an exported __rb_<lib>_<name>. Required by [wrap.tier1], harmless otherwise\n\nwarning: `enable_lto` embeds the whole module — prefer `[wrap.tier1]`\nenable_lto embeds the entire linked module at every llvmcall site. At whole-library scale that can crash Julia's JIT, and it duplicates file-local static state between the embedded bitcode and the .so — two calls to the same library can observe different copies of its internal state. Every Hub config sets enable_lto = false.This is not a statement about Tier 1. The per-function slicing path under [wrap.tier1] is immune to both problems and is the supported way to get Tier 1. The knobs are independent, and enable_lto = false together with [wrap.tier1] enable = true is the intended combination.","category":"section"},{"location":"config/#[binary]","page":"Configuration (replibuild.toml)","title":"[binary]","text":"Key Type Default Notes\ntype String \"shared\" \"shared\", \"static\", \"executable\"; anything else warns and falls back to \"shared\"\noutput_name String \"\" → lib<project>.so (.a when static) In ingest mode this is forced to the ingested .so's basename\nstrip_symbols Bool false Parsed, not consulted","category":"section"},{"location":"config/#[wrap]","page":"Configuration (replibuild.toml)","title":"[wrap]","text":"Key Type Default Notes\nlanguage String \"cpp\" \"c\", \"cpp\", \"rust\" (experimental). Selects generator, toolchain, and the enable_lto default. discover() sets it from the scanned extensions\nenabled Bool true \nmodule_name String \"\" → CamelCase of the project name \nuse_clang_jl Bool true false skips AST extraction (DWARF-only path)\nshim_headers Vector{String} [] Headers the macro-shim TU includes (preserved)\ndag Bool false Export DAG type-graph diffs to <project>/dag/\nstyle String \"clang\" \"clang\", \"basic\", \"none\"; validated, but not currently dispatched on — the basic symbol-only generator is selected by missing compilation_metadata.json, not by this key","category":"section"},{"location":"config/#[wrap.varargs]-*(preserved)*","page":"Configuration (replibuild.toml)","title":"[wrap.varargs] (preserved)","text":"Typed overloads for variadic functions. Julia type names, variadic arguments only, list-of-lists. See §2.3 for the full rules and the allowed type names.\n\n[wrap.varargs]\nlua_pushfstring = [[\"Cstring\"], [\"Cint\"], [\"Cstring\", \"Cint\"]]","category":"section"},{"location":"config/#[wrap.macros]-*(preserved)*","page":"Configuration (replibuild.toml)","title":"[wrap.macros] (preserved)","text":"One sub-table per macro. ret and args are C type strings; the presence of args selects function-like vs value macro. See §2.2.\n\n[wrap.macros.deflateInit]\nret  = \"int\"\nargs = [\"z_stream*\", \"int\"]\n\n[wrap.macros.ZLIB_VERSION]\nret = \"const char*\"","category":"section"},{"location":"config/#[wrap.cstring_owned]-*(preserved)*","page":"Configuration (replibuild.toml)","title":"[wrap.cstring_owned] (preserved)","text":"function name = \"deallocator symbol\". See §2.4.","category":"section"},{"location":"config/#[wrap.tier1]-*(preserved)*","page":"Configuration (replibuild.toml)","title":"[wrap.tier1] (preserved)","text":"C projects only. Routes eligible functions through Base.llvmcall on a per-function bitcode slice instead of ccall.\n\n[wrap.tier1]\nenable       = true\nmax_slice_kb = 64\nallow_setjmp = false\nexclude      = [\"lua_error\"]\n\nKey Type Default Notes\nenable Bool false Run the slicer over every Tier-1 candidate and emit Base.llvmcall for the accepted ones\nmax_slice_kb Int 64 A tripwire, not a tuning knob — declarations-only slices are kilobyte-sized regardless of function size, so a hit means something unexpected got embedded\nallow_setjmp Bool false Allow slices whose own body calls setjmp/longjmp or a returns_twice callee. Direct-reach only: setjmp buried in a .so-side callee never trips the gate, and does not need to — longjmp across the calling Julia frame is identical exposure for ccall and llvmcall\nexclude Vector{String} [] Names (mangled or plain) that stay on ccall unconditionally\n\nRequires [link] promote_statics = true (the default) and the in-process C pipeline. On a fallback or ingest build there is no promoted module, so Tier 1 disables itself for the whole wrap with a warning rather than shipping unverified slices. Every non-accepted function emits exactly the ccall it would have emitted with Tier 1 off, and the module exports TIER1_FUNCTIONS::Set{String}.\n\nThe wrap prints two counts — N slices accepted then N slices emitted (M functions). A gap between them is the ABI shape gate refusing a Cstring or struct crossing, not a failure.","category":"section"},{"location":"config/#[types]","page":"Configuration (replibuild.toml)","title":"[types]","text":"Key Type Default Notes\nstrictness String \"warn\" \"strict\", \"warn\", \"permissive\"; any unrecognised value silently becomes \"warn\"\nallow_unknown_structs Bool true Opaque pointers for unknown structs instead of failing\nallow_unknown_enums Bool false Map unknown enums to Int32\nallow_function_pointers Bool true Ptr{Cvoid} for function pointers\ncustom Table{String→String} {} Custom type mappings, e.g. MyHandle = \"Ptr{Cvoid}\"; merged into the type registry\ntemplates Vector{String} [] C++ instantiations to force-emit (preserved) — §2.1\ntemplate_headers Vector{String} [] Headers for the instantiation stub (preserved) — §2.1\n\nStrictness modes: strict fails on any type that cannot be mapped exactly; warn reports the imperfect mapping and continues (Ptr{Cvoid} for complex pointers); permissive falls back silently. This governs type mapping; it does not relax the exact-or-opaque rule for struct layouts, which is unconditional.","category":"section"},{"location":"config/#[dependencies]","page":"Configuration (replibuild.toml)","title":"[dependencies]","text":"Each dependency is a named sub-table. Both spellings work:\n\n[dependencies.cjson]\ntype    = \"git\"\nurl     = \"https://github.com/DaveGamble/cJSON.git\"\ntag     = \"v1.7.18\"\ncommit  = \"acc76239bee01d8e9c858ae2cab296704e52d916\"\nexclude = [\"test\", \"fuzzing\"]\n\n[dependencies]\npugixml = { type = \"git\", url = \"https://github.com/zeux/pugixml.git\", tag = \"v1.15\" }\n\nKey Type Default Notes\ntype String \"local\" \"git\", \"local\", \"system\" — note the default is local, not git; a [dependencies.x] with a url but no type will not clone\nurl String \"\" Required for type = \"git\"\ntag String \"\" (default branch) Tag or branch; a tag makes the clone shallow\ncommit String \"\" Expected 40-hex object name. Validated at load; mismatch after checkout is a hard error — §2.11\npath String \"\" Required for type = \"local\"; scanned in place, never copied\npkg_config String \"\" For type = \"system\"; runs pkg-config --cflags\nexclude Vector{String} [] Paths to skip during source injection — §2.6\n\nGit dependencies clone into .replibuild_cache/deps/<name>/ and re-checkout on tag change. Resolved sources merge into the compilation graph before [compile] runs, so they are compiled with the same toolchain and flags as your own files — which is exactly why the resulting DWARF is consistent across the whole project.","category":"section"},{"location":"config/#[ingest]","page":"Configuration (replibuild.toml)","title":"[ingest]","text":"Experimental, C only. The presence of this section is the mode switch: no compilation happens, only DWARF extraction and wrapper generation over a .so you built yourself (with -g). Ingested libraries dispatch through Tier 3 (ccall) exclusively — no LTO bitcode, no Tier-1 slices, no Tier-2 thunks, and the C++ API surface of an ingested binary is unsupported.\n\nKey Type Default Notes\nlibrary String — (required) Path to the pre-built .so; relative paths resolve against the TOML's directory. Missing → the section is ignored with a warning and you silently get source-build mode\nheaders Vector{String} [] Header search dirs for type extraction and the noexcept scan; merged into the include dirs\nextra_link_libs Vector{String} [] Additional -l libraries the wrapper loads at runtime. Ingest-only — in a source build use [link] link_libraries\n\nGenerate it with RepliBuild.ingest(\"/path/to/libfoo.so\", headers=[...]) rather than by hand.","category":"section"},{"location":"config/#[llvm]","page":"Configuration (replibuild.toml)","title":"[llvm]","text":"Key Type Default Notes\ntoolchain String \"auto\" \"auto\", \"system\", \"jll\"; anything else warns and falls back to \"auto\"\nversion String \"\" (auto-detect) Relevant to the C++ bucket; the C bucket uses the bundled JLL clang and Julia's own libLLVM","category":"section"},{"location":"config/#[cache]","page":"Configuration (replibuild.toml)","title":"[cache]","text":"Key Type Default Notes\nenabled Bool true Skip recompiling unchanged files\ndirectory String \".replibuild_cache\" The real cache path — this key, not [paths] cache","category":"section"},{"location":"config/#[workflow]","page":"Configuration (replibuild.toml)","title":"[workflow]","text":"Key Type Default\nstages Vector{String} [\"discover\", \"compile\", \"link\", \"binary\", \"wrap\"]\n\nValidated against the five known stage names (an unknown one is a config validation error), but not currently consulted — the pipeline runs its fixed order. Do not use it to skip a stage; call the individual verbs instead.\n\n","category":"section"},{"location":"config/#4.-Interactions-and-precedence","page":"Configuration (replibuild.toml)","title":"4. Interactions and precedence","text":"wrap.language decides more than the generator. It selects the toolchain (JLL clang + in-process libLLVM for c; system clang++ + the external pipeline for cpp) and the default value of [link] enable_lto. Setting it wrong produces a build that works and a wrapper that is missing the whole reason you chose the other one.\n[wrap.tier1] requires [link] promote_statics and the in-process C path. fallback = true, ingest mode, or a C++ project all disable Tier 1 for the whole wrap, loudly.\nenable_lto and [wrap.tier1] are independent knobs and both can emit. Slicing did not replace the whole-module call-site path.\n[ingest] wins over source-build keys. A config with both gets warnings naming [compile] source_files and any git/local dependency, and proceeds in ingest mode.\n[binary] output_name is overridden in ingest mode when left empty, so wrap() finds the library where it expects it.\n[cache] directory is the cache path; [paths] cache is inert. Likewise [paths] source and [paths] include.\n\n","category":"section"},{"location":"config/#5.-What-fails-loudly,-what-warns,-what-is-ignored","page":"Configuration (replibuild.toml)","title":"5. What fails loudly, what warns, what is ignored","text":"Knowing which is which is the difference between a five-minute fix and an afternoon.\n\nHard error at config load\n\n[dependencies.<n>] commit that is not a full 40-hex object name.\n[wrap.varargs.<f>] that is not a list, or whose entries are not lists — with a message showing the correct shape.\nvalidate_config!: empty project.name; non-existent project.root; a [compile] source_files entry that does not exist on disk; an optimization_level outside 0/1/2/3/s/z; an unknown [workflow] stages entry; an invalid binary.type / wrap.style / llvm.toolchain / types.strictness value reaching validation.\n\nHard error later in the pipeline\n\nA resolved dependency HEAD that disagrees with the declared commit.\nA macro shim #include that resolved outside the project/dependency tree.\nA vararg overload naming a type outside the allowed set (§2.3).\nA wrapper whose ccall signatures would name an undeclared type — refused before it is written.\nA generated wrapper containing an unqualified error(\"…\") call — refused, because a library that defines its own error symbol would rebind it and take out every failure path in the module.\n\nWarning, then proceeds\n\nMissing [project] section or project.name.\nAn unparseable uuid (a new one is generated).\n[ingest] without library → the section is dropped and you are in source-build mode.\n[ingest] alongside source_files or fetchable dependencies.\nInvalid binary.type / wrap.style / llvm.toolchain → default substituted.\nA [wrap.cstring_owned] value that is not a non-empty string → entry ignored.\nA struct whose layout cannot be modelled consistently → degrades to a correctly-sized opaque region (capped at ten warnings plus a summary).\n\nAccepted and ignored\n\n[discovery] (whole section), [workflow] stages, [wrap] style, [binary] strip_symbols, [paths] source / include / cache.\nextra_link_libs outside [ingest].\nAny key not in the schema — TOML parses it, the config drops it. There is no unknown-key warning, so a typo'd key name is silent. Check your spelling against §3 when a setting appears to have no effect.\n\n","category":"section"},{"location":"config/#6.-Worked-configurations","page":"Configuration (replibuild.toml)","title":"6. Worked configurations","text":"","category":"section"},{"location":"config/#A-C-library-from-git,-with-Tier-1","page":"Configuration (replibuild.toml)","title":"A C library from git, with Tier 1","text":"[project]\nname    = \"lua\"\nversion = \"5.4.7\"\nroot    = \".\"\n\n[dependencies.lua]\ntype    = \"git\"\nurl     = \"https://github.com/lua/lua.git\"\ntag     = \"v5.4.7\"\ncommit  = \"1ab3208a1fceb12fca8f24ba57d6e13c5bff15e3\"\nexclude = [\"onelua.c\", \"lua.c\", \"luac.c\", \"ltests.c\"]   # the interpreter, not the library\n\n[compile]\nflags = [\"-O1\", \"-fPIC\", \"-DLUA_USE_LINUX\"]\n\n[link]\nenable_lto         = false      # whole-module payload stays off\noptimization_level = \"2\"\n\n[binary]\ntype = \"shared\"\n\n[wrap]\nlanguage     = \"c\"\nshim_headers = [\"lua.h\", \"lauxlib.h\", \"lualib.h\"]\n\n[wrap.tier1]\nenable = true\n\n[wrap.varargs]\nlua_pushfstring = [[\"Cstring\"], [\"Cint\"], [\"Cdouble\"], [\"Cstring\", \"Cint\"]]\n\n[wrap.macros.lua_pop]\nret  = \"void\"\nargs = [\"lua_State*\", \"int\"]\n\n[wrap.macros.LUA_REGISTRYINDEX]\nret = \"int\"\n\n[types]\nstrictness              = \"warn\"\nallow_unknown_structs   = true\nallow_function_pointers = true\n\n[cache]\nenabled = true","category":"section"},{"location":"config/#A-C-library-with-templates","page":"Configuration (replibuild.toml)","title":"A C++ library with templates","text":"[project]\nname    = \"fmt\"\nversion = \"12.1.0\"\nroot    = \".\"\n\n[dependencies.fmt]\ntype    = \"git\"\nurl     = \"https://github.com/fmtlib/fmt.git\"\ntag     = \"12.1.0\"\ncommit  = \"407c905e45ad75fc29bf0f9bb7c5c2fd3475976f\"\nexclude = [\"test\", \"doc\", \"support\", \"benchmark\"]\n\n[compile]\nflags = [\"-O2\", \"-fPIC\", \"-std=c++20\"]\n\n[link]\nenable_lto         = false      # C++ default; Tier 2/3\noptimization_level = \"2\"\n\n[wrap]\nlanguage = \"cpp\"\n\n[types]\nstrictness       = \"warn\"\ntemplates        = [\"fmt::basic_memory_buffer<char>\"]\ntemplate_headers = [\"<fmt/format.h>\"]\n\n[cache]\nenabled = true","category":"section"},{"location":"config/#A-pre-built-C-binary-(ingest)","page":"Configuration (replibuild.toml)","title":"A pre-built C binary (ingest)","text":"[project]\nname = \"foo\"\n\n[ingest]\nlibrary         = \"/path/to/libfoo.so\"      # must have been built with -g\nheaders         = [\"/path/to/include\"]\nextra_link_libs = [\"m\", \"pthread\"]\n\n[wrap]\nlanguage = \"c\"\n\n","category":"section"},{"location":"config/#7.-Troubleshooting-index","page":"Configuration (replibuild.toml)","title":"7. Troubleshooting index","text":"What you see Look at\nA C++ template type is missing, or members are Any [types] templates / template_headers — §2.1\nA struct from a header has no fields -fstandalone-debug — §2.5\nA struct became an opaque blob with a layout warning Unmodellable member; check the warning's named type\nA macro-based API is absent from the wrapper [wrap.macros] + shim_headers — §2.2\nMacro values are wrong but everything compiled A shim header resolved to a system copy — §2.2\nprintf-style function only takes its fixed arguments [wrap.varargs] — §2.3\ninvalid type '…' in [wrap.varargs.f] You wrote C types; use Julia types — §2.3\nmust be a list of type lists You wrote f = [\"Cint\"]; write f = [[\"Cint\"]]\nMemory grows on every string-returning call [wrap.cstring_owned] — §2.4\nUnrelated symbols from tests/examples in the wrapper exclude — §2.6\nA TU fails to compile with a missing header [compile] include_dirs — §2.7\nA TU fails on an undeclared identifier upstream's build defines [compile] flags / defines — §2.9\nTier 1 accepted slices but most functions demoted Missing -l in link_libraries — §2.8\n\"Tier 1 disabled for this wrap\" fallback = true, ingest mode, C++, or promote_statics = false\nA setting seems to have no effect at all Check it is not in the \"accepted and ignored\" list — §5\nuse() serves a stale wrapper The build cache keys on RepliBuild's own version too; see the registry","category":"section"}]
}
